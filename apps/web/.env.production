# Build de producción del PWA vecino + admin comercio.
# Esta URL queda QUEMADA en el JS del build — todos los usuarios la verán.
# Cambialo cuando deployes el API a Railway/Render/Fly y haga un nuevo build:
#   pnpm --filter @misanpedro/web build && pnpm --filter @misanpedro/web deploy:gh-pages
#
# Si dejás esta URL placeholder, los registros y el catálogo no van a funcionar
# en el sitio público (GH Pages).
VITE_API_URL=https://api-production-43c52.up.railway.app

VITE_SUPPORT_WHATSAPP=5493329000000
VITE_SUPPORT_EMAIL=soporte@micuidad.com
