# Cazabug loop 2 — barrido por recorrido y dimensión

Totales: {"P0":2,"P1":28,"P2":71,"P3":41} · 142 confirmados de 149 candidatos (7 refutados).


# P0 (2)

### [R3-01] El fix de `enabled` quedó a medias: un owner deshabilitado (o degradado) conserva TODOS sus poderes hasta 1 h — y puede auto-perpetuarse creando otro super
- **Sector:** R3 · lentes 4 Auth cruzada / 5 RBAC / fix incompleto del barrido anterior
- **Síntoma:** Echás a un admin del equipo (Equipo → Deshabilitar / Eliminar). El panel dice "Deshabilitado". Pero durante la próxima hora esa persona sigue creando ciudades, suspendiendo comercios, cancelando suscripciones y — si era super — invitando un admin nuevo con su email personal y rol super. Cuando pasa la hora, ya tiene una cuenta nueva y limpia.
- **Repro:** 1) Loguearte como super A y anotar su access token. 2) Desde otro super B: DELETE /api/v1/owner/admins/<A> (o PATCH {enabled:false}). 3) Con el access token viejo de A (no el refresh): POST /api/v1/owner/admins {email:'atacante@gmail.com',nombre:'X',rol:'super'} → 200 OK, admin creado. 4) POST /api/v1/owner/apps, PATCH /owner/merchants/:id, PATCH /owner/subscriptions/:id → todos 200. Sólo POST /owner/merchants/:id/support-session y /revoke-support devuelven 403.
- **Causa raíz:** 1) ¿Por qué un owner deshabilitado sigue operando? Porque la autorización se resuelve 100% con el claim del JWT. 2) ¿Por qué? `requireOwnerAuth` sólo hace `verifyAccessToken` + `payload.type==='owner'` (auth.ts:71-84) y `requireOwnerRole` lee `auth.rol` del token (auth.ts:97-101), sin tocar la DB. 3) ¿Por qué no se revalida? Decisión explícita de performance documentada en auth.ts:90-92 ("un cambio de rol propaga en ≤1h sin pegarle a la DB en cada request"). 4) ¿Por qué entonces support-session SÍ revalida? Porque en el barrido anterior se parchó SÓLO ese endpoint (owner.ts:901-907 lo dice: "alto poder"), asumiendo que impersonar era la única acción peligrosa. 5) CAUSA TERMINAL: el criterio de "alto poder" se aplicó por endpoint en vez de por superficie. `POST /admins` (crear un super nuevo) es estrictamente MÁS peligroso que impersonar un comercio, y quedó afuera. La revocación de refresh (owner.ts:363, 398) no ayuda: no invalida el access ya emitido.
- **Evidencia:** `apps/api/src/middleware/auth.ts:71`, `apps/api/src/middleware/auth.ts:97`, `apps/api/src/routes/owner.ts:307`, `apps/api/src/routes/owner.ts:396`, `apps/api/src/routes/owner.ts:904`
- **Fix:** Meter la revalidación en el middleware, no endpoint por endpoint: en `requireOwnerAuth`, cargar `Owner.findById(payload.sub).select('enabled rol').lean()` y (a) 403 si `!enabled`, (b) usar el `rol` de la DB — no el del token — para `requireOwnerRole`. Es 1 query indexada por request en un panel de baja concurrencia. Si molesta el costo, cachear en memoria 30-60 s con invalidación al PATCH/DELETE de /admins, y bajar el TTL del access de owner a 5-10 min. Alternativa mínima aceptable: agregar el c

### [R1-01] Sesión del vecino revocada → el front genera un cupón FANTASMA local (código de 6 dígitos que no existe en el backend)
- **Sector:** F1 · lentes 1 costura demo/seed · 3 contrato FE/BE · 4 auth cruzada · 14 errores tragados
- **Síntoma:** El vecino toca "Canjear descuento", la app le muestra un QR y un código de 6 dígitos igual que siempre, y en el mostrador el comercio lo tipea y le sale "No encontramos este código". El vecino jura que lo tiene en pantalla. No hubo ningún cartel de "tu sesión venció" en ningún momento.
- **Repro:** 1) Vecino logueado en el celu A. 2) Desde el celu B (o Perfil → "Cerrar sesión en todos lados") se revocan los refresh tokens. 3) En el celu A, cualquier request cae 401 → doRefresh recibe 401 → tokens.clear('user') borra access+refresh de localStorage PERO stores.ts sigue con `user` seteado (nadie lo limpia: el evento msp:session-expired con subject 'user' no tiene listener). 4) El vecino navega a un cupón y toca "Canjear descuento". 5) handleActivate: `user` existe → no lo manda a /datos; tokens.get('user').access es null → entra al branch local → activationActions.activate(coupon.id) inventa un código con Math.floor(100_000 + Math.random()*900_000) y navega a /activacion/<a-xxxxxxxx>. 6) La pantalla muestra QR + código. 7) El comercio valida: el backend no conoce ese código → 404 → "No encontramos este código".
- **Causa raíz:** 1) ¿Por qué el código no existe en el backend? Porque se creó con activationActions.activate() (store local) en vez de POST /activations. 2) ¿Por qué se tomó el camino local? Porque `!userToken` es true en CuponDetailPage.tsx:111. 3) ¿Por qué había `user` pero no token? Porque tokens.clear(subject) (api.ts:97) borra los tokens sin tocar stores.ts. 4) ¿Por qué nadie sincronizó el store? Porque el único listener de msp:session-expired filtra `detail?.subject !== 'merchant'` y hace return (ApiSync.tsx:38); la mitad "vecino" que promete el comentario de api.ts:33-35 nunca se implementó. 5) CAUSA TERMINAL: el fallback demo/offline (`!userToken → activación local`) sigue vivo en producción y usa la AUSENCIA DE TOKEN como señal de "estoy en modo demo", cuando en producción esa misma señal significa "tu sesión se murió". Dos estados distintos comparten el mismo discriminante.
- **Evidencia:** `apps/web/src/pages/CuponDetailPage.tsx:111`, `apps/web/src/lib/api.ts:97`, `apps/web/src/components/ApiSync.tsx:38`, `apps/web/src/lib/stores.ts:270`, `apps/web/src/lib/api.ts:33`
- **Fix:** (a) En ApiSync agregar el handler de subject === 'user': tokens.clear('user') + userActions.signOut() + toast 'Tu sesión se cerró, entrá con tu email', para que store y tokens nunca queden desincronizados. (b) En CuponDetailPage.handleActivate dejar de usar 'no hay token' como sinónimo de 'modo demo': si user existe pero no hay token, mandar a /datos?next=/cupon/<id>/activar en vez de fabricar una activación local. (c) Gatear todo el camino activationActions.activate() detrás de import.meta.env.


# P1 (28)

### [R1-02] Si le revocaron la sesión (o expiró el refresh), la app le genera al vecino un CÓDIGO DE 6 DÍGITOS FALSO y le dice que lo muestre en el mostrador
- **Sector:** R1 · lentes 1 Costura demo/fallback + 3 Contrato FE/BE + 14 Errores tragados
- **Síntoma:** El vecino toca "Canjear descuento", falla (toast de error), vuelve a tocar y esta vez SÍ aparece la pantalla del cupón activo con QR y código de 6 dígitos y el texto "Mostrá este código en <comercio>. Se canjea una sola vez". Ese código no existe en el backend: el cajero lo tipea y le sale "No encontramos este código. Revisá los dígitos con el cliente". El vecino queda como si estuviera inventando el descuento delante del cajero.
- **Repro:** 1) Vecino logueado en el celular A.
2) Desde el celular B (o el vecino desde Perfil) usa "Cerrar sesión en todos lados" → `revokeAllForSubject('user', ...)` revoca el refresh del celular A.
3) En el celular A, con la app ya abierta (sin cambiar de tab, para que ApiSync no re-corra), abrir un cupón y tocar "Canjear descuento".
4) POST /activations → 401 → `doRefresh` falla → `tokens.clear('user')` (api.ts:146) → se dispara `msp:session-expired` con subject 'user' → NADIE lo escucha → el store local sigue con `user`.
5) Tocar "Canjear descuento" otra vez: ahora `tokens.get('user').access` es null y `user` sigue truthy → se toma la rama local (CuponDetailPage.tsx:111) → `activationActions.activate()` inventa un código con `Math.random()`.
6) Ir al comercio: /redemptions/validate → 404.
- **Causa raíz:** ¿Por qué se genera un código falso? Porque CuponDetailPage tiene una rama offline/demo que crea la activación localmente cuando no hay access token. → ¿Por qué no hay access token si el vecino está "logueado"? Porque `request()` limpia los tokens en el 401 definitivo pero no toca el store `user`. → ¿Por qué no se limpia el store? Porque la limpieza depende de un evento (`msp:session-expired`) que en el cliente sólo tiene handler para `subject === 'merchant'`. → ¿Por qué se agregó el handler sólo para merchant? Porque el problema que se estaba arreglando era el cajero quedando en una página muerta en /admin/canje (ver comentario en ApiSync.tsx:28-33); el lado vecino nunca se cubrió. → CAUSA TERMINAL: la "sesión" del vecino está representada en DOS lugares independientes (tokens en `msp.tok.user.*` y user en `misanpedro.v1`) y sólo uno de los dos tiene camino de invalidación; la rama demo aprovecha esa incoherencia para producir datos que parecen reales.
- **Evidencia:** `apps/web/src/pages/CuponDetailPage.tsx:109`, `apps/web/src/lib/stores.ts:270`, `apps/web/src/lib/api.ts:146`, `apps/web/src/components/ApiSync.tsx:38`, `apps/web/src/pages/CuponActivoPage.tsx:214`
- **Fix:** 1) En ApiSync agregar el handler simétrico para `subject === 'user'`: `userActions.signOut()` + toast "tu sesión se cerró, entrá con tu email" + navegar a /datos. 2) Sacar la rama local de `handleActivate`: si no hay token, mandar a /datos con `next` (que ya está implementado en RegistroPage.irADondeIba). 3) Si se quiere conservar el modo demo, gatearlo con `import.meta.env.PROD === false` como ya se hace en DescuentosPage.tsx:88 y MisCuponesPage.tsx:37.

### [R1-05] El límite anti-fuerza-bruta del OTP se cuenta por IP: 5 recuperaciones de cuenta por hora para TODOS los vecinos detrás del mismo NAT — y se saltea mandando un X-Forwarded-For cualquiera
- **Sector:** R1 · lentes 4 Auth + 13 Degradación + 9 Carreras
- **Síntoma:** Dos caras del mismo defecto. (a) Disponibilidad: el vecino que se cambió de celular pone su email y le sale "demasiados intentos, esperá un momento" aunque sea su primer intento del día, porque es el 6º vecino de esa hora en el WiFi del mostrador / en el bloque CGNAT del operador móvil. Justo el último eslabón del recorrido. (b) Seguridad: cualquiera que mande `X-Forwarded-For: 9.9.9.9` (valor cambiante) tiene límite infinito, así que el cap documentado de 5 mails/hora no existe y se puede bombardear la casilla de cualquier vecino cuyo email se conozca.
- **Repro:** (a) Desde una misma IP pública: POST /api/v1/auth/request-otp con 5 emails registrados distintos → el 6º devuelve 429. Igual con /auth/claim cuando el email ya existe (esa rama se ejecuta a propósito bajo `otpRequestLimiter`, ver issueRecoveryCode).
(b) Repetir el mismo POST cambiando en cada request el header `X-Forwarded-For` (el proxy lo APPENDEA, y `clientKey` toma `split(',')[0]`, es decir el valor del atacante) → nunca aparece el 429; cada request manda un mail real a la víctima.
- **Causa raíz:** ¿Por qué se bloquea un vecino legítimo? Porque el bucket se llavea por IP y nada más. → ¿Por qué la IP no alcanza? Porque el sujeto real de este límite es el EMAIL (una casilla, un cap de mails), no el origen de red; y en Argentina el móvil es CGNAT masivo y el mostrador es un WiFi compartido. → ¿Por qué se sabía y no se corrigió? Porque en user-auth.ts:31 el propio comentario justifica el cap alto del claim "porque varios comercios comparten la IP del mostrador" — el problema estaba identificado, pero el fix del mail-bomb (issueRecoveryCode) metió la rama de recuperación bajo el cap BAJO de 5/h con la misma llave por IP. → ¿Por qué el header se puede spoofear? Porque `clientKey` toma el primer elemento de X-Forwarded-For, que es el que controla el cliente cuando el proxy appendea. → CAUSA TERMINAL: un único limitador genérico por IP, en memoria y sin llave de sujeto, se está usando para tres propósitos distintos (anti-abuso de red, cap de mails por casilla, anti-fuerza-bruta de código) y no sirve bien para ninguno.
- **Evidencia:** `apps/api/src/middleware/security.ts:73`, `apps/api/src/routes/user-auth.ts:33`, `apps/api/src/routes/user-auth.ts:144`, `apps/api/src/routes/user-auth.ts:31`, `nixpacks.toml:55`
- **Fix:** 1) Llavear por SUJETO además de por origen: para request-otp/claim-recovery usar `${prefix}:${appId}:${email}` (cap real de mails por casilla) y dejar el cap por IP mucho más alto como red de contención. 2) Tomar la IP confiable como el ÚLTIMO hop antes del proxy (o un header propio del proveedor), nunca el primer elemento de XFF controlado por el cliente. 3) Mover los buckets a un store compartido (o al menos documentar que con >1 réplica el cap se multiplica). 4) Cuando se devuelva 429 en el f

### [R1-06] El "ahorro total" del vecino se calcula desde dos fuentes distintas: Home/Perfil y Canjeados pueden mostrar números diferentes, y la API corta en 100 sin avisar
- **Sector:** R1 · lentes 8 Plata (totales que no cierran) + 10 Paginación
- **Síntoma:** En el home la billetera dice "Ahorrado en San Pedro: $18.400 · 12 canjes"; el vecino entra a Canjeados y el cartel grande dice otro número. Además, a partir de cierta cantidad de activaciones, canjes viejos desaparecen del historial y del total sin ninguna señal (no hay "ver más", no hay total).
- **Repro:** 1) Vecino con 120 activaciones, de las cuales 40 canjeadas y las canjeadas son de las más viejas.
2) SavingsWallet/ClubCard llaman `api.activations.mine('canjeado')` → GET /activations/me?status=canjeado → devuelve hasta 100 CANJEADAS → total = suma de las 40.
3) CanjeadosPage lee el store local, que se llena con `syncMyActivations()` → `api.activations.mine()` SIN status → devuelve las 100 más recientes por `activatedAt`, mezcladas de todos los estados → sólo entran algunas canjeadas → total = suma de esas pocas.
4) Los dos carteles muestran plata distinta en la misma sesión.
5) Variante sin volumen: el store local es aditivo (`upsertActivation` nunca borra), así que también puede mostrar canjes que el backend ya no devuelve.
- **Causa raíz:** ¿Por qué dos totales? Porque hay dos fuentes de verdad para la misma métrica: el endpoint filtrado por status y el espejo en localStorage. → ¿Por qué existe el espejo? Porque las pantallas viejas (MisCupones/Canjeados/CuponActivo) se construyeron sobre el store local y después se le colgó un sync. → ¿Por qué divergen aun con pocos datos? Porque el sync trae `mine()` sin status (los 100 más recientes de CUALQUIER estado) mientras la billetera pide `mine('canjeado')`: los dos `.limit(100)` recortan conjuntos distintos. → ¿Por qué nadie ve el recorte? Porque `/activations/me` no devuelve `total` ni cursor y el front no tiene paginación. → CAUSA TERMINAL: el "ahorro acumulado" es un agregado derivado que se recalcula en 3 lugares del front (SavingsWallet, ClubCard.computeClub, CanjeadosPage.summary) sobre datasets distintos, en vez de venir calculado del backend sobre la tabla Redemption completa.
- **Evidencia:** `apps/api/src/routes/activations.ts:208`, `apps/web/src/lib/syncActivations.ts:19`, `apps/web/src/components/features/SavingsWallet.tsx:104`, `apps/web/src/components/features/SavingsWallet.tsx:109`, `apps/web/src/pages/CanjeadosPage.tsx:52`
- **Fix:** 1) Un endpoint único de resumen (`GET /activations/me/summary` o derivado de Redemption) que devuelva `{ ahorroTotal, canjes, comercios, mesActual }` calculado en el backend sobre toda la tabla, y que las 3 pantallas consuman ESE número. 2) Paginar `/activations/me` (cursor por `activatedAt` + `total`) y que el front muestre "ver más" en vez de cortar mudo. 3) Que CanjeadosPage lea la misma query que la billetera (`mine('canjeado')`) en vez del store local. 4) En el conteo de comercios usar una 

### [R1-07] La ficha del comercio devuelve el cupón MUTILADO: mismo cupón, otro precio y otro límite de uso que en el catálogo
- **Sector:** R1 · lentes 3 Contrato FE/BE roto + 8 Plata
- **Síntoma:** El mismo cupón muestra "Pagás $8.000 · ahorrás $2.000" en el home y "20% OFF" a secas en la ficha del comercio. Peor: un cupón configurado como 4 usos por mes aparece con el cartel "Ya lo usaste · No disponible" en la ficha del comercio después del PRIMER canje, mientras en el home sigue disponible. El vecino entra por el comercio, ve que no puede usarlo, y se va.
- **Repro:** 1) Crear un cupón con `precioReferencia: 10000`, `porcentaje: 20`, `usoMaxPorPersona: 4`, `usoVentana: 'mes'`.
2) Home (/): la tarjeta viene de GET /coupons → `valorCupon()` entra en modo 'pesos_exacto' → "Pagás $8.000 · ahorrás $2.000"; `estadoUsoCupon` usa max=4/mes.
3) Canjearlo una vez.
4) Abrir /comercio/<slug>: los cupones vienen de GET /merchants/:slug, cuyo serializer local NO manda precioReferencia, tipoOferta, precioFijo, alcance, mostrarAhorroVecino, productoGancho, usoMaxPorPersona ni usoVentana.
5) `apiCouponToLocal` los deja undefined → `valorCupon()` cae a 'solo_pct' ("20% OFF") y `estadoUsoCupon` aplica los defaults `max=1`, ventana `'devida'` → bloqueado.
- **Causa raíz:** ¿Por qué dos formas del mismo cupón? Porque hay DOS serializadores de Coupon: el completo exportado en coupons.ts y una copia privada dentro de merchants.ts. → ¿Por qué la copia quedó atrás? Porque cada vez que se agregó un campo (sistema de valor, límite de uso) se actualizó el de coupons.ts, que es el que se ve en el home, y nadie tocó el de merchants.ts. → ¿Por qué no explota? Porque todos los campos nuevos son opcionales y el front tiene defaults "seguros" (`?? 1`, `?? 'devida'`, `mostrarAhorroVecino !== false`) — el dato faltante no da error, da una respuesta plausible y equivocada. → ¿Por qué es grave? Porque el default de `usoVentana` es el MÁS restrictivo ('devida') y el de valor es el MENOS informativo ('solo_pct'): el campo ausente siempre degrada en contra del vecino. → CAUSA TERMINAL: el contrato del cupón público no está centralizado (packages/shared define schemas de ENTRADA pero no el serializer de SALIDA), así que "agregar un campo al cupón" requiere acordarse de dos lugares.
- **Evidencia:** `apps/api/src/routes/merchants.ts:280`, `apps/api/src/routes/coupons.ts:36`, `apps/web/src/pages/MerchantDetailPage.tsx:84`, `apps/web/src/pages/MerchantDetailPage.tsx:277`, `apps/web/src/lib/usoLimite.ts:38`
- **Fix:** Borrar el `serializeCoupon` privado de merchants.ts y usar el exportado de coupons.ts (ya está exportado). Idealmente moverlo a `packages/shared` junto a los schemas para que el contrato de salida tenga un solo dueño, y agregar un test de contrato que compare las claves devueltas por GET /coupons y GET /merchants/:slug.

### [R2-01] Contrato FE/BE roto: GET /redemptions/clientes nunca devuelve `telefono` (sólo el `whatsapp` legacy) → la campaña de WhatsApp del comercio no le puede enviar a NINGÚN vecino dado de alta con el onboarding actual
- **Sector:** R2 · lentes 3 Contrato FE/BE roto (campo que el back no manda y el front lee) + 1 costura legacy/actual
- **Síntoma:** El comercio entra a "Promociones por WhatsApp", ve el botón "Montar campaña ahora · 37 clientes", confirma "¿Enviar a 37 clientes ahora?" y recibe un toast rojo "No pudimos enviar la campaña · invalid input". Nunca sale un mensaje. En "Mis clientes" la columna WhatsApp del CSV sale vacía para todos y en el detalle del cliente el botón para escribirle por WhatsApp no aparece. El comercio concluye que "la app no anda" sin ninguna pista de por qué.
- **Repro:** 1) Vecino nuevo se da de alta por el flujo actual (POST /auth/claim): se guarda `telefono`, `whatsapp` queda undefined. 2) El vecino activa un cupón y el comercio confirma el canje. 3) El comercio abre /admin/whatsapp: `useApiMerchantClientes()` trae al vecino y el contador de audiencia dice 1. 4) Toca "Montar campaña" y confirma. 5) `recipientPayload` queda [] porque `c.whatsapp ?? c.telefono` es '' y `.filter(r => r.to.length >= 8)` lo descarta. 6) POST /wa/campaign con `recipients: []` → zod `.min(1)` → 400 'invalid input' → toast genérico.
- **Causa raíz:** ¿Por qué no se envía nada? Porque el payload de destinatarios llega vacío. ¿Por qué llega vacío? Porque cada cliente tiene `telefono: ''`. ¿Por qué? Porque el front lee `c.whatsapp ?? c.telefono` y el backend no serializa NINGUNO de los dos con dato: manda `whatsapp: u?.whatsapp` y omite `telefono` por completo. ¿Por qué `whatsapp` está vacío? Porque desde el cambio de identidad de esta semana el alta del vecino (POST /auth/claim) sólo escribe `telefono`; `whatsapp` quedó como campo legacy que ya nadie llena. ¿Por qué nadie lo notó? Porque la migración teléfono→email tocó el MODELO y el alta, pero no el serializador de /redemptions/clientes, que quedó apuntando al campo viejo — y en la base de prod los usuarios legacy (que sí tienen `whatsapp`) enmascaran el problema en cualquier prueba manual con datos viejos. Causa terminal: el serializador del endpoint de clientes no se actualizó junto con el cambio de identidad, y el front lo compensó con un `??` que da la ilusión de fallback pero no tiene dónde caer porque el campo ni siquiera viaja.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:435`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/User.ts:15`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/User.ts:19`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/user-auth.ts:194`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminWhatsappPage.tsx:358`
- **Fix:** En `apps/api/src/routes/redemptions.ts` agregar `telefono: u?.telefono` al objeto de cliente (dejando `whatsapp` por compatibilidad con vecinos legacy), y en el front invertir la precedencia a `c.telefono ?? c.whatsapp ?? ''` en las tres pantallas (AdminWhatsappPage, AdminClientesPage, AdminClienteDetailPage). Además: cuando `recipientPayload.length === 0` pero `recipients.length > 0`, no mandar el POST — mostrar un error explícito del tipo 'Ninguno de tus N clientes tiene un teléfono válido car

### [R2-02] Comercio suspendido/cancelado que valida un código propio recibe "Este descuento es de otro comercio": el front mapea CUALQUIER 403 a 'wrong-merchant' y se come el 403 de `requireMerchantActive`
- **Sector:** R2 · lentes 6 Estados no manejados + 3 contrato FE/BE + 14 errores tragados
- **Síntoma:** El comercio quedó suspendido (o canceló) pero su access token todavía vive. En el mostrador escanea el QR del vecino y la pantalla le dice, en rojo: "Este descuento es de otro comercio · El código es válido pero corresponde a otro local adherido. Pedile al cliente uno de los tuyos." El cajero discute con el cliente por un cupón que ES suyo, y el comercio nunca se entera de que la cuenta está suspendida. El panel además no muestra ningún banner (el sticky sólo existe para `pending_payment`).
- **Repro:** 1) Comercio logueado y operando. 2) El owner lo suspende (o el reconciliador de expiry.service lo baja a 'suspendido'). 3) Dentro de la hora de vida del access token, el comercio abre /admin/validar y valida un código propio y válido. 4) `requireMerchantActive` responde 403 `{error:'merchant inactivo', estado:'suspendido', mensaje:'Tu cuenta está suspendida. Contactá soporte.'}`. 5) `useApiValidateByCode` no es un 409 → `reason = String(403)`. 6) `toLegacyResult` mapea '403' → 'wrong-merchant'. 7) `errorCopy('wrong-merchant')` imprime el mensaje equivocado y descarta por completo el `mensaje` que mandó el backend.
- **Causa raíz:** ¿Por qué se muestra el mensaje equivocado? Porque el front traduce el status HTTP a un motivo de negocio. ¿Por qué esa traducción es incorrecta? Porque 403 tiene DOS emisores en esta ruta: el guard de propiedad del cupón ('cupón de otro comercio') y el guard de estado del comercio ('merchant inactivo'), y el front asume que sólo existe el primero. ¿Por qué asume eso? Porque el mapeo se escribió cuando el único 403 posible era el de propiedad; `requireMerchantActive` se sumó después a /validate y /confirm sin actualizar el traductor. ¿Por qué nadie lo detectó? Porque el backend SÍ manda un discriminador (`error:'merchant inactivo'` y `estado`) pero el front sólo mira `err.status` y `payload.status` — nunca `payload.error` ni `payload.estado`. Causa terminal: el contrato de error del backend se enriqueció (campo `estado`/`mensaje`) y el consumidor sigue leyendo únicamente el código HTTP, así que un estado terminal del comercio se disfraza de error del cliente.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:333`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:334`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:378`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/middleware/auth.ts:140`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/middleware/auth.ts:145`
- **Fix:** En `toLegacyResult` (AdminValidarPage.tsx) discriminar el 403 por payload: si `payload.error === 'merchant inactivo'` (o viene `payload.estado`), emitir un reason nuevo ('merchant-inactivo') con copy que use `payload.mensaje` del backend, y sólo mapear a 'wrong-merchant' el 403 con `error: 'cupón de otro comercio'`. Para eso `useApiValidateByCode` debe propagar `err.payload.error`/`err.payload.estado`, no sólo `err.status`. Complementario: extender el banner sticky de MerchantShell a `suspendido

### [R2-09] El panel muestra "Plan Estándar · $50.000/mes · Estado: Activa" mientras la landing prometió "Gratis · después $30.000/mes": tres fuentes distintas para el mismo precio y ninguna es el precio real del tenant si no está seteado
- **Sector:** R2 · lentes 8 plata + 3 contrato entre apps
- **Síntoma:** El comerciante se sumó leyendo en la landing "Gratis y sin tarjeta hasta que se completen los primeros 50 comercios... después $30.000/mes, congelado de por vida". Abre Mi Comercio y lee "Plan: Estándar · $50.000 / mes — Estado: Activa". Cree que le están cobrando $50.000 desde ya, o que le cambiaron el precio.
- **Repro:** 1) Tenant SIN `precioMensual` cargado (el campo del modelo App no tiene default). 2) GET /tenant/:slug/config devuelve `precioMensual: undefined`. 3) La landing cae a `PRECIO_MENSUAL = 30000`. 4) El panel cae a `?? 50000` en el mismo render. 5) El backend, si llegara a facturar, cae a `PLAN_AMOUNT_ARS ?? 50_000`. Tres números para el mismo concepto.
- **Causa raíz:** ¿Por qué difieren? Porque cada capa tiene su propio fallback hardcodeado. ¿Por qué hay fallbacks distintos? Porque el precio se bajó de 50.000 a 30.000 en el paquete de la landing pero el literal del panel y el del billing quedaron en el valor viejo (el espejo de la PWA, apps/web/src/lib/launch.ts, sólo replica TOTAL_CUPOS, no el precio). ¿Por qué no se detectó? Porque la convención declarada del repo es duplicar constantes chicas con un comentario apuntando al canónico, y el `?? 50000` de AdminComercioPage ni siquiera está marcado como espejo de nada. ¿Por qué el fallback se usa? Porque `App.precioMensual` es opcional y sin default en el schema. Causa terminal: no existe una fuente única del precio; hay un literal por app, y el campo del tenant que debería ganar es opcional.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminComercioPage.tsx:1388`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/src/lib/launch.ts:19`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:15`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/App.ts:86`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/src/sections/Pricing.tsx:30`
- **Fix:** Unificar: hacer `App.precioMensual` obligatorio (o con default) y que el panel muestre exactamente `tenant.config.precioMensual`, sin fallback numérico — si no está, mostrar '—' en vez de inventar. En paralelo, alinear `PLAN_AMOUNT_ARS` y el literal del panel al valor canónico y agregar un test de coherencia. Y ajustar el card de suscripción al modelo actual (gratis por hito): mostrar 'Programa de lanzamiento · gratis' cuando el comercio no tiene Subscription.

### [R3-03] Reactivar un comercio desde el owner se deshace solo a los <=10 minutos, y la UI te sigue mostrando "Activo"
- **Sector:** R3 · lentes 12 Integridad referencial / 9 Carreras / 6 Estados
- **Síntoma:** El comercio te llama: "me suspendieron". Entrás a Comercios, tocás Reactivar, la fila cambia a Activo, la Auditoría registra "Reactivó comercio". Diez minutos después el comercio te vuelve a llamar porque sigue sin poder operar. Vos mirás el panel y dice Activo (nunca se refrescó). El barrido de expiración lo volvió a suspender en silencio, sin dejar rastro en la auditoría.
- **Repro:** Comercio con Subscription en `cancelled|paused|rejected`, `nextBillingAt` en el pasado y `freeTrialUntil` vencido/ausente. 1) PATCH /api/v1/owner/merchants/<id> {estado:'activo'} → 200, merchant.estado='activo'. 2) Esperar el tick de `runExpirySweep` (<=10 min; corre también al boot). 3) GET /api/v1/owner/merchants?estado=suspendido → el comercio está ahí de nuevo. 4) GET /api/v1/owner/audit → hay un `merchant.reactivate` y NINGUNA entrada de la re-suspensión.
- **Causa raíz:** 1) ¿Por qué se revierte? Porque `runExpirySweep` hace `Merchant.updateMany({estado:'activo', ...}, {estado:'suspendido'})` cada 10 min. 2) ¿Por qué pisa una decisión humana? Porque el reconciliador sólo mira el estado DERIVADO (suscripción vencida) y no distingue "activo porque paga" de "activo porque el owner lo decidió". 3) ¿Por qué no hay forma de distinguirlo? Porque `Merchant.estado` es un enum plano sin campo de override manual ni `estadoMotivo`/`suspendidoPor`. 4) ¿Por qué el owner no se entera? Porque `MerchantsPage.toggleEstado` hace update optimista local sin refetch, y el sweep no escribe en OwnerAuditLog. 5) CAUSA TERMINAL: se agregó un reconciliador automático (fix del barrido anterior sobre suscripciones vencidas) sobre un modelo de estado que ya tenía un escritor manual (el owner), sin definir quién gana ni dejar rastro de la escritura automática.
- **Evidencia:** `apps/api/src/routes/owner.ts:871`, `apps/api/src/services/expiry.service.ts:56`, `apps/api/src/services/expiry.service.ts:74`, `apps/owner/src/pages/MerchantsPage.tsx:87`
- **Fix:** Tres cosas: (1) Modelar el override: agregar `estadoManualAt`/`estadoManualPor` en Merchant y excluir del `updateMany` del sweep a los que tienen override reciente. (2) Auditar la escritura automática: emitir un OwnerAuditLog `system.merchant.suspend` por cada comercio bajado por el sweep, con el motivo (subId + nextBillingAt) — hoy la Auditoría miente por omisión. (3) En `MerchantsPage.toggleEstado`, refetchear la página en vez de update optimista, y avisar en el propio botón cuando la suscripc

### [D1-01] Borrar o pausar un cupón deja las activaciones vivas: la billetera del vecino queda en un esqueleto que pulsa para siempre y al tocarlo lo patea al inicio
- **Sector:** D1 · lentes 12 Integridad referencial + 6 Estados no manejados (skeleton infinito)
- **Síntoma:** El comercio borra (o simplemente pausa) un descuento. Los vecinos que ya lo habían activado abren "Mis cupones" y donde estaba su cupón ven una barra gris que late para siempre — no es que tarda: nunca resuelve. Si tocan la tarjeta, la app los devuelve al inicio sin decir una palabra. El código de 6 dígitos sigue existiendo en la base, en estado 'activo'.
- **Repro:** 1) Comercio publica un cupón. 2) Vecino lo activa (POST /api/v1/activations) → Activation status='activo'. 3) Comercio hace DELETE /api/v1/coupons/:id (o PATCH con estado='pausado'). 4) Vecino abre /#/mis-cupones: GET /api/v1/coupons ya no devuelve ese cupón (filtra estado 'activo' + merchant activo + vigencia futura), así que couponMap no lo tiene. 5) La tarjeta renderiza el div animate-pulse indefinidamente. 6) Vecino navega a /#/activacion/:id → coupon undefined, sin loading y sin error de red → <Navigate to="/" replace />.
- **Causa raíz:** 1) ¿Por qué ve un esqueleto? Porque getCoupon(a.couponId) devuelve undefined. 2) ¿Por qué undefined? Porque el mapa de cupones de la billetera se arma SOLO con useApiCoupons() = GET /coupons, que es el catálogo público y activo. 3) ¿Por qué se arma con el catálogo, si GET /activations/me ya devuelve el cupón y el comercio embebidos (serializeActivation) y syncActivations los guarda como couponTitulo/merchantNombre? Porque esa pantalla nunca se migró a leer el dato embebido. 4) ¿Por qué no se detectó? Porque el fix del bug #3 (snapshots) se aplicó a CanjeadosPage —que sí hace `c?.titulo ?? r.couponTitulo` y además distingue loading de vacío— y NO a MisCuponesPage ni a CuponActivoPage: el arreglo quedó a medias. 5) Causa terminal: el snapshot en Activation se escribe SOLO al canjear ("Solo se setean al canjear", Activation.ts), así que una activación viva no tiene de dónde caerse, y el borrado de cupón es un hard-delete sin cascada ni tombstone: no queda absolutamente nada en el sistema que pueda resolver ese couponId.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:262`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/MisCuponesPage.tsx:214`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/CuponActivoPage.tsx:158`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Activation.ts:32`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/CanjeadosPage.tsx:168`
- **Fix:** Tres capas: (a) en MisCuponesPage y CuponActivoPage resolver con `c?.titulo ?? a.couponTitulo` / `m?.nombre ?? a.merchantNombre` (ya están en el store) y, cuando no haya nada, mostrar un estado EXPLÍCITO ("Este descuento ya no está disponible") con acción de cancelar la activación, nunca un skeleton ni un redirect mudo; (b) en el backend, escribir couponTituloSnapshot/merchantNombreSnapshot al CREAR la activación, no sólo al canjear; (c) convertir DELETE /coupons/:id en soft-delete (o, como míni

### [D1-03] Cancelar una suscripción sólo la marca cancelada en nuestra base: Mercado Pago sigue debitando y el próximo webhook la resucita a 'authorized'
- **Sector:** D1 · lentes 8 Plata + 13 Degradación + 12 Integridad entre nuestro modelo y el del proveedor
- **Síntoma:** El comercio toca "cancelar" y la app le responde textualmente "Tu suscripción se cancela el <fecha>". No se cancela nada en Mercado Pago: el preapproval sigue vivo y le vuelve a debitar el mes siguiente. Peor: cuando MP avisa por webhook que la suscripción sigue autorizada, nuestro registro vuelve solo a 'authorized' y reaparece en el MRR del owner como si nunca se hubiera cancelado.
- **Repro:** 1) Comercio con Subscription status='authorized' y preapprovalId real. 2) POST /api/v1/billing/cancel → sub.status='cancelled', respuesta 200 con el mensaje de cancelación. Nadie llama a la API de MP (mp.service sólo exporta createPreapproval y getPreapproval). 3) Llega el próximo ciclo: MP cobra y dispara el webhook type='preapproval'. 4) POST /api/v1/billing/webhook → getPreapproval(dataId) devuelve status 'authorized' → `sub.status = mapMpStatus(detail.status)` pisa el 'cancelled' con 'authorized'. 5) GET /owner/stats vuelve a contar esa suscripción en el MRR; el comercio sigue pagando.
- **Causa raíz:** 1) ¿Por qué le siguen cobrando? Porque la cancelación nunca viaja a MP. 2) ¿Por qué no viaja? Porque no existe una función de cancelación en el service de MP: sólo hay crear y consultar. 3) ¿Por qué se aceptó así? Porque MP quedó bypasseado en el alta (3 meses gratis) y la ruta de cancelación se escribió pensando en el estado local, no en el contrato con el proveedor — el mismo agujero está documentado y asumido para el refund (admin.ts lo declara manual) pero para la cancelación ni siquiera está documentado. 4) ¿Por qué se resucita? Porque el webhook trata el estado de MP como fuente de verdad absoluta y lo escribe sin comparar contra una intención local de cancelación. 5) Causa terminal: hay dos escritores del mismo campo (nuestro panel y el webhook) sin ninguna noción de precedencia ni de "cancelación solicitada"; el modelo Subscription no tiene un `cancelRequestedAt` que el webhook deba respetar.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:243`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:104`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/mp.service.ts:33`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:1058`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:262`
- **Fix:** Implementar mp.service.cancelPreapproval(preapprovalId) (PUT /preapproval/:id {status:'cancelled'}) y llamarlo desde POST /billing/cancel y desde PATCH /owner/subscriptions/:id, fallando ruidoso (5xx + estado 'cancelación pendiente') si MP no responde en vez de prometer una cancelación que no ocurrió. En el modelo, agregar `cancelRequestedAt` y hacer que el webhook NO promueva a 'authorized' una suscripción con cancelación solicitada sin registrar la discrepancia.

### [D1-04] El vecino ejerce su derecho de borrado y quedan sus notas privadas con datos personales en el comercio; además el canje anonimizado viola el propio esquema (userId es required)
- **Sector:** D1 · lentes 12 Integridad referencial + privacidad (habeas data)
- **Síntoma:** El vecino borra su cuenta y la app le responde "Tu cuenta y datos personales fueron eliminados". Las notas que el comercio escribió sobre él —texto libre de hasta 1000 caracteres, del tipo "alergia al gluten", "viene con los hijos"— siguen enteras en la base, apuntando a un usuario que ya no existe y sin forma de llegar a ellas desde ninguna pantalla: basura con datos personales adentro, imposible de auditar y de borrar.
- **Repro:** 1) Vecino canjea en un comercio; el comercio le crea una nota (POST /redemptions/clientes/notes) → CustomerNote{appId, merchantId, userId, text}. 2) Vecino hace DELETE /api/v1/auth/me. 3) El handler anonimiza Redemption ($unset userId), borra Activation, revoca refresh tokens y borra el User. NUNCA toca CustomerNote. 4) La nota queda con userId apuntando a un doc inexistente. 5) Como el canje quedó sin userId, GET /redemptions/clientes ya no lista a esa persona → nadie puede volver a abrir esas notas ni pedir su borrado. 6) Efecto colateral: los Redemption resultantes quedan sin `userId`, campo declarado `required: true` en el esquema — updateMany no corre validators, así que la colección queda con documentos que el propio modelo considera inválidos.
- **Causa raíz:** 1) ¿Por qué queda PII? Porque el borrado enumera a mano tres colecciones (Redemption, Activation, RefreshToken) y CustomerNote no está en la lista. 2) ¿Por qué no está? Porque CustomerNote se agregó después, como feature del comercio, y nadie volvió sobre el handler de habeas data. 3) ¿Por qué nadie volvió? Porque no hay un punto único que declare "todo lo que referencia a User" — cada modelo declara su ref y el borrado la replica de memoria. 4) ¿Por qué el $unset rompe el esquema? Porque se eligió "anonimizar" un campo marcado required en vez de despuntar la referencia con un valor centinela o mover el canje a un modelo agregado. 5) Causa terminal: no existe una operación de borrado de sujeto (cascada declarativa o barrido por refs); hay un handler ad-hoc que se desincroniza cada vez que se agrega un modelo con `ref: 'User'`.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/user-auth.ts:435`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/user-auth.ts:447`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/CustomerNote.ts:12`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Redemption.ts:16`
- **Fix:** Agregar CustomerNote.deleteMany({appId, userId}) al handler de DELETE /auth/me (son notas privadas del comercio SOBRE el vecino: caen bajo su derecho de supresión), y en el esquema de Redemption dejar userId opcional documentando explícitamente el estado 'anonimizado', o reemplazar el $unset por un `userDeletedAt` + userId centinela. A futuro: una función única deleteUserData(appId, userId) que recorra una lista declarada de refs a User, con un test que falle si aparece un modelo nuevo con ref:'

### [D2-01] Cancelar la suscripción NO la cancela en MercadoPago: le seguimos cobrando al comercio y el webhook la resucita
- **Sector:** D2 · lentes 8 Plata / 13 Degradación / 9 Idempotencia
- **Síntoma:** El comercio aprieta "Cancelar" en Mi Comercio, la app le dice "Tu suscripción fue cancelada y vamos a procesar el reembolso completo", el MRR del owner baja... y MercadoPago le sigue debitando la tarjeta todos los meses. Peor: cuando MP manda el webhook del siguiente cobro, la suscripción vuelve sola a 'authorized' y el comercio reaparece en el MRR como si nunca hubiera cancelado.
- **Repro:** 1) Comercio con Subscription.status='authorized' y preapprovalId real de MP. 2) POST /api/v1/billing/cancel. 3) La respuesta dice 'cancelada'; en Mongo sub.status='cancelled'. 4) Grepear mp.service.ts: no existe ninguna función que llame a PUT https://api.mercadopago.com/preapproval/:id con status 'cancelled' → el preapproval sigue vivo en MP. 5) Al siguiente cobro MP postea a /billing/webhook con type='preapproval'; getPreapproval devuelve status 'authorized' → billing.ts:105 hace sub.status = mapMpStatus('authorized') → vuelve a 'authorized' y suma otra vez al MRR.
- **Causa raíz:** ¿Por qué se le sigue cobrando? Porque el preapproval de MP nunca se cancela. ¿Por qué? Porque mp.service.ts sólo expone createPreapproval y getPreapproval — no hay cancelPreapproval. ¿Por qué? Porque la integración se construyó sólo para el camino feliz (alta → webhook → activar) y el alta quedó bypasseada por los 3 meses gratis, así que la cancelación nunca se ejercitó de punta a punta. ¿Por qué nadie lo detectó? Porque nuestra DB es la única fuente de verdad que miramos (MRR, panel, mails) y ahí sí figura 'cancelled' — el estado real vive en MP y jamás se compara. Causa terminal: el estado de la suscripción es bidireccional (MP→nosotros por webhook) pero nosotros sólo escribimos de un lado; no hay ninguna escritura nuestra hacia MP, y el webhook pisa incondicionalmente nuestro estado con el de MP.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:243`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:254`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/mp.service.ts:85`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:105`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:111`
- **Fix:** Agregar `cancelPreapproval(preapprovalId)` en mp.service.ts (PUT /preapproval/:id {status:'cancelled'}) y llamarla ANTES de persistir el estado local en los tres puntos (billing /cancel, owner PATCH /subscriptions/:id, admin /refund). Si MP falla, NO devolver 'cancelada': dejar la sub en un estado 'cancel_pending' y reintentar. Además, en el webhook no permitir la transición cancelled→authorized sin un pago nuevo: si sub.status==='cancelled' y detail.status==='authorized', loguear alerta y no re

### [D2-02] POST /billing/preapproval no es idempotente: dos clics = dos suscripciones = doble cobro y MRR inflado
- **Sector:** D2 · lentes 9 Carreras/idempotencia / 8 Plata
- **Síntoma:** El comercio entra dos veces a "Activar pago" (o hace doble tap, o vuelve atrás y reintenta) y quedan DOS Subscription para el mismo comercio, cada una con su preapproval real en MercadoPago. Si completa el checkout de ambas, le cobran dos veces por mes. En el panel del owner el MRR se cuenta duplicado, y "Cancelar" sólo cancela una.
- **Repro:** 1) Login comercio. 2) POST /api/v1/billing/preapproval dos veces. 3) `Subscription.countDocuments({merchantId})` = 2, ambas con externalReference distinto y preapprovalId distinto. 4) Si ambos webhooks llegan como 'authorized', computeMrr suma amountARS dos veces. 5) En /owner/stats se ve la inconsistencia en la misma pantalla: `resumen.suscripcionesActivas` (cuenta suscripciones) dice 2 y `embudo.pagando` (cuenta merchantId distintos) dice 1. 6) POST /billing/cancel hace findOne(...).sort({createdAt:-1}) → sólo cancela la más nueva; la vieja sigue authorized y sigue cobrando.
- **Causa raíz:** ¿Por qué hay dos suscripciones? Porque el handler hace Subscription.create() sin mirar si ya hay una. ¿Por qué no mira? Porque no hay índice único {appId, merchantId} filtrado por status ∈ {pending, authorized}, y el modelo sólo indexa merchantId sin unique. ¿Por qué no se puso? Porque el diseño asume 'una sub por comercio' pero esa invariante nunca se escribió ni en el schema ni en el código. ¿Por qué duele tanto? Porque toda la lectura downstream (billing /me, /cancel) usa `findOne().sort({createdAt:-1})` — asume implícitamente 'la última es LA suscripción' — mientras que el MRR usa `aggregate` sobre TODAS. Causa terminal: dos capas leen la misma entidad con supuestos de cardinalidad opuestos (una vs muchas) y ninguna los enforcea.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:157`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Subscription.ts:7`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:226`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:142`
- **Fix:** Antes del create, buscar una sub del mismo comercio con status ∈ {'pending','authorized'} y devolver su initPoint en vez de crear otra. Reforzar con índice parcial único {appId:1, merchantId:1} + partialFilterExpression {status:{$in:['pending','authorized']}}. Y unificar la lectura: un helper `getActiveSubscription(appId, merchantId)` usado por /me, /cancel, /refund y computeMrr.

### [D2-04] La landing promete $30.000/mes "congelado de por vida" y el backend cobra $50.000: tres fallbacks distintos para el mismo precio
- **Sector:** D2 · lentes 3 Contrato FE/BE / 8 Plata / 1 Costura
- **Síntoma:** En una ciudad a la que el owner no le cargó precioMensual, el comercio lee en la landing "$30.000/mes congelado de por vida", entra al panel y ahí dice "Plan Estándar · $50.000/mes", y cuando activa el pago MercadoPago le cobra $50.000. Tres números para el mismo plan, y el que se cobra es más caro que el prometido.
- **Repro:** 1) Crear/tener una App sin precioMensual (el campo es .optional() en el schema del owner y no tiene default en el modelo — todas las apps sembradas antes del multi-país lo tienen vacío). 2) Abrir <ciudad>.micuidad.com/comercios → Pricing/SocialProof/FAQ muestran priceLabel(config) = PRECIO_MENSUAL = 30000. 3) Registrarse y abrir Mi Comercio → Row 'Plan' = formatMoney(undefined ?? 50000) = $50.000. 4) POST /billing/preapproval → amount = PLAN_AMOUNT_ARS = 50_000 → eso es lo que viaja a MP en auto_recurring.transaction_amount.
- **Causa raíz:** ¿Por qué hay tres números? Porque el precio fallback está hardcodeado tres veces en tres paquetes. ¿Por qué no está una sola vez? Porque el repo tiene la convención explícita de NO importar @misanpedro/shared desde el front y duplicar constantes 'con un comentario al canónico'. ¿Por qué falló la convención? Porque apps/web/src/lib/launch.ts dice ser 'ESPEJO de apps/landing/src/lib/launch.ts' pero sólo copió TOTAL_CUPOS y no PRECIO_MENSUAL; y AdminComercioPage ni siquiera usa launch.ts: escribe 50000 inline. ¿Por qué nadie lo cazó? Porque el bug sólo se materializa cuando precioMensual está vacío, y en la ciudad de prueba (sanpedro) está cargado, así que los tres caminos coinciden. Causa terminal: el precio de lista es una regla de negocio con valor por defecto y se modeló como constante duplicada por capa en vez de como dato del tenant con default en el modelo.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/src/lib/launch.ts:19`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/src/lib/tenant.ts:212`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminComercioPage.tsx:1388`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:155`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/App.ts:86`
- **Fix:** Poner un default real en App.precioMensual (o hacerlo required en createAppSchema, que ya lo exige el form del owner) y eliminar los tres fallbacks: la landing y el panel deben mostrar '—' o esconder el precio si el tenant no lo trae, nunca inventar un número. Si se quiere conservar un fallback, que exista UNO SOLO en packages/shared y que el backend lo devuelva ya resuelto en /tenant/:slug/config.

### [D2-14] El link de referidos apunta siempre a la ciudad de APP_URL_FRONT: en cualquier otra ciudad el referido no se acredita y la semana gratis prometida nunca llega
- **Sector:** D2 · lentes 2 Multi-tenant / 8 Plata / 14 Errores tragados
- **Síntoma:** Un comercio de Nariño comparte su link "Recomendá y ganá" por WhatsApp. Su colega hace clic, se registra... en San Pedro. El referido nunca se confirma, el referidor nunca recibe su semana gratis, y en el panel de referidos figura 0 pendientes: no hay error, no hay aviso, no pasó nada.
- **Repro:** 1) Tenant B (subdomain 'minarino') distinto del que apunta APP_URL_FRONT. 2) Comercio de B: GET /api/v1/referrals/me → link = `${env.APP_URL_FRONT}/#/admin/registro?ref=ABC123` (host de la ciudad principal). 3) El referido abre ese link y se registra: el X-Tenant-Slug resuelve al tenant de APP_URL_FRONT, no a B. 4) merchant-auth.ts:131 hace `Merchant.findOne({ appId, referralCode: refCode })` con el appId equivocado → no encuentra nada → el `if (referrer && ...)` no entra → NO se crea ningún Referral, ni siquiera 'rejected'. 5) El referidor ve 0 pendientes y 0 confirmados para siempre.
- **Causa raíz:** ¿Por qué el link es de otra ciudad? Porque usa env.APP_URL_FRONT en vez de tenantFrontUrl(tenant). ¿Por qué? Porque el fix tenant-aware de URLs se aplicó a billing (back_url), merchant-auth (welcome) y owner (support-session) pero se salteó referrals. ¿Por qué se salteó? Porque referrals.ts ni siquiera importa tenantFrontUrl — es el único consumidor de APP_URL_FRONT que quedó en las rutas. ¿Por qué falla en silencio? Porque el lookup del código de referido está scopeado por appId (correcto para el aislamiento) y el código trata 'no encontrado' como 'no vino ref', sin log ni respuesta. Causa terminal: un fix transversal aplicado sitio por sitio sin barrido exhaustivo, combinado con un camino de error que no distingue 'sin ref' de 'ref inválido'.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/referrals.ts:47`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchant-auth.ts:131`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:175`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/lib/urls.ts:20`
- **Fix:** Usar `tenantFrontUrl(c.get('tenant'))` en referrals.ts:47 (el middleware tenantContext ya corre en esas rutas). Y en el signup, cuando venga `ref` y no matchee ningún Merchant del tenant, crear el Referral con status 'rejected' + rejectedReason 'code_not_found' y loguear, para que el agujero sea visible en vez de mudo.

### [R1-01] Todos los rate-limits (incluidos los 3 flujos de OTP) se saltean mandando un X-Forwarded-For inventado
- **Sector:** N1 · lentes Auth cruzada / carreras / degradación (lente 4+9+13)
- **Síntoma:** Un atacante puede pedir códigos OTP sin límite para el email de cualquier vecino, comercio u owner: le llena la casilla de códigos, quema la reputación del remitente SMTP y anula el freno de 5 mails/hora que se diseñó justo para eso. El vecino ve decenas de mails 'tu código es 483920' que él nunca pidió.
- **Repro:** 1) POST /api/v1/auth/request-otp con X-Tenant-Slug: sanpedro, body {email: victima@x.com} y header 'X-Forwarded-For: 1.2.3.4'. 2) Repetir 20 veces cambiando el último octeto del XFF (1.2.3.5, 1.2.3.6, …). 3) Las 20 devuelven 200 y mandan 20 mails; sin cambiar el header, a partir de la 6ª devolvería 429. Mismo truco en /merchant/auth/request-otp y /owner/auth/request-otp.
- **Causa raíz:** 1) ¿Por qué no frena? Porque cada request cae en un bucket distinto. 2) ¿Por qué? Porque la clave del bucket es `x-forwarded-for.split(',')[0]`, o sea el primer elemento del header. 3) ¿Por qué es atacable ese elemento? Porque el proxy de borde (Envoy/Railway) APPENDEA la IP real al final del XFF que ya venía; el primer elemento es el que puso el cliente. 4) ¿Por qué se eligió el primero? Porque es la convención 'IP del cliente original' cuando confiás en toda la cadena de proxies, y acá se confía en exactamente UNO. 5) CAUSA TERMINAL: `TRUST_PROXY` se leyó como 'confío en el header entero' en vez de 'confío en UN proxy, así que la IP real es la ÚLTIMA entrada'. El helper es único y compartido, así que el error se propaga a los 13 limitadores de la plataforma de una sola vez.
- **Evidencia:** `apps/api/src/middleware/security.ts:76`, `apps/api/src/middleware/security.ts:74`, `nixpacks.toml:55`, `apps/api/src/routes/user-auth.ts:33`
- **Fix:** En `clientKey`, con TRUST_PROXY=true tomar la ÚLTIMA entrada del XFF (`xff.split(',').pop()`), que es la que agrega el proxy de borde y el cliente no puede falsificar. Como el bucket es in-memory y por instancia, además conviene: (a) para los flujos de OTP, sumar un segundo bucket con clave = email normalizado (no falsificable, y es el recurso que se quiere proteger: la casilla de la víctima); (b) mover el estado a Mongo/Redis para que el límite valga cuando haya más de una réplica. Test de regr

### [R1-02] El panel Owner se autodeslogea solo cada hora: dos requests en paralelo rotan el mismo refresh y el backend interpreta 'robo de token' y revoca TODAS las sesiones
- **Sector:** N1 · lentes Carreras/idempotencia + auth (lente 9+4)
- **Síntoma:** El super-admin abre el panel, mira el dashboard y de golpe lo escupe al login. Tiene que volver a pedir un código al mail. Pasa sistemáticamente ~1h después de cada login, y en las pantallas que cargan dos cosas a la vez (Dashboard, Estadísticas).
- **Repro:** 1) Loguearse al Owner Panel con OTP. 2) Esperar a que venza el access (1h) — o borrar a mano `access` del store dejando `refresh`. 3) Entrar a /dashboard: dispara `Promise.all([owner.metrics(), owner.listApps()])`. 4) Ambas dan 401 → ambas llaman `tryRefresh()` con el MISMO `auth.refresh`. 5) La primera rota OK; la segunda llega con el token ya revocado → `rotateRefreshToken` detecta reuso y hace `updateMany({subjectId}, {revokedAt})`, que revoca también el token nuevo recién emitido. 6) Retorna null → 401 → `authActions.signOut()`. Sesión muerta.
- **Causa raíz:** 1) ¿Por qué lo desloguea? Porque el refresh nuevo queda revocado a los milisegundos de nacer. 2) ¿Por qué? Porque la defensa anti-robo `rotateRefreshToken` revoca toda la familia del subject cuando ve un token revocado reusado. 3) ¿Por qué se reusa un token revocado si nadie robó nada? Porque dos requests concurrentes leyeron el mismo refresh de localStorage antes de que ninguna lo rotara. 4) ¿Por qué no se deduplicó? Porque `tryRefresh()` del panel owner no tiene el candado de 'refresh en vuelo'. 5) CAUSA TERMINAL: el arreglo de esta misma carrera existe y está documentado con lujo de detalle en el cliente de la PWA (`refreshInFlight` en apps/web/src/lib/api.ts), pero nunca se portó al segundo cliente HTTP del monorepo. Hay dos clientes HTTP con la misma responsabilidad y sólo uno aprendió la lección; una revisión por tarea del Owner Panel no podía ver el fix que vive en otra app.
- **Evidencia:** `apps/owner/src/lib/api.ts:80`, `apps/api/src/services/jwt.service.ts:160`, `apps/owner/src/pages/DashboardPage.tsx:46`, `apps/web/src/lib/api.ts:69`
- **Fix:** Portar el candado `refreshInFlight` de apps/web/src/lib/api.ts a apps/owner/src/lib/api.ts: una sola promesa de refresh por vez, las demás esperan esa misma. Complementario en backend: dar una ventana de gracia al reuso (aceptar el token viejo durante N segundos después de rotarlo, devolviendo el token nuevo ya emitido) para que un reintento por red perdida no dispare la defensa anti-robo. Test: dos `api()` en paralelo con el access vencido deben terminar ambas OK y dejar UNA sola rotación en Re

### [R1-03] Cancelar la suscripción no la cancela en Mercado Pago: sólo cambia el estado local, y el próximo webhook la resucita
- **Sector:** N1 · lentes Plata + degradación de integraciones (lente 8+13)
- **Síntoma:** El comercio aprieta 'Cancelar suscripción', la app le dice que quedó cancelada (o que le van a devolver la plata), y Mercado Pago le sigue debitando $50.000 todos los meses. Peor: al mes siguiente el webhook de MP vuelve a poner la suscripción en 'authorized', así que ni siquiera queda rastro de que la canceló.
- **Repro:** 1) Comercio con preapproval MP autorizado. 2) POST /api/v1/billing/cancel → responde 200 con 'Tu suscripción se cancela el <fecha>'. 3) Grepear el código: no hay ninguna llamada a MP en ese handler ni función de cancelación en mp.service.ts. 4) Cuando MP cobra el siguiente mes, dispara el webhook preapproval → billing.ts:104 hace `sub.status = mapMpStatus(detail.status)` = 'authorized' y pisa el 'cancelled'.
- **Causa raíz:** 1) ¿Por qué le siguen cobrando? Porque el preapproval sigue vivo en MP. 2) ¿Por qué? Porque /billing/cancel sólo hace `sub.status = 'cancelled'; await sub.save()`. 3) ¿Por qué no llama a MP? Porque mp.service.ts sólo expone `createPreapproval` y `getPreapproval` — no existe un `cancelPreapproval`. 4) ¿Por qué nadie lo notó? Porque MP quedó bypasseado en el alta (3 meses gratis sin tarjeta), así que hoy no hay comercios pagando y el camino no se ejercita. 5) CAUSA TERMINAL: la infra de cobro quedó a medio camino — se construyó el alta del preapproval pero no su baja, y el estado local se trata como fuente de verdad de algo que vive en un sistema externo. El webhook, que SÍ es la fuente de verdad, sobrescribe la decisión del usuario sin ninguna protección.
- **Evidencia:** `apps/api/src/routes/billing.ts:243`, `apps/api/src/services/mp.service.ts:33`, `apps/api/src/routes/billing.ts:104`, `apps/api/src/routes/billing.ts:262`
- **Fix:** Agregar `cancelPreapproval(id)` en mp.service.ts (PUT /preapproval/{id} con {status:'cancelled'}) y llamarlo ANTES de tocar el estado local; si MP falla, devolver error y NO decirle al comercio que quedó cancelada. Además, en el webhook, no reactivar una suscripción que fue cancelada localmente: guardar `cancelledLocallyAt` y exigir una acción explícita para revivirla. Test: cancelar → verificar que se llamó a MP; simular un webhook 'authorized' posterior → el status local debe seguir 'cancelled

### [R1-10] Borrar la cuenta deja vivas las notas privadas que el comercio escribió sobre el vecino (incluidas de salud) y la suscripción push atada a su userId
- **Sector:** N1 · lentes Integridad referencial + privacidad (lente 12)
- **Síntoma:** El vecino ejerce su derecho de supresión (Ley 25.326), la app le responde 'Tu cuenta y datos personales fueron eliminados', y en el panel del comercio siguen existiendo notas como 'Marta es vegana' o 'alergia al gluten' con su userId; además su navegador sigue suscripto a las notificaciones push de la ciudad con el userId del muerto pegado.
- **Repro:** 1) Vecino con al menos un canje en un comercio; el comercio le escribe una nota (POST /redemptions/clientes/:userId/notas → CustomerNote con userId + text). 2) El vecino habilita push (PushSubscription con userId). 3) DELETE /api/v1/auth/me. 4) El handler borra User, borra Activations, hace $unset del userId en Redemption y revoca RefreshTokens — no toca CustomerNote, ni PushSubscription, ni Otp. 5) En Mongo quedan CustomerNote.userId y PushSubscription.userId apuntando a un User inexistente, y el texto libre de las notas intacto.
- **Causa raíz:** 1) ¿Por qué sobreviven? Porque el handler de borrado enumera colecciones a mano. 2) ¿Por qué no las incluye? Porque se escribió cuando existían Activation y Redemption, y CustomerNote/PushSubscription se agregaron después sin volver sobre él. 3) ¿Por qué nadie lo detectó? Porque no hay ningún registro central de 'colecciones que referencian a User', ni un test que asegure que después de borrar no queda ninguna referencia. 4) ¿Por qué importa más que un dato huérfano cualquiera? Porque CustomerNote es texto libre escrito POR TERCEROS sobre la persona, y en la práctica guarda datos sensibles (los ejemplos del propio modelo son alergias y dieta = datos de salud). 5) CAUSA TERMINAL: el borrado es una lista blanca hecha a mano en un sistema donde las referencias a User crecen; el mensaje que se le devuelve al usuario ('fueron eliminados') es una promesa más fuerte que lo que el código hace, así que cada colección nueva convierte esa promesa en falsa sin que nada avise.
- **Evidencia:** `apps/api/src/routes/user-auth.ts:435`, `apps/api/src/routes/user-auth.ts:449`, `apps/api/src/models/CustomerNote.ts:13`, `apps/api/src/models/PushSubscription.ts:21`
- **Fix:** Centralizar el borrado en un servicio `purgeUser(appId, userId)` que recorra una lista declarada de colecciones con referencia a User (CustomerNote → delete, PushSubscription → delete o $unset userId, Otp → delete por email, Activation → delete, Redemption → $unset userId) y agregar un test que, después de purgar, corra un barrido genérico buscando ese ObjectId en todas las colecciones y falle si aparece. Incluir CustomerNote en /me/data-export. Mientras tanto, ajustar el copy para no prometer m

### [C1-01] Pausar/despausar un descuento borra las condiciones y resetea el límite de uso por persona (zod v4 `.partial()` NO saca los `.default()`)
- **Sector:** C1 · lentes 3 contrato FE/BE roto + 11 límites/stock + 8 plata (el límite es lo que evita que el vecino repita el descuento)
- **Síntoma:** El comercio configuró "hasta 3 usos por semana por persona" y escribió las condiciones ("no acumulable, de lunes a jueves"). Pausa el descuento un rato y lo vuelve a activar desde el menú de la tarjeta. Sin tocar nada más, el descuento vuelve con las condiciones VACÍAS y con el límite en "1 vez de por vida". Los vecinos que ya lo habían usado una vez pasan a ver "Ya usaste este cupón el máximo de veces permitido" y el cajero no entiende por qué.
- **Repro:** 1) Crear un cupón con condiciones='No acumulable' y usoMaxPorPersona=3, usoVentana='semana'.
2) En /admin/cupones abrir el menú de la tarjeta y tocar "Pausar" (manda exactamente `PATCH /coupons/:id {"estado":"pausado"}`).
3) Leer el doc en Mongo: condiciones quedó '', usoMaxPorPersona=1, usoVentana='devida'.
Verificado ejecutando el mismo shape con la zod instalada (4.4.3): `couponShape.partial().parse({estado:'pausado'})` → `{"condiciones":"","usoMaxPorPersona":1,"usoVentana":"devida","estado":"pausado"}`.
- **Causa raíz:** 1) ¿Por qué se resetea el límite? Porque `coupon.usoMaxPorPersona = data.usoMaxPorPersona` corre aunque el cliente no haya mandado el campo. 2) ¿Por qué corre? Porque el guard es `data.X !== undefined` y `parsed.data.usoMaxPorPersona` viene con valor. 3) ¿Por qué viene con valor si el body sólo trae `estado`? Porque `couponUpdateSchema` es `couponShape.partial()` y en zod v4 `.partial()` envuelve el campo pero NO desactiva el `.default(...)` que ya tenía. 4) ¿Por qué el mismo shape sirve para crear y para editar? Porque `couponShape` se reusa tal cual para create y update, y los defaults se escribieron pensando SOLO en el alta. 5) CAUSA TERMINAL: un shape con `.default()` compartido entre POST (donde el default es correcto) y PATCH (donde cualquier default es un pisotón silencioso), apoyado en el supuesto —falso en zod 4— de que `.partial()` neutraliza los defaults. El backend no distingue "no mandado" de "mandado vacío".
- **Evidencia:** `packages/shared/src/schemas.ts:281`, `packages/shared/src/schemas.ts:328`, `packages/shared/src/schemas.ts:338`, `packages/shared/src/schemas.ts:357`, `apps/api/src/routes/coupons.ts:242`
- **Fix:** Separar el shape de create del de update: `couponUpdateSchema` tiene que construirse sin defaults (ej. definir `couponUpdateShape` con los mismos campos pero `.optional()` sin `.default()`, o mapear el shape sacando los `ZodDefault` antes de `.partial()`). Mientras tanto, un test de regresión en packages/shared que afirme que `couponUpdateSchema.parse({estado:'pausado'})` es exactamente `{estado:'pausado'}`. Y en coupons.ts, para PATCH, decidir sobre `Object.prototype.hasOwnProperty.call(body, '

### [R1-01] El webhook de Mercado Pago responde 200 aunque NO haya podido procesar el pago: MP no reintenta nunca y el comercio queda en pending_payment para siempre
- **Sector:** C2 · lentes 13 Degradación · 14 Errores tragados · 8 Plata
- **Síntoma:** El comercio paga la suscripción en Mercado Pago, ve el comprobante de MP, vuelve al panel y sigue diciendo 'pendiente de pago'. No recibe el recibo. No hay ningún error en ningún lado. Cobramos y no habilitamos.
- **Repro:** 1) Comercio con MP_ACCESS_TOKEN configurado hace POST /billing/preapproval y paga. 2) MP dispara el webhook a POST /api/v1/billing/webhook con type='preapproval'. 3) En ese instante la API de MP devuelve cualquier cosa que no sea 2xx al GET /preapproval/:id (429 por rate limit, 500, 502, mantenimiento). 4) getPreapproval() devuelve null → el bloque `if (detail)` no entra → el handler llega igual a `return c.json({ ok: true })`. 5) MP recibe 200, marca la notificación como entregada y NO reintenta. 6) Subscription queda status='pending' y Merchant queda estado='pending_payment' de forma permanente: no hay job de reconciliación que vuelva a consultar MP.
- **Causa raíz:** 1) ¿Por qué el comercio queda sin activar? Porque sub.status nunca pasa a 'authorized'. 2) ¿Por qué? Porque el webhook no entró al bloque que lo actualiza. 3) ¿Por qué? Porque getPreapproval() devolvió null. 4) ¿Por qué devolvió null en vez de fallar? Porque el servicio traduce CUALQUIER respuesta no-OK de MP a `null` (`if (!res.ok) return null`), borrando la diferencia entre 'no existe' y 'MP está caído'. 5) CAUSA TERMINAL: el handler del webhook usa un único código de salida (200 ok:true) tanto para 'procesado' como para 'no pude procesar'. El ACK a MP no está acoplado al éxito del procesamiento, así que el único mecanismo de recuperación que existe (el reintento del proveedor) se desactiva justo en el caso en que hace falta. Nótese la asimetría perversa: si el fetch tira excepción de red, el error sube al onError → 500 → MP SÍ reintenta; si MP devuelve 500, tragamos y perdemos el evento para siempre.
- **Evidencia:** `apps/api/src/services/mp.service.ts:90`, `apps/api/src/routes/billing.ts:94`, `apps/api/src/routes/billing.ts:122`, `apps/api/src/routes/billing.ts:110`
- **Fix:** Separar 'no encontrado' de 'no pude consultar': que getPreapproval devuelva un discriminated union ({kind:'ok',data} | {kind:'notfound'} | {kind:'error',status}) o que directamente tire en 5xx/429. En el webhook, responder 5xx cuando no se pudo procesar (MP reintenta con backoff) y 200 sólo cuando la Subscription quedó efectivamente actualizada. Complementariamente: un job de reconciliación en expiry.service.ts que barra Subscriptions con status='pending' + preapprovalId != null y más de N minut

### [R1-02] El healthcheck miente: /api/v1/health devuelve 200 ok:true con Mongo desconectada, y el arranque nunca reintenta la conexión
- **Sector:** C2 · lentes 13 Degradación · 3 Contrato roto · 14 Errores tragados
- **Síntoma:** Railway muestra el deploy en verde, el dashboard dice que todo anda, y la app está 100% muerta: cada pantalla tarda 10 segundos y termina en 'Sin conexión'. Nadie se entera hasta que escribe un comercio.
- **Repro:** 1) Deployar con la MONGODB_URI mal, o que Atlas tenga un corte al momento del boot. 2) `connectDB()` rechaza → el catch de bootstrap loguea 'failed to connect DB; starting anyway' y sigue. 3) El server hace listen igual. 4) Railway pega a healthcheckPath '/api/v1/health' → 200 `{ok:true, db:'disconnected'}` → deploy PROMOVIDO a producción, restartPolicy ON_FAILURE nunca dispara porque el proceso no muere. 5) Cualquier request real pasa por tenantContext → `App.findOne(...)` → mongoose bufferea 10s (bufferTimeoutMS default) → 'buffering timed out' → app.onError → 500 `{error:'internal error'}`. 6) Como `connectingPromise = null` sólo se resetea en el evento 'disconnected' y NADIE vuelve a llamar connectDB(), el proceso queda sirviendo 500s hasta que un humano redeploya.
- **Causa raíz:** 1) ¿Por qué queda un deploy verde sirviendo 500s? Porque el healthcheck dijo que estaba sano. 2) ¿Por qué lo dijo? Porque /api/v1/health devuelve `ok:true` hardcodeado y el estado de la DB va sólo como dato informativo en el body, sin afectar el status HTTP. 3) ¿Por qué se usa ESE endpoint y no /health/ready, que sí devuelve 503? Porque railway.json apunta a /api/v1/health — existe el endpoint correcto y está desconectado del deploy. 4) ¿Por qué el proceso arranca sin DB en vez de morir? Porque bootstrap() envuelve connectDB en un try/catch que sólo loguea, decisión pensada para 'no tirar el server por un blip'. 5) CAUSA TERMINAL: se eligió 'degradar' en vez de 'fallar', pero no se implementó ninguna de las dos mitades que hacen viable degradar: ni la señal honesta hacia afuera (health que refleje el estado real), ni la recuperación hacia adentro (retry/backoff de reconexión). Queda el peor de los tres mundos: no muere, no se recupera y no avisa.
- **Evidencia:** `apps/api/src/index.ts:95`, `apps/api/src/index.ts:115`, `railway.json:9`, `apps/api/src/index.ts:402`, `apps/api/src/db/connection.ts:203`
- **Fix:** (a) railway.json → healthcheckPath: '/api/v1/health/ready'. (b) Que /api/v1/health devuelva `ok: dbReady` con status 503 cuando la DB no está (o dejarlo como endpoint de diagnóstico y no de gate). (c) Agregar reconexión con backoff: en el handler de 'disconnected' y en el catch de bootstrap, reintentar connectDB() con backoff exponencial, y mandar un captureException a Sentry al primer fallo. (d) Setear bufferCommands=false o un bufferTimeoutMS bajo para que con la DB caída los requests fallen r

### [R1-03] Fuga permanente de conexiones SSE: writeSSE de Hono nunca tira, así que el loop de heartbeat corre para siempre y unsubscribe() jamás se ejecuta
- **Sector:** C2 · lentes 13 Degradación · 14 Errores tragados · 9 Carreras
- **Síntoma:** El API se va poniendo lento y le crece la memoria hasta que Railway lo reinicia. Cada reinicio corta las campañas de WhatsApp en curso y deja a los comercios con la barra de progreso clavada. Nadie sabe por qué.
- **Repro:** 1) Un comercio abre el panel (monta useMerchantNotifications + useWhatsappStream) → 2 conexiones SSE. 2) Se le corta el 4G / cierra la tab / pasa por un proxy que corta a los 60s. 3) El cliente detecta el error y reconecta a los 5s (es.onerror → scheduleReconnect). 4) En el server, el handler de la conexión muerta sigue en `while (alive) { await stream.sleep(25_000); await stream.writeSSE(...) }`. `StreamingApi.write()` de Hono tiene un `try { await this.writer.write(input) } catch {}` — traga TODO error, así que writeSSE nunca rechaza, el catch que setea `alive = false` nunca corre, y `unsubscribe()` (whatsapp.ts:100 / notifications.ts:62) nunca se alcanza. 5) El listener queda para siempre en el Set de listeners y el timer de 25s también. 6) Con un cajero en wifi malo son decenas de conexiones fantasma por hora; con N comercios, cientos por día hasta el OOM.
- **Causa raíz:** 1) ¿Por qué crece la memoria? Porque los listeners de pub/sub y los timers de heartbeat nunca se liberan. 2) ¿Por qué no se liberan? Porque el `while (alive)` nunca termina. 3) ¿Por qué no termina? Porque `alive` sólo pasa a false dentro de un `catch` alrededor de `stream.writeSSE(...)`. 4) ¿Por qué ese catch nunca se ejecuta? Porque `StreamingApi.write()` de Hono captura y descarta silenciosamente el error de escritura sobre un socket cerrado (hono/dist/utils/stream.js:34-43), y `streamSSE` sólo registra el listener de abort `if (isOldBunVersion())` — corriendo en Node, nunca. 5) CAUSA TERMINAL: la detección de desconexión se apoyó en un contrato que la librería no ofrece ('escribir a un stream muerto tira'). No se usó la primitiva que Hono sí expone para esto (`stream.onAbort` / `c.req.raw.signal`), y no hay ninguna red de contención (ni TTL máximo por conexión, ni límite de conexiones por comercio) que acote el daño cuando la suposición falla.
- **Evidencia:** `apps/api/src/routes/whatsapp.ts:91`, `apps/api/src/routes/notifications.ts:53`, `node_modules/.pnpm/hono@4.12.18/node_modules/hono/dist/utils/stream.js:34`, `apps/api/src/services/whatsapp.service.ts:93`
- **Fix:** Usar `stream.onAbort(() => { alive = false; unsubscribe() })` dentro del callback de streamSSE (Hono expone onAbort en StreamingApi) y además registrar `c.req.raw.signal.addEventListener('abort', ...)` a mano, ya que streamSSE sólo lo hace en Bun viejo. Envolver todo el cuerpo en try/finally para que `unsubscribe()` corra siempre. Como red de contención: cortar la conexión con un TTL máximo (ej. 10 min, el EventSource reconecta solo) y loguear periódicamente el tamaño de `listeners` para tener l

### [R1-04] Con la sesión caída pero el usuario todavía en localStorage, la app le fabrica al vecino un código de 6 dígitos INVENTADO en el browser
- **Sector:** C2 · lentes 1 Costura demo/seed · 13 Degradación · 14 Errores tragados
- **Síntoma:** El vecino toca 'Activar', la app le muestra el código y el QR como siempre, va al mostrador, el cajero lo tipea y sale 'Descuento inválido'. Para el vecino la app funcionó perfecto. Para el comercio el vecino está intentando colarse.
- **Repro:** 1) Vecino logueado en la PWA. 2) Se le revoca el refresh (cierra sesión en todos lados desde otro celular, o el backend responde 401 al refresh por cualquier motivo). 3) Entra a 'Mis cupones' → se dispara syncMyActivations() → api.activations.mine() → 401 → doRefresh falla → `request()` hace `tokens.clear('user')` y dispara `msp:session-expired` con subject 'user'. 4) El listener global de ApiSync IGNORA subject 'user' (`if (detail?.subject !== 'merchant' ...) return`), y syncMyActivations tiene un `catch {}` vacío: NO se limpia el user del store local ni se muestra nada. 5) Ahora hay user en localStorage y access token = null. 6) El vecino entra a un cupón real y toca Activar: `if (!userToken || !looksLikeMongoId)` toma la rama LOCAL → `activationActions.activate()` genera `Math.floor(100_000 + Math.random()*900_000)` y navega a /activacion/:id. 7) CuponActivoPage renderiza ese código como cualquier otro (no distingue origen) y ni siquiera arranca el polling (lo saltea justo porque no hay token). 8) El cajero valida contra POST /redemptions/validate: ese código no existe en Mongo.
- **Causa raíz:** 1) ¿Por qué el vecino tiene un código que no existe? Porque el front lo generó localmente. 2) ¿Por qué lo generó? Porque `handleActivate` cae a la rama local cuando no hay access token. 3) ¿Por qué esa rama existe para un cupón real? Porque el guard mezcla dos condiciones distintas con un OR: `!looksLikeMongoId` (dato demo → local está bien) y `!userToken` (sesión rota → local está MAL). 4) ¿Por qué hay un vecino sin token pero con user local? Porque `tokens.clear()` y el store de usuario son dos fuentes de verdad separadas, y el único puente (el evento msp:session-expired) sólo está cableado para el sujeto 'merchant'. 5) CAUSA TERMINAL: sobrevive un modo demo/offline que fabrica identificadores del dominio (códigos de canje) en el cliente, y su condición de activación se solapa con un estado de error real. En vez de degradar a 'no puedo activarte esto ahora', degrada a 'te miento un código'. El fallback demo nunca debería poder producir un artefacto que el backend tiene que reconocer.
- **Evidencia:** `apps/web/src/pages/CuponDetailPage.tsx:109`, `apps/web/src/lib/stores.ts:184`, `apps/web/src/lib/stores.ts:270`, `apps/web/src/lib/api.ts:145`, `apps/web/src/components/ApiSync.tsx:38`
- **Fix:** Cortar el OR: la rama local sólo puede correr cuando el cupón NO es real (`!looksLikeMongoId`) y NUNCA por falta de token. Si hay un cupón de Mongo y no hay access token, mandar al login/recuperación de cuenta ('Necesitamos que vuelvas a entrar para activar tu cupón'), no fabricar un código. Además, hacer que `tokens.clear('user')` y el store de usuario sean una sola verdad: que ApiSync escuche también subject 'user' y ejecute `userActions.signOut()`, o directamente llamar signOut desde `tokens.

### [R1-05] Campaña de WhatsApp interrumpida por un deploy queda registrada como campaña 'enviada' con conteo parcial, consume 1 de los 4 cupos del mes, y deja la UI del comercio en 'Enviando…' para siempre
- **Sector:** C2 · lentes 13 Degradación · 6 Estados no manejados · 12 Integridad referencial
- **Síntoma:** El comercio manda una campaña a 500 vecinos, la barra queda clavada en 'Enviando… 120/500' y no se va nunca (ni recargando arranca de nuevo: el botón de enviar desaparece). En el historial figura una campaña con '120 enviados' como si hubiera terminado bien. Perdió un envío de los 4 del mes y 380 vecinos nunca recibieron nada.
- **Repro:** 1) Comercio con sesión de WA conectada dispara POST /wa/campaign con 500 destinatarios → 202 aceptado. El envío dura ~29 min (2-5s por mensaje). 2) A los 10 minutos Railway redeploya (o el proceso se reinicia por el leak de R1-03). 3) `campaignsInFlight` vive sólo en memoria y `shutdown()` no lo drena — `awaitCampaign()` está exportado 'para tests o un shutdown ordenado' pero NO se llama desde ningún lado del código. El proceso hace process.exit(0) con la campaña a mitad. 4) Quedan 120 documentos WaSend con ese campaignId. Como WaSend no guarda ni el total ni el estado de la campaña, GET /wa/campaigns la reconstruye como `{sentCount:120, failedCount:0}` — indistinguible de una campaña completa de 120. 5) `campaignsThisMonth()` cuenta campaignIds distintos: el cupo mensual se consumió igual. 6) En el front, el respaldo por timeout refetchea el historial, pero como `acumulado (120) < phase.total (500)` nunca cierra la fase: sólo actualiza la barra y vuelve a esperar, para siempre. 7) Con el botón 'Montar campaña' condicionado a `phase.kind === 'idle'`, el comercio no puede ni reintentar sin recargar.
- **Causa raíz:** 1) ¿Por qué el comercio ve una campaña 'enviada' con 120 de 500? Porque el historial se deriva de los mensajes efectivamente intentados. 2) ¿Por qué no se nota que faltan 380? Porque no existe ningún registro de la INTENCIÓN (cuántos eran, cuándo empezó, en qué estado terminó): sólo hay hechos sueltos en WaSend. 3) ¿Por qué no existe? Porque la campaña se modeló como una promesa en un Map en memoria (`campaignsInFlight`) y no como una entidad persistida. 4) ¿Por qué eso rompe en un reinicio? Porque el proceso es efímero (Railway redeploya) y `shutdown()` no drena las promesas en vuelo pese a que el helper para hacerlo ya existe y está sin cablear. 5) CAUSA TERMINAL: se convirtió un trabajo largo (29 minutos) en async con 202 —correcto— pero sin darle durabilidad ni estado terminal. El único observador del progreso es un stream in-memory sin replay, y la única fuente de reconstrucción es incompleta por diseño. La UI, obligada a inferir el final desde datos que nunca alcanzan, se queda esperando un evento que no va a llegar.
- **Evidencia:** `apps/api/src/routes/whatsapp.ts:25`, `apps/api/src/routes/whatsapp.ts:28`, `apps/api/src/models/WaSend.ts:17`, `apps/api/src/routes/whatsapp.ts:38`, `apps/web/src/pages/admin/AdminWhatsappPage.tsx:576`
- **Fix:** Persistir la campaña como entidad: un modelo WaCampaign {appId, merchantId, campaignId, total, text, status: 'running'|'done'|'interrupted', startedAt, finishedAt}. Al arrancar el proceso, marcar como 'interrupted' toda campaña que quedó en 'running'. GET /wa/campaigns devuelve total y status → el front puede cerrar la fase con un mensaje honesto ('se interrumpió: 120 de 500 enviados') y ofrecer reanudar. Cablear `awaitCampaign` en shutdown() con un timeout. Y no contar el cupo mensual por campa

### [R1-06] Ninguno de los cuatro fronts tiene ErrorBoundary: un chunk lazy que no se puede bajar (deploy nuevo con bundle viejo, u offline) = pantalla en blanco
- **Sector:** C2 · lentes 13 Degradación · 6 Estados no manejados · 15 UX
- **Síntoma:** El vecino (o el cajero, en el medio de un canje) toca un botón y la app se pone toda blanca. No hay mensaje, no hay botón de reintentar, no hay nada. La única salida es cerrar y volver a abrir — y si es una PWA instalada, ni siquiera es obvio cómo.
- **Repro:** 1) Usuario con la PWA abierta en /#/admin/validar. 2) Se deploya (los assets hasheados cambian de nombre; los viejos ya no existen en el server). 3) El usuario navega a una ruta cuyo chunk todavía no bajó, ej. /#/admin/whatsapp. 4) El `import('@/pages/admin/AdminWhatsappPage')` pide un archivo hasheado que ya no existe. El SPA fallback lo trata correctamente como archivo y devuelve 404 limpio (index.ts:357) en vez de HTML, así que el import rechaza. 5) React.lazy propaga el rechazo. No hay ningún ErrorBoundary en el árbol (`<Suspense>` cubre el loading, NO el error): grep de ErrorBoundary/componentDidCatch/getDerivedStateFromError en apps/web, apps/owner, apps/landing y apps/landing-vecino devuelve CERO resultados. 6) React desmonta todo el árbol → pantalla blanca. Mismo resultado offline sobre cualquier ruta cuyo chunk no esté precacheado.
- **Causa raíz:** 1) ¿Por qué pantalla blanca? Porque React desmontó todo el árbol al no encontrar quién capture el error. 2) ¿Por qué nadie lo captura? Porque no existe ningún ErrorBoundary en ninguno de los cuatro fronts. 3) ¿Por qué se asumió que no hacía falta? Porque Suspense da la sensación de estar cubriendo el caso asíncrono, pero sólo maneja el estado pendiente, no el rechazado. 4) ¿Por qué el chunk falla en primer lugar? Porque el code-splitting acopla la vida del bundle a la vida del deploy, y con `cleanupOutdatedCaches: true` + `clientsClaim: true` el SW nuevo toma el control de la página vieja y borra el precache anterior. 5) CAUSA TERMINAL: se adoptó code-splitting agresivo (30+ rutas lazy) y un SW con auto-update inmediato sin el contrapeso que ese combo exige — un límite de error que detecte 'chunk viejo' y ofrezca recargar. La app no tiene ningún piso: cualquier excepción de render en cualquier página termina en blanco.
- **Evidencia:** `apps/web/src/App.tsx:13`, `apps/web/src/App.tsx:110`, `apps/api/src/index.ts:355`, `apps/web/vite.config.ts:55`
- **Fix:** Agregar un ErrorBoundary de app envolviendo el <Suspense> de cada shell, con un fallback accionable ('Hay una versión nueva de la app' + botón que haga location.reload()). Detectar específicamente el error de chunk (/Failed to fetch dynamically imported module|ChunkLoadError/) y recargar automáticamente una sola vez (flag en sessionStorage para no loopear). Replicar en apps/owner. Reportar desde el boundary a Sentry para tener visibilidad de cuántas pantallas blancas se están comiendo hoy.

### [R1-01] La PWA no resuelve el tenant por dominio propio y el header X-Tenant-Slug que manda PISA la resolución por customDomain del backend → una ciudad con dominio propio sirve la ciudad equivocada
- **Sector:** T1 · lentes 2 fuga multi-tenant / 3 contrato FE-BE
- **Síntoma:** El owner le configura a Ramallo su dominio propio (ramallodescuentos.com.ar). Un vecino entra y ve el catálogo, los comercios, los colores y el nombre de OTRA ciudad (San Pedro, o la última que ese celular visitó). Si se registra, la cuenta le queda creada en la ciudad equivocada; si un comercio de Ramallo intenta entrar por ahí, el login le dice que no está registrado y lo manda a darse de alta de nuevo (comercio duplicado en San Pedro). Además el 'modo soporte' del owner sobre un comercio de esa ciudad tira 'código inválido o vencido'.
- **Repro:** 1) Owner → App detail → cargar customDomain = 'ramallodescuentos.com.ar' en la ciudad Ramallo (apps/owner/src/pages/AppDetailPage.tsx:307). 2) Apuntar el DNS al servicio. 3) Abrir https://ramallodescuentos.com.ar en un browser limpio. 4) detectInitialSlug(): no hay ?tenant; el host NO termina en .micuidad.com ni .misanpedro.app → no se lee subdominio; localStorage vacío → cae al literal 'sanpedro' (tenant.ts:240). 5) Todo request sale con `X-Tenant-Slug: sanpedro` (api.ts:131) y loadTenantConfig pide /tenant/sanpedro/config. 6) En el backend el header gana ANTES de mirar customDomain → tenant = San Pedro. Resultado: catálogo, cupones, branding y alta del vecino, todo en San Pedro.
- **Causa raíz:** 1) ¿Por qué se ve otra ciudad? Porque el backend resolvió appId = San Pedro. 2) ¿Por qué? Porque el request trajo X-Tenant-Slug: sanpedro y ese header tiene prioridad máxima (tenant.ts:66-67), así que findTenantByCustomDomain(host) nunca se ejecuta. 3) ¿Por qué el front manda ese slug? Porque detectInitialSlug() sólo sabe resolver por ?tenant, por subdominio de dos hosts fijos y por localStorage; no tiene NINGUNA rama de customDomain. 4) ¿Por qué manda algo en vez de no mandar nada (y dejar que el back resuelva por Host)? Porque el último fallback devuelve el literal 'sanpedro' en vez de null, así que el slug SIEMPRE existe y el header SIEMPRE viaja. 5) CAUSA TERMINAL: hay dos resolutores de tenant independientes (uno en el front, uno en el back) con reglas distintas, y el que tiene MENOS información (el front, que no puede consultar App.customDomain) es el que gana por contrato. El arreglo previo de S5-07 —poner el header antes del customDomain— cerró un agujero y abrió éste: dejó al customDomain del backend inalcanzable en la práctica, porque el único cliente que existe siempre manda header.
- **Evidencia:** `apps/web/src/lib/tenant.ts:215`, `apps/web/src/lib/tenant.ts:240`, `apps/web/src/lib/api.ts:131`, `apps/api/src/middleware/tenant.ts:66`, `apps/api/src/middleware/tenant.ts:75`
- **Fix:** Dos cambios, ambos chicos: (a) que `detectInitialSlug()` devuelva null cuando el host no es un host de plataforma y no hay ?tenant/localStorage/VITE_TENANT_SLUG (sacar el literal 'sanpedro'), y que `request()` omita el header X-Tenant-Slug si el slug es null — así el backend resuelve por Host→customDomain, que es el único que puede consultar la DB; (b) agregar un `GET /tenant/by-host` (o aceptar el host en /tenant/:slug/config) para que el front aprenda su slug real en dominio propio y lo persis

### [R1-04] Backend caído o vecino sin señal al abrir la PWA ⇒ pantalla "No encontramos esa ciudad" cuyo único botón BORRA el tenant elegido
- **Sector:** F1 · lentes 13 degradación · 6 estados no manejados · 15 UX
- **Síntoma:** El vecino abre la app (instalada en el celu) con mala señal o mientras el API está caído y ve: "No encontramos esa ciudad — La ciudad 'sanpedro' no existe o no está disponible. Probá con otra." Su ciudad existe y funciona perfecto. Si toca el único botón ("Ver ciudades disponibles") pierde la ciudad guardada y cae en el selector, que tampoco puede listar nada.
- **Repro:** 1) Poner el dispositivo en modo avión (o tirar el API abajo / que responda 5xx). 2) Abrir la PWA (el service worker sirve el bundle, así que la app arranca). 3) main.tsx dispara loadTenantConfig(slug). 4) El fetch rechaza → el catch setea { loading:false, error:'Failed to fetch', config:null }. 5) App.tsx evalúa if (!loading && !config && error) → true → renderiza TenantNotFoundScreen. 6) Toda la app queda cortada ANTES del HashRouter, así que ni el OfflineBanner ni los estados 'Sin conexión / Reintentar' de las páginas llegan a montarse. 7) Tocar el botón ejecuta clearTenantSlug() (borra misanpedro.tenant.slug) + reload → TenantSelectorPage → listAvailableTenants() también falla y devuelve [] → 'Todavía no hay ciudades'.
- **Causa raíz:** 1) ¿Por qué dice que la ciudad no existe? Porque App.tsx interpreta cualquier `error` del tenant como 'ciudad inválida'. 2) ¿Por qué? Porque loadTenantConfig colapsa DOS causas distintas en el mismo campo error: el 404 del API ('tenant no encontrado') y la excepción de red del catch ('error de red'). 3) ¿Por qué se colapsaron? Porque el estado del tenant no distingue notFound de unreachable. 4) ¿Por qué el remedio es destructivo? Porque el único CTA de esa pantalla asume el diagnóstico 'tu slug está mal' y por eso borra la selección persistida. 5) CAUSA TERMINAL: se diseñó el estado de error del bootstrap del tenant para la falla determinística (slug inválido) y no para la transitoria (sin red / API caído), que en una PWA offline-capable es la mucho más frecuente — y el recovery elegido es irreversible.
- **Evidencia:** `apps/web/src/lib/tenant.ts:272`, `apps/web/src/App.tsx:181`, `apps/web/src/App.tsx:149`, `apps/web/src/pages/TenantSelectorPage.tsx:142`, `apps/web/src/lib/tenant.ts:312`
- **Fix:** Separar el estado: en loadTenantConfig el catch (red) debe setear algo tipo { error: null, unreachable: true } y el !res.ok con 404 { notFound: true }. En App.tsx mostrar TenantNotFoundScreen sólo con notFound; para unreachable mostrar una pantalla 'Sin conexión' con botón Reintentar (loadTenantConfig(slug)) que NO borre el slug. Extra: cachear la última TenantConfig buena en localStorage y arrancar con ella cuando el fetch falla — es una PWA, el branding y el nombre de la ciudad no tienen por q


# P2 (71)

### [R1-01] Cambiar de ciudad deja al vecino en "sesión zombie": todo 403, y sigue viendo el historial y el ahorro de la OTRA ciudad
- **Sector:** R1 · lentes 2 Fuga multi-tenant + 4 Auth cruzada + 14 Errores tragados
- **Síntoma:** El vecino abre un link con ?tenant=otraciudad (o toca otra ciudad en el selector). La app se recarga con la marca de la ciudad nueva, lo sigue mostrando logueado con su nombre, y en Canjeados le muestra sus canjes y su ahorro de la ciudad vieja. Pero si intenta activar cualquier cupón le sale "No se pudo activar el cupón · tenant mismatch". No hay botón de Salir en la app del vecino: queda trabado para siempre en ese estado.
- **Evidencia:** `apps/web/src/lib/tenant.ts:175`, `apps/web/src/lib/tenant.ts:197`, `apps/web/src/lib/stores.ts:13`, `apps/web/src/lib/api.ts:14`, `apps/api/src/middleware/auth.ts:57`
- **Fix:** 1) Scopear el storage del vecino por tenant: `misanpedro.v1.${slug}` y `msp.tok.user.*.${slug}` (mismo patrón que ya usa alerts.ts). 2) En `setTenantSlug()`/`clearTenantSlug()`, antes de recargar, limpiar tokens y store del vecino si el slug cambia. 3) Tratar el 403 con `error === 'tenant mismatch'` igual que un 401 en `request()` (limpiar tokens + disparar `msp:session-expired`). 4) Agregar en ApiSync un handler para `subject === 'user'` que haga `userActions.signOut()` y avise "cambiaste de ci

### [R1-03] La pantalla de historial/ahorro ("Canjeados") quedó huérfana: no está en la navegación y nada la linkea; "Mis cupones" directamente no está ruteada
- **Sector:** R1 · lentes 6 Estados no manejados / navegación + 3 Contrato roto
- **Síntoma:** El vecino canjea, ve el toast "¡Tu canje quedó registrado!" y después no puede volver a su historial: en la barra inferior hay Mapa · Locales · Cupones · Alertas · Perfil, y en ninguna pantalla hay un link a /canjeados. Tampoco existe una pantalla "Mis cupones" donde ver el código que activó hace 5 minutos: si sale de la pantalla del QR (bloqueo de pantalla, notificación, back), la única forma de volver es acordarse de qué cupón era, buscarlo en el catálogo y tocar "Ver mi cupón activo" — y la tarjeta del catálogo no muestra ninguna señal de que ya tiene un código activo para ese cupón.
- **Evidencia:** `apps/web/src/layouts/AppShell.tsx:10`, `apps/web/src/App.tsx:211`, `apps/web/src/components/RedemptionWatcher.tsx:51`, `apps/web/src/pages/CuponActivoPage.tsx:183`, `apps/web/src/pages/CuponActivoPage.tsx:382`
- **Fix:** 1) Volver a poner Canjeados en `links` de AppShell (o linkearlo desde SavingsWallet/ClubCard con "Ver mis canjes"). 2) Implementar la acción prometida en RedemptionWatcher (toast con acción que navegue a /canjeados) y borrar el `void navigate`. 3) Rutear MisCuponesPage (o borrarla y corregir los copys que dicen "desde Cupones"). 4) Pasar a CouponCard el estado de activación activa para mostrar "Tenés tu código listo →".

### [R1-04] Si el cupón se pausa/vence o el comercio se suspende, la pantalla del código activo rebota al home sin decir nada — con el vecino parado en el mostrador
- **Sector:** R1 · lentes 3 Contrato FE/BE roto + 6 Estados no manejados
- **Síntoma:** El vecino abre /activacion/:id para mostrar su QR y la app lo devuelve al inicio, sin mensaje. Su activación sigue 'activo' en la base y el backend ya le mandó el título del cupón y el nombre del comercio: la pantalla simplemente no los usa. Pasa cada vez que el cupón deja de estar en el catálogo público: pausado por el comercio, vencido por el job de expiración, agotado por stock, o comercio suspendido por suscripción vencida.
- **Evidencia:** `apps/api/src/routes/coupons.ts:66`, `apps/api/src/routes/coupons.ts:74`, `apps/web/src/pages/CuponActivoPage.tsx:21`, `apps/web/src/pages/CuponActivoPage.tsx:156`, `apps/api/src/routes/activations.ts:237`
- **Fix:** En CuponActivoPage resolver título/%/comercio con la MISMA cascada que CanjeadosPage: `coupon = apiCoupon ?? { titulo: activation.couponTitulo, porcentaje: activation.couponPorcentaje }` y `merchant = apiMerchant ?? { nombre: activation.merchantNombre }`. Sólo rebotar si tampoco hay snapshot. Idem en MisCuponesPage.tsx:214. Además, cuando la activación esté viva pero el cupón ya no sea canjeable, mostrar un aviso explícito ("este descuento se pausó/venció — hablá con el comercio") en vez de un Q

### [R1-08] Cuando el cajero no tipea el monto, se guarda el precio de lista COMO SI FUERA el ticket real y se le muestra al vecino "Ticket / Pagaste" como un hecho
- **Sector:** R1 · lentes 1 Costura fallback + 8 Plata
- **Síntoma:** El vecino compra $3.000, el cajero confirma sin cargar monto (es opcional y el copy dice "confirmá así nomás"). En su historial le aparece: "Ticket $12.000 · Pagaste $9.600 · Ahorraste $2.400". Los tres números son inventados a partir del precio de lista del cupón y se presentan como su gasto real.
- **Evidencia:** `apps/api/src/routes/redemptions.ts:200`, `apps/api/src/routes/redemptions.ts:253`, `apps/web/src/pages/CanjeadosPage.tsx:178`, `apps/web/src/pages/CanjeadosPage.tsx:204`, `apps/web/src/lib/api.ts:632`
- **Fix:** Persistir el origen: `montoTicketOrigen: 'cajero' | 'estimado'` (o dejar `montoTicket` sólo cuando lo cargó el cajero y guardar la estimación en un campo aparte usado únicamente para `ahorroEstimado`). En CanjeadosPage, ocultar el bloque Ticket/Pagaste cuando el origen sea estimado, o rotularlo "estimado sobre el precio de lista".

### [R1-09] Cupón AGOTADO: el panel le dice al cajero "ya fue canjeado, el cliente ya lo usó en una visita anterior" — acusando al vecino de algo que no hizo
- **Sector:** R1 · lentes 3 Contrato FE/BE roto + 11 Límites/stock
- **Síntoma:** El vecino llega con un código válido y sin usar de un cupón que se quedó sin stock. El cajero valida y la pantalla dice "Este descuento ya fue canjeado · El cliente ya lo usó en una visita anterior. Cada descuento vale una sola vez." El vecino queda como que estaba intentando pasar dos veces el mismo cupón; el cajero no se entera de que el problema es su propio stock.
- **Evidencia:** `apps/api/src/routes/redemptions.ts:108`, `apps/web/src/pages/admin/AdminValidarPage.tsx:335`, `apps/web/src/pages/admin/AdminValidarPage.tsx:371`, `apps/api/src/routes/redemptions.ts:189`
- **Fix:** Agregar `case 'agotado': reason = 'sold-out'` con copy propio ("Este descuento se agotó · Se llegó al tope de canjes que cargaste. El código del cliente es válido: si querés aceptarlo, subí el tope en Descuentos.") y cambiar el `default` a 'not-found'/genérico en vez de 'already-redeemed'. Tipar los status del canje en `packages/shared` y usar un switch exhaustivo.

### [R1-11] "Armá tu plan" recomienda cupones que el vecino ya agotó: los muestra disponibles y al entrar dicen "Ya lo usaste"
- **Sector:** R1 · lentes 3 Contrato roto entre pantallas + 6 Estados
- **Síntoma:** El vecino toca una ocasión en el carrusel del home, ve "Los que más te rinden" con 6 cupones, elige el mejor y al abrirlo el botón está gris con "Ya lo usaste · Disponible el 12 de junio". La misma tarjeta en el home aparecía tachada con "Ya lo usaste", pero en el planificador no.
- **Evidencia:** `apps/web/src/pages/PlanPage.tsx:247`, `apps/web/src/pages/DescuentosPage.tsx:323`, `apps/web/src/components/CouponCard.tsx:68`, `apps/web/src/components/CouponCard.tsx:83`, `apps/web/src/pages/CuponDetailPage.tsx:94`
- **Fix:** Mover el cálculo adentro de CouponCard (que llame `useUsoEstado()` él mismo, o consumir un contexto), o hacer la prop obligatoria para que TypeScript marque los llamadores que faltan. Como mínimo, pasar `usoEstado={getUsoEstado(m.coupon)}` en PlanPage y filtrar los agotados de las recomendaciones.

### [R1-12] Borrar un cupón deja activaciones huérfanas: el vecino llega con su código y el comercio lee "No encontramos este código, revisá los dígitos"
- **Sector:** R1 · lentes 12 Integridad referencial + 3 Contrato
- **Síntoma:** El comercio borra un descuento desde su panel ("Descuento eliminado · No se podrá reactivar"). Los vecinos que ya lo tenían activado siguen con el QR y el código de 6 dígitos en la mano. Al validarlo, el panel dice "No encontramos este código · Revisá los dígitos con el cliente. Si sigue fallando, que abra el QR" — el mismo mensaje que si el vecino se hubiera equivocado tipeando. Nadie en la escena sabe que el cupón se borró.
- **Evidencia:** `apps/api/src/routes/coupons.ts:251`, `apps/api/src/routes/coupons.ts:262`, `apps/api/src/routes/redemptions.ts:90`, `apps/api/src/models/Activation.ts:29`, `apps/web/src/pages/admin/AdminCuponesPage.tsx:93`
- **Fix:** 1) Escribir el snapshot (`couponTituloSnapshot`, `merchantNombreSnapshot`) al CREAR la activación, no sólo al canjear — así el historial y la pantalla del código sobreviven a cualquier borrado. 2) En DELETE /coupons/:id, o bien soft-delete (`estado: 'eliminado'`), o bien cancelar las activaciones 'activo' del cupón y avisarle al comercio cuántas hay antes de confirmar. 3) Diferenciar en validate el 404 de "código inexistente" del de "cupón dado de baja" y darle copy propio.

### [R1-13] Recuperar la cuenta en otro celular NO cierra la sesión del celular perdido: el refresh token viejo no vence nunca y nadie avisa
- **Sector:** R1 · lentes 4 Auth cruzada / higiene de sesión
- **Síntoma:** Al vecino le roban el celular. Entra desde otro con su email + código y recupera su cuenta. El celular robado sigue con sesión válida indefinidamente: puede ver su nombre, teléfono, historial completo de canjes y descargar el export de datos personales. La única forma de cortarlo es que el vecino descubra por su cuenta el botón "Cerrar sesión en todos lados" en Perfil — que además también lo desloguea a él y le exige un tercer código.
- **Evidencia:** `apps/api/src/routes/user-auth.ts:269`, `apps/api/src/routes/user-auth.ts:65`, `apps/api/src/services/jwt.service.ts:109`, `apps/api/src/routes/user-auth.ts:379`, `apps/web/src/pages/PerfilPage.tsx:327`
- **Fix:** 1) Que `verify-otp` del vecino ofrezca (o aplique) la revocación: como mínimo devolver `otrasSesiones: N` y que la pantalla de recuperación pregunte "¿Cerramos las sesiones de otros dispositivos?". 2) Agregar `revokeAllForSubjectExcept(subjectType, subjectId, currentTokenHash)` para poder cerrar las otras sin autodesloguearse, y usarlo en el botón de Perfil. 3) Considerar un TTL largo (ej. 12 meses) en vez de `NEVER_EXPIRES_AT` para las sesiones del vecino.

### [R2-03] Cupón agotado por stock: al validar, al vecino se lo acusa de "ya lo usaste". El status 'agotado' del 409 cae en el `default` del switch → 'already-redeemed'
- **Sector:** R2 · lentes 6 Estados no manejados + 11 límites/stock + 3 contrato FE/BE
- **Síntoma:** El comercio puso un cupo de 50 canjes. Llega el vecino 51, el cajero valida su código y la pantalla dice: "Este descuento ya fue canjeado · El cliente ya lo usó en una visita anterior. Cada descuento vale una sola vez." El vecino nunca lo usó. El cajero le dice que sí. Y el comercio tampoco se entera de que su cupo se agotó — no hay ningún aviso de "se te terminó el stock".
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:109`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:348`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:349`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:350`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminValidarPage.tsx:374`
- **Fix:** Agregar `case 'agotado': reason = 'sold-out'` en `toLegacyResult` y una entrada en `errorCopy` con copy operativo para el comercio ('Se agotó el cupo de este descuento — subí el cupo o creá uno nuevo desde Mis descuentos'), con CTA a /admin/cupones. Y cambiar el `default` a un motivo neutro ('No pudimos validar este código, probá de nuevo') para que un status futuro del backend nunca vuelva a convertirse en una acusación al vecino. Idealmente, en el backend separar `status` (de la activación) de

### [R2-04] Cancelar la suscripción dentro de los 10 días de arrepentimiento deja al comercio en un callejón sin salida: 'cancelado' bloquea el login, y el mensaje del backend le promete reactivarse desde una pantalla a la que no puede entrar (y que no tiene botón de reactivación)
- **Sector:** R2 · lentes 6 Estados no manejados + 4 auth cruzada de estados + 13 degradación
- **Síntoma:** El comercio toca "Cancelar suscripción" en Mi Comercio dentro de los primeros 10 días. Ve "¡Listo! Te reembolsamos". Al siguiente refresh queda afuera: el panel lo escupe al login y, cuando pide el código, el login le responde "Tu cuenta fue cancelada. Si querés volver, escribinos a soporte." No hay camino self-service de vuelta. Mientras tanto el propio backend, en el 403 de cualquier endpoint operativo, le dice "Reactivala desde Mi Comercio para volver a operar" — una pantalla que no puede abrir y que, aun abriéndola, no tiene control de reactivación.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:249`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchant-auth.ts:327`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/middleware/auth.ts:144`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminComercioPage.tsx:1421`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminComercioPage.tsx:1433`
- **Fix:** Dos cambios mínimos: (a) permitir el login de comercios 'cancelado' emitiendo una sesión degradada (o mantener el 403 pero agregar un flujo de reactivación por OTP que devuelva al comercio a 'pending_payment'), y (b) agregar en SubscriptionCard una rama `estado === 'cancelado'` con botón 'Reactivar mi cuenta' que llame a /billing/preapproval y ponga el merchant en `pending_payment`. Si la decisión de producto es que cancelar sea irreversible sin soporte, hay que corregir el mensaje de `requireMe

### [R2-05] El reemplazo server-side del precio en el JSON-LD de la landing quedó huérfano: busca `"50000"` pero el HTML ahora dice `"30000"` → toda ciudad publica a Google el precio de San Pedro con la moneda de la otra ciudad
- **Sector:** R2 · lentes 3 contrato roto entre capas (acoplamiento por string literal) + 8 plata/moneda mezclada
- **Síntoma:** La landing de comercios de cualquier ciudad que no sea San Pedro se sirve con datos estructurados que anuncian 30.000 en la MONEDA de esa ciudad. Para Nariño (COP) el rich result de Google dice '30000 COP/mes' aunque el `precioMensual` real del tenant sea otro. El comerciante que llega desde Google ve un precio y en el panel ve otro.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/index.ts:245`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/index.html:72`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/dist/index.html:72`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/index.ts:246`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/landing/src/lib/launch.ts:19`
- **Fix:** Reemplazar el literal por el valor actual y, sobre todo, romper el acoplamiento: exportar el token desde un único lugar (p.ej. `PRECIO_MENSUAL` en packages/shared o un placeholder explícito `__PRECIO_MENSUAL__` en el index.html) y agregar un test que lea `apps/landing/index.html` y falle si el token que busca `injectLandingMeta` no aparece. Mismo test para 'MiSanPedro', 'San Pedro', '"ARS"' y '#ea580c'.

### [R2-06] El historial de campañas de WhatsApp se arma desde los últimos 500 envíos individuales, no desde las últimas 500 campañas: con lotes grandes el reporte muestra menos entregados de los que salieron
- **Sector:** R2 · lentes 10 paginación/truncado + 8 números que no cierran entre pantallas
- **Síntoma:** El comercio manda una campaña a 400 vecinos, la ve llegar al 100% en vivo (SSE), y la semana siguiente manda otra de 400. Cuando vuelve a abrir la pantalla, el historial le dice que la primera campaña tuvo 100 entregados en lugar de 400. El número baja solo con el tiempo, a medida que entran envíos nuevos.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:306`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:183`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:17`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:331`
- **Fix:** Cambiar /wa/campaigns por una agregación en Mongo: `$match` por appId+merchantId+campaignId≠null, `$group` por campaignId sumando ok/fail y tomando `$max: sentAt`, `$sort` por sentAt desc, `$limit: 50`. Así el límite se aplica a campañas y los conteos son totales reales. Test: 2 campañas de 400 → ambas deben reportar 400.

### [R2-07] POST /redemptions/validate no chequea el límite de uso por persona pero POST /confirm sí: el cajero anuncia "Descuento válido" y el canje se cae recién al confirmar
- **Sector:** R2 · lentes 3 contrato entre dos endpoints de la misma transacción + 6 estados no manejados
- **Síntoma:** El cajero valida el código, la pantalla se pone verde con "Descuento válido · 30% off", toca "Confirmar canje", carga el monto, aprieta "Confirmar canje" y recibe un toast rojo: "No se pudo confirmar · Este vecino ya usó el cupón el máximo de veces permitido." Ya le dijo al cliente que estaba todo bien y ya le pasó el producto por caja.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:165`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:108`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:170`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:242`
- **Fix:** Llamar `checkUsoLimite` también en POST /redemptions/validate y devolver un 409 con `status: 'limite_persona'` + `nextDisponible`, con copy propio en `errorCopy` ('Este vecino ya usó este descuento — vuelve a estar disponible el X'). Alternativa más robusta: extraer una función `puedeCanjearse(activation, coupon, userId)` compartida por /validate y /confirm para que sea imposible que se desincronicen.

### [R2-08] El comercio cambia el nombre de su local y el panel sigue mostrando el viejo: `apiMerchant` sólo se escribe en el login y ApiSync pide /merchant/auth/me pero descarta la respuesta
- **Sector:** R2 · lentes 3 contrato FE/BE (respuesta fresca descartada) + 6 estados no manejados
- **Síntoma:** El comercio corrige su nombre (o su categoría) en Mi Comercio, ve el toast "Comercio actualizado · Los cambios ya se ven en la app del vecino" y el nombre de esa pantalla se actualiza. Pero el header del panel, el sidebar y el título del Dashboard siguen mostrando el nombre anterior — para siempre, incluso cerrando y abriendo la app, hasta que cierre sesión y vuelva a entrar. Queda con la sensación de que el cambio 'a medias' no se guardó.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/components/ApiSync.tsx:101`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/components/ApiSync.tsx:99`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminComercioPage.tsx:276`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/layouts/MerchantShell.tsx:77`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchant-auth.ts:517`
- **Fix:** En ApiSync, usar la respuesta: `const me = await api.merchantApi.me()` y llamar a un `merchantAuth.hydrate(me.user, me.merchant)` que haga `update()` sobre el store (idempotente, sólo si cambió algo). Y en el `save()` de AdminComercioPage, actualizar `apiMerchant.nombre/categoria` con la respuesta del PATCH. Con eso se corrigen de una vez el nombre, la categoría y el estado.

### [R2-10] "Mis descuentos" cuenta los canjes por cupón sobre los últimos 200 canjes del comercio, teniendo `stockUsado` (el conteo real) en el mismo payload
- **Sector:** R2 · lentes 8 números que no cierran + 10 truncado
- **Síntoma:** El comercio abre Mis descuentos y ve "12 canjes" en un cupón que en realidad tuvo 300. Peor: el número BAJA con el tiempo, porque a medida que entran canjes nuevos los viejos se caen de la ventana de 200. En Estadísticas, el mismo cupón aparece con otro número (el del período, calculado en el backend sobre TODOS los canjes).
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminCuponesPage.tsx:32`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminCuponesPage.tsx:62`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:355`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:27`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:283`
- **Fix:** Usar `c.stockUsado ?? 0` como número de canjes de cada tarjeta en AdminCuponesPage y bajar el `useApiRecentRedemptions(200)` de esa página. Si se quiere el conteo por período, pedirlo al backend con una agregación. Además, agregar un chequeo periódico (o test de integración) que compare `Coupon.stockUsado` contra `Redemption.countDocuments({couponId})` para detectar la deriva del contador materializado.

### [R2-11] El premio del referido se pierde para siempre en silencio si algo falla después de reclamar `firstCouponAt`: el claim es irreversible y el catch sólo loguea
- **Sector:** R2 · lentes 12 integridad referencial + 14 errores tragados + 9 idempotencia
- **Síntoma:** El comercio A invita al comercio B. B se registra y publica su primer descuento. En "Recomendá y ganá", A ve a B para siempre con el chip azul "Se registró" (pendiente) y sus "Semanas ganadas" quedan en 0. B tampoco recibe sus 15 días extra. Ninguno de los dos tiene forma de reclamarlo y no hay reintento posible: publicar más descuentos no vuelve a disparar nada.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:148`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:149`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:193`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:142`
- **Fix:** Invertir el orden o hacerlo reintentable: (a) mover el claim de `firstCouponAt` DESPUÉS de acreditar el referido, o (b) en el catch, revertir `firstCouponAt` con un `updateOne({_id, firstCouponAt: claimedDate}, {$unset:{firstCouponAt:''}})` para que el próximo cupón reintente. Independientemente, agregar al `runExpirySweep` un paso que busque Referrals 'pending' cuyo `referredMerchantId` ya tenga `firstCouponAt` y los acredite — la reconciliación es la única forma de cerrar el agujero de verdad.

### [R3-02] Existe una superficie de super-admin PARALELA (`/api/v1/admin/*`) con token estático, sin RBAC y que NO escribe una sola línea en la auditoría
- **Sector:** R3 · lentes 5 RBAC sólo en el front / 12 Integridad / auditoría
- **Síntoma:** Entrás a Auditoría convencido de que ahí está todo lo que pasó en la plataforma. Pero un comercio puede ser suspendido, cancelado o "reembolsado" desde otra puerta (`/api/v1/admin`) y en Auditoría no aparece nada. Además ese endpoint puede poner estados que el panel del owner ni siquiera ofrece (`cancelado`, `pending_payment`).
- **Evidencia:** `apps/api/src/routes/admin.ts:15`, `apps/api/src/routes/admin.ts:115`, `apps/api/src/routes/admin.ts:6`, `apps/api/src/routes/admin.ts:40`, `apps/api/src/index.ts:134`
- **Fix:** Decidir una sola puerta. Lo correcto: borrar `apps/api/src/routes/admin.ts` y desmontar `app.route('/api/v1/admin', ...)` en index.ts:134 — todo lo que hace ya existe en `/owner/*` con identidad, rol y auditoría. Si por runbook hace falta un acceso de emergencia, dejarlo pero (a) forzar OwnerAuditLog con `ownerEmail:'x-admin-token'` en cada mutación, (b) `timingSafeEqual` para el token, (c) rateLimit, (d) alinear el cálculo de MRR con `computeMrr()` en vez de `activeSubs * PLAN_AMOUNT_ARS`.

### [R3-04] El MRR por ciudad SUMA monedas distintas en un solo número y lo etiqueta con la moneda actual de la ciudad; y las tres pantallas de MRR dan tres números diferentes
- **Sector:** R3 · lentes 8 Plata (moneda mezclada) / 3 Contrato FE-BE
- **Síntoma:** Con ciudades en más de un país, el gráfico "MRR por ciudad" muestra barras comparables que no lo son, y una ciudad que cambió de moneda muestra pesos argentinos rotulados como COP. Encima el Dashboard dice "MRR: ARS 500.000 / COP 3.000.000", Estadísticas dice "MRR (ARS) $500.000" y el gráfico por ciudad suma 3.500.000. Ningún número cierra con el otro.
- **Evidencia:** `apps/api/src/services/ownerStats.service.ts:40`, `apps/api/src/services/ownerStats.service.ts:25`, `apps/owner/src/pages/StatsPage.tsx:86`, `apps/owner/src/pages/StatsPage.tsx:133`, `apps/api/src/routes/owner.ts:736`
- **Fix:** (a) Clavar la clave del Map en `${appId}|${currency}` y devolver una fila por ciudad x moneda, con `moneda` = la de la SUSCRIPCION (`r._id.currency`), no la de la App. (b) En StatsPage, o graficar una serie por moneda, o normalizar a una moneda de reporte con un tipo de cambio explícito y decirlo en el subtítulo. (c) Reemplazar el KPI `byCurrency['ARS']` por el mismo desglose multi-moneda que ya tiene DashboardPage (líneas 116-137) para que las dos pantallas den lo mismo. (d) Renombrar `Subscrip

### [R3-05] `GET /owner/metrics` no tiene gate de rol: el rol `soporte` — al que /stats le esconde el MRR a propósito — lo ve igual en el Dashboard
- **Sector:** R3 · lentes 5 RBAC probado contra el endpoint, no contra el menú
- **Síntoma:** Le das el rol "Soporte — Comercios y vecinos (sin pagos)" a alguien, le desaparece el menú Pagos y el bloque de MRR de Estadísticas... pero el Dashboard, que es la primera pantalla que ve al entrar, le muestra el MRR de toda la plataforma en un StatCard gigante.
- **Evidencia:** `apps/api/src/routes/owner.ts:474`, `apps/api/src/routes/owner.ts:502`, `apps/api/src/routes/owner.ts:512`, `apps/owner/src/lib/rbac.ts:30`, `apps/owner/src/pages/DashboardPage.tsx:142`
- **Fix:** Agregar `requireOwnerRole('super','admin','finanzas','viewer')` al bloque `revenue` (o partir /metrics en dos: KPIs operativos sin gate + revenue con gate, igual que se hizo en /stats). Y en `DashboardPage`, envolver el StatCard de MRR en `canSeeMrr(auth.owner?.rol)`. Estructural: mover la MATRIX a `packages/shared` y derivar de ahí tanto los middlewares del back como el gating del front, con un test que recorra las rutas de `ownerRoutes` y falle si alguna que toque plata no tiene gate.

### [R3-06] La pantalla Comercios del owner descarga los documentos completos: hasta ~30 MB de imágenes base64 por comercio, x50 filas, para mostrar seis columnas de texto
- **Sector:** R3 · lentes 6 Lista de 1000 / degradación / eficiencia
- **Síntoma:** Con comercios que cargaron su galería, la pestaña Comercios tarda una eternidad y en móvil directamente se cuelga o se queda en skeleton. Se están bajando megas de fotos en base64 que la tabla ni muestra.
- **Evidencia:** `apps/api/src/routes/owner.ts:851`, `apps/api/src/models/Merchant.ts:77`, `packages/shared/src/schemas.ts:195`, `apps/owner/src/pages/MerchantsPage.tsx:200`, `apps/api/src/routes/owner.ts:843`
- **Fix:** Proyectar: `Merchant.find(filter, 'appId slug nombre categoria estado createdAt freeTrialUntil').lean()` en `/owner/merchants` (y equivalente en `/owner/users` y `/owner/subscriptions`, excluyendo `rawLast`). Si el detalle completo hace falta, que sea un `GET /owner/merchants/:id` aparte. De fondo: sacar las imágenes de Mongo a un bucket/CDN y guardar URLs — el propio comentario del modelo lo tiene anotado como deuda.

### [R3-07] El dominio propio de una ciudad no se normaliza al guardarlo y, una vez seteado, NO se puede borrar desde el panel
- **Sector:** R3 · lentes 3 Contrato FE-BE (undefined silencioso) / 6 Estados / integridad
- **Síntoma:** Cargás el dominio propio con una mayúscula, un espacio o pegando 'https://...': se guarda igual, el panel lo muestra en verde como configurado, y la ciudad nunca resuelve por ese dominio (404 'tenant not found'). Peor: querés borrarlo o mudarlo a otra ciudad, vaciás el campo, guardás... y vuelve. La otra ciudad tira 409 'customDomain ya en uso'. Quedás trabado sin salida por UI.
- **Evidencia:** `apps/api/src/routes/owner.ts:747`, `apps/api/src/middleware/tenant.ts:31`, `apps/owner/src/pages/AppDetailPage.tsx:149`, `apps/api/src/routes/owner.ts:785`, `apps/api/src/lib/urls.ts:21`
- **Fix:** En `updateAppSchema`: `customDomain: z.string().trim().toLowerCase().regex(/^[a-z0-9]([a-z0-9-]*[a-z0-9])?(\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+$/).or(z.literal('')).optional()`, y tratar `''` (o `null`) como `$unset: { customDomain: 1 }` en el handler. En el front, mandar `customDomain: draft.customDomain.trim()` SIEMPRE (string vacío incluido) en vez de `|| undefined`. Backfill: normalizar a minúsculas los `customDomain` ya guardados. Bonus: en DnsSetupCard, mostrar el custom domain como 'sin veri

### [R3-08] Los filtros de la Auditoría son de coincidencia EXACTA: "Comercios" oculta las reactivaciones, "Ciudades" oculta las creaciones y "Equipo" oculta los cambios de rol y las bajas
- **Sector:** R3 · lentes 3 Contrato FE-BE / auditoría
- **Síntoma:** Vas a Auditoría, tocás el chip "Equipo" para revisar qué se hizo con los permisos y ves sólo invitaciones. Los cambios de rol y las deshabilitaciones — lo que de verdad importa auditar — no aparecen. Con "Comercios" ves suspensiones y ninguna reactivación. Parece que esas acciones no se registran; se registran, el filtro las esconde.
- **Evidencia:** `apps/owner/src/pages/AuditPage.tsx:32`, `apps/api/src/routes/owner.ts:419`, `apps/api/src/routes/owner.ts:368`, `apps/api/src/routes/owner.ts:875`
- **Fix:** Cambiar los valores de los chips a prefijos: `'app.'`, `'merchant.'`, `'admin.'`, `'subscription.'` (y dejar `'auth.login'` exacto o pasarlo a `'auth.'`). Mejor todavía: usar el filtro `recurso` que el back ya soporta (`recurso=merchant`), que es la dimensión que el chip realmente quiere. Y completar ACTION_META con `subscription.*` y `support.session.revoke`.

### [R3-09] El front no espeja su propia matriz de RBAC: viewer y finanzas ven botones de Suspender, Soporte, Editar ciudad e Invitar admin que siempre terminan en 403
- **Sector:** R3 · lentes 5 RBAC sólo en el front (al revés) / 14 Errores tragados / UX
- **Síntoma:** Un usuario 'viewer' o 'finanzas' entra a Comercios y ve los botones Soporte, Cerrar soporte y Suspender. Los toca, y aparece una franja roja que dice 'forbidden'. Lo mismo con Editar en el detalle de una ciudad, y con Invitar administrador si escribe /equipo a mano. El panel promete acciones que la persona no puede hacer y devuelve un error técnico en inglés.
- **Evidencia:** `apps/owner/src/lib/rbac.ts:26`, `apps/owner/src/pages/MerchantsPage.tsx:218`, `apps/owner/src/pages/SubscriptionsPage.tsx:196`, `apps/owner/src/pages/AppDetailPage.tsx:224`, `apps/owner/src/App.tsx:29`
- **Fix:** Gatear las acciones, no sólo el menú: `can(rol,'comercios','editar')` en los botones de MerchantsPage, `can(rol,'pagos','editar')` en SubscriptionsPage, `can(rol,'apps','editar')` en el botón Editar de AppDetailPage y `can(rol,'equipo','editar')` en TeamPage. Además, envolver las rutas sensibles en un `<RequireRole recurso="equipo">` que redirija con un mensaje humano en vez de dejar que la página cargue y falle. Y mapear los `error` conocidos del back a texto en castellano en `lib/api.ts`.

### [D1-02] Suspender o cancelar un comercio no toca sus cupones: el vecino todavía puede ACTIVAR un cupón de un comercio suspendido y se lleva un código que nunca va a poder canjear
- **Sector:** D1 · lentes 12 Integridad referencial + 2/5 Exposición pública sin gate de estado
- **Síntoma:** El owner suspende un comercio (o el comercio cancela su suscripción). Sus cupones siguen con estado 'activo' en la base. El endpoint público de detalle de cupón los sigue sirviendo, y la app deja activarlos: el vecino se lleva un código de 6 dígitos, va al local, y recién ahí el cajero recibe un 403 "Tu cuenta está suspendida". El vecino queda plantado en el mostrador.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:88`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/activations.ts:95`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:871`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:249`
- **Fix:** Chequear el comercio en el camino de activación (Merchant.findOne({_id: coupon.merchantId, appId, estado: {$in:['activo','pending_payment']}}) → 409 con motivo claro), y filtrar por estado del comercio también en GET /coupons/:id. Además, centralizar la transición de estado del Merchant en un service que apague los cupones ('pausado') y cancele las activaciones vivas, para que los tres caminos (owner, billing, expiry, admin) hereden la misma cascada.

### [D1-05] Borrar la cuenta resetea el límite "una vez en la vida" de los cupones: el vecino se borra, se vuelve a dar de alta con el mismo mail y vuelve a canjear
- **Sector:** D1 · lentes 11 Límites + 12 Contadores desincronizados del hecho real
- **Síntoma:** Un cupón configurado como "1 uso por persona, de por vida" (el default) se puede farmear: el vecino lo canjea, borra su cuenta desde Perfil, se registra otra vez con el mismo email —el alta no pide código si el email no existe— y el cupón le vuelve a aparecer disponible. El comercio paga el descuento dos veces creyendo que es imposible.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/usageLimit.ts:22`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/user-auth.ts:435`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/user-auth.ts:194`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Coupon.ts:30`
- **Fix:** Conservar en el canje un identificador seudónimo estable derivado del email dentro del tenant (ej. hash HMAC de appId+email guardado como `subjectHash` en Redemption) y que checkUsoLimite cuente por ese campo, no por userId. El borrado sigue eliminando el User y despunteando userId, pero el contador sobrevive sin exponer PII. Si se prefiere no guardar nada, al menos aplicar un enfriamiento al alta cuando el email tuvo canjes anonimizados recientes.

### [D1-06] Los canjes anonimizados cuentan como UN cliente fantasma: tres pantallas del comercio dan tres números distintos de clientes y de ventas
- **Sector:** D1 · lentes 8 Los totales tienen que cerrar entre pantallas + 12 Contadores desincronizados
- **Síntoma:** El panel del comercio muestra en el inicio "3 clientes únicos", en el Asesor "2 clientes" y en la lista de Clientes salen 2 filas. Y las ventas no cierran: el total de ingresos del inicio incluye tickets de personas que ya no aparecen en ninguna lista. El comercio no tiene forma de saber cuál de los tres números es el bueno.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchants.ts:174`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:142`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:405`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:155`
- **Fix:** Decidir la semántica una sola vez (propuesta: el canje anonimizado SIGUE contando como venta y como canje, y NO cuenta como cliente identificable, pero se expone aparte como "clientes que se dieron de baja: N") y aplicarla desde una sola función compartida. Como parche inmediato, en merchants.ts:174 agregar `.filter(Boolean)` para que el fantasma deje de contar.

### [D1-07] Editar un cupón agotado lo revive a 'activo' sin resetear el stock: vuelve al catálogo y toda activación falla con "cupón agotado"
- **Sector:** D1 · lentes 12 Contador desincronizado del estado + 11 Límites
- **Síntoma:** Un cupón se agota. El comercio lo edita para cambiarle cualquier cosa (el título, la vigencia) y al guardar vuelve a aparecer publicado en la app. Pero ningún vecino lo puede activar: cada intento devuelve "cupón agotado". Queda un cupón zombi en el catálogo, que el comercio ve como activo y que sólo genera errores.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminCuponEditPage.tsx:516`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:229`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:244`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/activations.ts:103`
- **Fix:** En PATCH /coupons/:id, no permitir pasar a 'activo' un cupón cuyo stockUsado ya alcanza stockMaximo sin que la misma request suba el tope (o resetee el contador explícitamente); si el comercio sube stockMaximo, reactivar. En el front, mandar `estado` sólo cuando el comercio lo cambió de verdad, y ofrecerle un botón explícito "reponer stock". A mediano plazo, separar `estadoDeseado` (activo/pausado) de los derivados agotado/vencido, que se calculan al leer.

### [D1-08] Archivar o suspender una ciudad no cascadea a nada: los comercios de esa ciudad siguen suscriptos, Mercado Pago les sigue cobrando y su plata sigue contando en el MRR mientras la app les devuelve 410
- **Sector:** D1 · lentes 12 Integridad referencial (cascada de menos) + 8 Plata
- **Síntoma:** El owner archiva una ciudad. Desde ese momento absolutamente todo lo de esa ciudad devuelve 410/403: los vecinos no entran, los comercios no pueden ni loguearse. Pero las suscripciones siguen 'authorized', Mercado Pago sigue debitando mes a mes a comercios que no pueden usar el producto, y el panel del owner sigue mostrando esa plata en el MRR y esa ciudad en el ranking, como si estuviera funcionando.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:781`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/middleware/tenant.ts:98`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:24`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/expiry.service.ts:24`
- **Fix:** Al pasar una App a suspended/archived: pausar/cancelar sus suscripciones (contra MP, ver D1-03), pasar sus comercios a 'suspendido' y dejar constancia en OwnerAuditLog. Y filtrar por estado de la App en las métricas del owner (o al menos separar "MRR de ciudades activas" de "MRR de ciudades archivadas") para que el número que se mira no incluya plata de un servicio que no se está prestando.

### [D1-09] Poner una ciudad en 'Pendiente' desde el panel del owner no la saca de servicio: sigue 100% operativa
- **Sector:** D1 · lentes 3 Contrato roto entre lo que ofrece la UI y lo que hace el backend + 6 Estados no manejados
- **Síntoma:** El owner ve un selector de estado con "Pendiente / Activa / Suspendida / Archivada" y el modelo describe pending como "creada pero no activa". Elige Pendiente para sacar una ciudad de circulación mientras la prepara. La ciudad sigue andando exactamente igual: vecinos entran, comercios operan, se canjean cupones. No hay ningún aviso de que ese estado no hace nada.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/middleware/tenant.ts:98`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/App.ts:68`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/AppDetailPage.tsx:297`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/tenant.ts:57`
- **Fix:** Invertir el gate a lista blanca: `if (tenant.status !== 'active') return 403/410 según corresponda`, con el mensaje adecuado por estado (pending → "esta ciudad todavía no está publicada"). Aplicar lo mismo en routes/tenant.ts y en optionalTenantContext. Si se quiere que 'pending' siga siendo navegable para preview, que sea con un token explícito de preview, no por omisión.

### [D1-10] El reconciliador de suscripciones vencidas ignora en silencio toda suscripción que no tenga nextBillingAt: esos comercios nunca se suspenden
- **Sector:** D1 · lentes 12 Reconciliación incompleta (fix previo con un caso sin cubrir)
- **Síntoma:** Un comercio con la suscripción cancelada/pausada/rechazada sigue operando indefinidamente —creando cupones, validando canjes, viendo la PII de sus clientes— si su suscripción nunca llegó a tener una fecha de próximo cobro. El job de reconciliación corre cada 10 minutos, no reporta ningún error y no los toca nunca.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/expiry.service.ts:48`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:106`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Subscription.ts:21`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/expiry.reconcile.integration.test.ts:34`
- **Fix:** Cambiar el filtro a `$or: [{nextBillingAt: {$lt: now}}, {nextBillingAt: {$exists: false}}, {nextBillingAt: null}]` (una suscripción cancelada sin período pagado conocido no tiene período pagado por delante), y agregar un test que cubra explícitamente el caso sin nextBillingAt. Loguear cuántas suscripciones lapsed se saltearon por datos incompletos, en vez de reportar 0 silenciosamente.

### [D1-11] El comercio suspendido conserva la lista completa de PII de sus clientes hasta una hora, aunque el propio middleware documenta que esos endpoints deberían estar cerrados
- **Sector:** D1 · lentes 5 RBAC/gate incompleto + 12 fix previo con un caso sin cubrir
- **Síntoma:** Se suspende a un comercio por abuso o falta de pago. Durante la hora siguiente su panel sigue mostrando —y exportando— nombre, DNI, email, WhatsApp y fecha de nacimiento de todos sus clientes, más las notas privadas que escribió sobre ellos. El bloqueo sólo alcanza a crear cupones y validar canjes.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/middleware/auth.ts:117`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:395`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:432`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchant-auth.ts:383`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/jwt.service.ts:14`
- **Fix:** Aplicar requireMerchantActive a todos los endpoints listados en el propio doc del middleware (clientes, notas, recent, stats, wa/campaigns). Como el gate ya hace un findOne por request, el costo es un lookup indexado. Y agregar un test que recorra el router y falle si una ruta de esa lista no tiene el middleware, para que doc y código no vuelvan a divergir.

### [D1-12] El cupo de 4 campañas de WhatsApp por mes se evade mandando los mensajes de a uno por /wa/send, y el borde de mes se calcula en UTC y no en la hora del comercio
- **Sector:** D1 · lentes 11 Límites + 7 Fecha/timezone + 9 Contadores
- **Síntoma:** Un comercio que quiere mandar más de 4 campañas simplemente recorre su lista con el endpoint de envío individual: no consume cupo, no tiene rate limit y no queda agrupado como campaña. Además, el cupo se renueva tres horas antes de que empiece el mes para el comercio (a las 21:00 del último día, hora argentina), porque el corte se calcula con la hora del servidor.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:32`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:38`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:149`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:73`
- **Fix:** Medir el cupo por mensajes enviados en el mes (contar WaSend, no campaignId distintos) o, como mínimo, contar cada /send suelto contra el mismo presupuesto y ponerle rate limit; y no aceptar campaignId del cliente. Para el borde de mes, reusar tzForPais(tenant.pais) + monthStart de merchantStats en lugar de la hora del proceso.

### [D1-13] El MRR por ciudad suma monedas distintas en un solo número cuando el owner le cambia la moneda a una ciudad
- **Sector:** D1 · lentes 8 Plata (moneda mezclada)
- **Síntoma:** Una ciudad que cambió de moneda muestra en el panel del owner un MRR que es la suma aritmética de pesos y de la otra moneda, etiquetado con una sola moneda. El número no significa nada y sin embargo es el que se mira para decidir.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:40`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:45`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:163`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:734`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Subscription.ts:18`
- **Fix:** Indexar byCityMap por `${appId}|${currency}` y devolver una fila por (ciudad, moneda), o marcar explícitamente la fila como mixta y no sumarla. Además, bloquear (o exigir confirmación explícita del owner) el cambio de `moneda` en una ciudad que ya tiene suscripciones, y renombrar `amountARS` a `amount` para que el campo deje de mentir sobre su unidad.

### [D2-03] El MRR cuenta suscripciones 'authorized' de comercios suspendidos o cancelados: el MRR no cierra con los comercios que operan
- **Sector:** D2 · lentes 8 Plata / 12 Integridad referencial
- **Síntoma:** El owner suspende o cancela un comercio desde el panel (o desde /admin) y la tarjeta "MRR" del dashboard no se mueve. El comercio no puede operar, no aparece en el catálogo, pero sigue figurando como ingreso mensual recurrente. La cuenta de "comercios activos" y la de "suscripciones activas" divergen y nunca vuelven a cerrar.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:24`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:871`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/admin.ts:123`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/expiry.service.ts:56`
- **Fix:** Agregar al pipeline de computeMrr un $lookup a Merchant con match estado:'activo' (o denormalizar `merchantEstado` en la Subscription y mantenerlo con los mismos writes). Y en los tres puntos que cambian Merchant.estado a suspendido/cancelado, pasar la Subscription a 'paused'/'cancelled' en la misma operación (previa cancelación en MP, ver D2-01).

### [D2-05] El tope de $10.000.000 del ticket se bypassea entero cuando el cajero no carga el monto (precioReferencia no tiene .max)
- **Sector:** D2 · lentes 8 Plata / 11 Límites (N, N+1)
- **Síntoma:** El tope de $10M que protege las métricas se saltea sin tocar la API: basta con que el cupón tenga un "precio normal" absurdo y que el cajero confirme sin tipear el monto. Los canjes quedan registrados con tickets y ahorros de cientos de millones, y eso infla las ventas del comercio y el "Ahorro generado" cross-ciudad del owner.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/packages/shared/src/schemas.ts:298`, `/Users/alannaimtapia/dev/misanpedro-cazabug/packages/shared/src/schemas.ts:389`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:200`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:100`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Redemption.ts:22`
- **Fix:** Poner el tope en el invariante, no en el request: (a) agregar .max(10_000_000) a precioReferencia, precioFijo y costoReferencia en couponShape; (b) en redemptions.ts, clampear `montoEfectivo` a 10M antes de calcular el ahorro; (c) agregar `max: 10_000_000` a montoTicket en el schema de Redemption y Activation para que ni un script suelto pueda escribir por debajo.

### [D2-06] El reconciliador de suscripciones vencidas no dispara nunca si nextBillingAt quedó vacío (fix S11-01 incompleto)
- **Sector:** D2 · lentes 8 Plata / 13 Degradación
- **Síntoma:** El comercio cancela (o MP rechaza/pausa el cobro) y sigue operando gratis para siempre: crea cupones, ve la base de clientes, aparece en el catálogo. El sweep que se agregó justo para bajarlo nunca lo alcanza porque su filtro exige una fecha de próximo cobro que en muchos casos no existe.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/expiry.service.ts:48`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:106`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:157`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Subscription.ts:21`
- **Fix:** Cambiar el filtro a `$or: [{nextBillingAt: {$lt: now}}, {nextBillingAt: {$exists: false}}, {nextBillingAt: null}]`, o mejor: setear siempre nextBillingAt al crear la suscripción (now + 30d) y al autorizarla, para que el campo sea invariantemente presente. Agregar un test de integración con una sub cancelled sin nextBillingAt.

### [D2-07] El vecino ve "Ticket $X · Pagaste $Y" con un precio que el cajero nunca cargó (es el precio de lista del cupón)
- **Sector:** D2 · lentes 3 Contrato FE/BE / 8 Plata / 1 Costura
- **Síntoma:** En Canjeados, el vecino ve un desglose que parece un comprobante: "Ticket $12.000 · Pagaste $9.600 · Ahorraste $2.400". Pero si el cajero confirmó sin tipear el monto (el flujo normal, porque el campo es opcional y viene precargado), ese "Ticket" es el precio de lista que cargó el comercio, no lo que el vecino gastó. Le estamos mostrando como hecho una cifra estimada, y encima con la palabra "Pagaste".
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:253`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/CanjeadosPage.tsx:177`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/CanjeadosPage.tsx:204`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:66`
- **Fix:** Agregar `montoOrigen: 'cajero' | 'precio_lista'` (o un booleano montoEstimado) en Redemption y Activation, setearlo en redemptions.ts según si vino montoTicket, y en CanjeadosPage ocultar el bloque 'Ticket/Pagaste' (o rotularlo '≈ estimado') cuando el origen no sea el cajero. Corregir además el texto del Asesor para que refleje el fallback.

### [D2-08] El owner suma pesos, COP y CLP en un mismo número y lo muestra como ARS ("Ahorro generado")
- **Sector:** D2 · lentes 8 Plata (monedas mezcladas) / 3 Contrato FE/BE
- **Síntoma:** En Estadísticas del owner, la tarjeta "Ahorro generado · histórico, todas las ciudades" muestra un único importe con símbolo argentino que en realidad es la suma cruda de ahorros en ARS + COP + CLP + MXN. Con una sola ciudad colombiana el número deja de significar nada (se suman 1 a 1 monedas de magnitudes distintas). Lo mismo el gráfico "Canjes y ahorro por mes".
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:137`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/StatsPage.tsx:106`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/StatsPage.tsx:183`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:80`
- **Fix:** Denormalizar `currency` en Redemption al confirmar el canje (ya se tiene el tenant en contexto) y agrupar ahorroTotal/canjesPorMes por moneda, devolviendo un Record<moneda, número> como ya hace computeMrr.byCurrency. En el front, renderizar una línea por moneda en vez de un solo importe en ARS.

### [D2-09] Estadísticas del owner esconde el MRR de toda ciudad que no cobre en ARS (muestra $0)
- **Sector:** D2 · lentes 8 Plata / 6 Estados no manejados
- **Síntoma:** Si la plataforma cobra en COP, la pantalla de Estadísticas del owner dice "MRR (ARS) $0" y la "Tendencia de MRR" es una línea plana en cero, mientras el Dashboard —otra pantalla del mismo panel— muestra el MRR real desglosado por moneda. Dos pantallas del mismo producto contestan distinto la misma pregunta.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/StatsPage.tsx:86`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/StatsPage.tsx:92`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/DashboardPage.tsx:77`
- **Fix:** Reusar en StatsPage la misma lógica multi-moneda del Dashboard (listar cada entrada de byCurrency con su símbolo) y en la tendencia graficar una serie por moneda, o al menos la moneda dominante. Eliminar el `?? 0` a favor de un estado 'sin datos en esta moneda'.

### [D2-10] La tabla de Suscripciones del owner formatea todo en ARS ignorando el campo currency de la propia suscripción
- **Sector:** D2 · lentes 8 Plata (monedas mezcladas)
- **Síntoma:** En Pagos → Suscripciones, una suscripción de 80.000 COP se lista como "$ 80.000" con formato argentino. El operador que concilia cobros mirando esa tabla no tiene forma de saber en qué moneda está cada fila, aunque el dato viaja en la respuesta.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/pages/SubscriptionsPage.tsx:181`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/lib/format.ts:3`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/billing.ts:161`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Subscription.ts:18`
- **Fix:** Renombrar el campo a `amount` (con migración/alias) o, como mínimo, pasar `fmtMoney(s.amountARS, s.currency ?? 'ARS')` en SubscriptionsPage y agregar una columna Moneda. Usar también el locale del tenant en vez de es-AR fijo.

### [D2-11] El LTV del cliente se estima con ahorro×100/porcentaje teniendo el montoTicket real al lado — y para precio_fijo da cualquier cosa
- **Sector:** D2 · lentes 8 Plata (dos fórmulas para el mismo número) / 3 Contrato FE/BE
- **Síntoma:** La ficha de cliente muestra "LTV · valor del cliente" y "Ticket promedio" calculados a partir del ahorro y el porcentaje, cuando el ticket real ya viene en la respuesta del API. Los números no coinciden con "Ingresos generados" del Dashboard (que sí usa el ticket). Y en cupones de precio fijo sin precio normal el porcentaje es el del slider (default 30), así que el LTV sale inventado.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminClienteDetailPage.tsx:152`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminClienteDetailPage.tsx:78`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:417`, `/Users/alannaimtapia/dev/misanpedro-cazabug/packages/shared/src/valor.ts:12`
- **Fix:** Usar `c.ingresosTotal` de /redemptions/clientes para el LTV y `ingresosTotal / canjes` para el ticket promedio; dejar la fórmula inversa sólo como fallback para canjes legacy sin montoTicket, y sólo cuando tipoOferta no sea 'precio_fijo'. Borrar el reduce duplicado del Dashboard y el fallback de 4000 de merchantQueries.

### [D2-12] La ficha de cliente pide 500 canjes y el backend devuelve como máximo 200: la pantalla se contradice a sí misma
- **Sector:** D2 · lentes 10 Paginación / 6 Listas grandes / 8 Plata
- **Síntoma:** En un comercio con más de 200 canjes, la ficha de un cliente dice arriba "Total de canjes: 37" y abajo lista sólo 4, con un LTV calculado sobre esos 4. Dos números de la misma persona, en la misma pantalla, sacados de dos endpoints con topes distintos. Nada avisa que la lista está recortada.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminClienteDetailPage.tsx:44`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:355`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminClienteDetailPage.tsx:81`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:400`
- **Fix:** Agregar GET /redemptions/clientes/:userId/canjes con paginación real, o aceptar `userId` como filtro en /recent y devolver `{items, total, truncated}`. Mientras tanto, no pedir 500 si el backend clampea a 200 y mostrar 'mostrando los últimos N de M' cuando la lista esté recortada.

### [D2-17] Dos promesas contradictorias de "hasta cuándo es gratis" en la misma pantalla: "hasta los primeros 50 comercios" y una fecha a 90 días
- **Sector:** D2 · lentes 3 Contrato FE/BE / 8 Plata
- **Síntoma:** En Recomendá y ganá, el comercio lee en un párrafo "entrás gratis hasta que se completen los primeros 50 comercios" y tres bloques más abajo "Tu período gratis llega hasta el <fecha>". Dos respuestas distintas a la pregunta más cara del producto: ¿cuándo empiezo a pagar? Y las semanas que gana por referir se suman a la fecha, que es justo el criterio que la copy dice que no aplica.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminReferidosPage.tsx:156`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminReferidosPage.tsx:197`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchant-auth.ts:77`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:177`
- **Fix:** Decidir UNA regla y hacerla dato: si el modelo es por hito, guardar en App `gratisHastaComercios` y calcular el estado del trial contra contarComerciosAdheridos(); si es por tiempo, sacar la copy del hito. Mientras tanto, en AdminReferidosPage mostrar la semántica correcta de los bonos (que hoy son días sobre una fecha) o esconder la fecha.

### [R1-04] El reconciliador de suscripciones es ciego a las suscripciones sin nextBillingAt: el comercio que cancela nunca se suspende
- **Sector:** N1 · lentes Estados no manejados + plata (lente 6+8)
- **Síntoma:** Un comercio cancela la suscripción y sigue operando gratis para siempre: crea cupones, ve la PII de sus clientes y aparece en el catálogo. Nadie se entera porque el sweep corre cada 10 minutos y reporta 0 suspendidos.
- **Evidencia:** `apps/api/src/services/expiry.service.ts:50`, `apps/api/src/routes/billing.ts:157`, `apps/api/src/routes/billing.ts:106`, `apps/api/src/models/Subscription.ts:21`
- **Fix:** En el `find` del reconciliador, tratar la ausencia de fecha como período vencido: `$or: [{nextBillingAt: {$lt: now}}, {nextBillingAt: {$exists:false}}, {nextBillingAt: null}]` — mismo patrón que ya usa dos líneas más abajo para freeTrialUntil. Complementario: en /billing/cancel, si `sub.nextBillingAt` es null, setearlo a `now` (cancelación inmediata) y decírselo así al comercio. Test: sub cancelled sin nextBillingAt + merchant activo → debe quedar suspendido.

### [R1-05] Los '3 meses gratis' del comercio no vencen nunca: el reconciliador sólo mira comercios que tienen Subscription, y el alta no crea ninguna
- **Sector:** N1 · lentes Costura demo/real + plata (lente 1+8)
- **Síntoma:** Todo comercio que se dio de alta queda gratis para siempre. Pasados los 90 días de 'prueba gratis' sigue publicando cupones y cobrando canjes sin que la plataforma le pida nunca una tarjeta ni le corte el acceso. El reconciliador que se escribió para cerrar esto hoy cubre exactamente cero comercios reales.
- **Evidencia:** `apps/api/src/routes/merchant-auth.ts:78`, `apps/api/src/services/expiry.service.ts:48`, `apps/api/src/services/expiry.service.ts:60`
- **Fix:** Agregar una segunda pasada en runExpirySweep que arranque desde Merchant: comercios `estado:'activo'` con `freeTrialUntil < now` y SIN Subscription en estado 'authorized' → pasarlos a 'pending_payment' (no 'suspendido': hay que dejarlos entrar a pagar; ojo que requireMerchantActive hoy permite operar en pending_payment, así que además hay que decidir el gate). Antes de activarlo, mandar aviso por mail a 7 y 1 día. Test: merchant con trial vencido y sin Subscription → debe cambiar de estado.

### [R1-06] El shutdown mata la campaña de WhatsApp en curso: existe awaitCampaign para esperarla y nadie la llama
- **Sector:** N1 · lentes Degradación + errores tragados (lente 13+14)
- **Síntoma:** El comercio arranca una campaña a 500 vecinos (dura ~29 min). En el medio hay un deploy. La campaña muere en el mensaje 200: 300 vecinos nunca reciben nada, el comercio ya gastó una de sus 4 campañas del mes, y su pantalla queda con la barra 'Enviando…' girando para siempre porque el evento `campaign.done` nunca llega.
- **Evidencia:** `apps/api/src/routes/whatsapp.ts:27`, `apps/api/src/index.ts:431`, `apps/api/src/routes/whatsapp.ts:266`, `apps/web/src/pages/admin/AdminWhatsappPage.tsx:577`
- **Fix:** En `shutdown()`, después de cerrar el server y ANTES de `mongoose.disconnect()`, esperar con timeout a todas las campañas en vuelo (`await Promise.race([Promise.allSettled([...campaignsInFlight.values()]), sleep(20_000)])`). Además: pasar un flag de cancelación a `sendCampaign` para que corte limpio y publique un `campaign.done` parcial, y persistir el estado de la campaña (total esperado + estado) en Mongo para que el front pueda cerrar la fase al reconectar. En el front, cerrar la fase también

### [R1-07] El candado de 'una campaña por comercio a la vez' vive en memoria del proceso: tras un reinicio o con dos réplicas el comercio duplica los envíos y el cupo
- **Sector:** N1 · lentes Carreras/idempotencia (lente 9)
- **Síntoma:** El comercio manda la misma promoción dos veces a los mismos vecinos (a algunos les llega repetida), y se le consumen 2 de sus 4 campañas mensuales por una sola promo. En el caso del reinicio, además, no tiene forma de saber cuántos vecinos ya la recibieron.
- **Evidencia:** `apps/api/src/routes/whatsapp.ts:25`, `apps/api/src/routes/whatsapp.ts:233`, `apps/api/src/routes/whatsapp.ts:194`, `apps/api/src/routes/whatsapp.ts:270`
- **Fix:** Persistir la campaña: crear un documento `Campaign {appId, merchantId, campaignId, total, status:'running'|'done'|'aborted', startedAt}` con índice único parcial sobre (merchantId, status:'running'). El 409 sale del índice único, no del Map; el cupo mensual se cuenta sobre Campaign (no sobre WaSend, que llega tarde); y al bootear, marcar como 'aborted' las running huérfanas para que el front pueda mostrar 'se interrumpió'. Dejar `campaignsInFlight` sólo como handle de espera para el shutdown (ve

### [R1-09] La pantalla 'Dónde tenés la sesión abierta' dice 'Sólo este dispositivo' cuando la consulta falló — miente justo en la pantalla de seguridad
- **Sector:** N1 · lentes Estados no manejados + errores tragados (lente 6+14)
- **Síntoma:** El vecino que perdió el celular entra a Perfil para ver dónde quedó su sesión abierta. Si la request falla (sin señal, 500, token vencido), la pantalla le afirma 'Sólo este dispositivo' — la conclusión exactamente contraria a la verdad, sobre la que puede decidir no cerrar sesiones.
- **Evidencia:** `apps/web/src/pages/PerfilPage.tsx:92`, `apps/web/src/pages/PerfilPage.tsx:318`, `apps/api/src/routes/user-auth.ts:335`
- **Fix:** Modelar los tres estados: `sesiones: {estado:'cargando'|'error'|'ok', items}`. En error mostrar 'No pudimos ver tus sesiones' + botón Reintentar, y dejar habilitado 'Cerrar sesión en todos lados' (que es la acción segura). Nunca renderizar 'Sólo este dispositivo' salvo con una respuesta 200 que efectivamente traiga 1 sesión. En backend, devolver `total` además del array para poder decir 'y N más'.

### [R1-11] El historial de campañas de WhatsApp subcuenta los envíos: agrega sobre los últimos 500 mensajes, no sobre la campaña
- **Sector:** N1 · lentes Plata / totales que no cierran (lente 8+10)
- **Síntoma:** El comercio manda una campaña a 500 vecinos, después otra a 300, y en el historial la primera aparece con 200 enviados en vez de 500. Los números de la pantalla 'Campañas' no cierran con lo que realmente se mandó ni con el cupo consumido.
- **Evidencia:** `apps/api/src/routes/whatsapp.ts:300`, `apps/api/src/routes/whatsapp.ts:181`, `apps/api/src/routes/whatsapp.ts:329`
- **Fix:** Reemplazar el find+agregación en memoria por un `WaSend.aggregate([{$match:{appId,merchantId,campaignId:{$ne:null}}},{$group:{_id:'$campaignId',sentCount:{$sum:{$cond:['$ok',1,0]}},failedCount:{$sum:{$cond:['$ok',0,1]}},sentAt:{$max:'$sentAt'},text:{$first:'$text'}}},{$sort:{sentAt:-1}},{$limit:50}])`: se limita por campaña, no por mensaje. Test: dos campañas de 500 y 300 → la de 500 debe reportar 500.

### [R1-18] En el alta, cuando el email ya existía, la pantalla dice 'pedí uno nuevo' pero no hay ningún botón para pedir un código nuevo
- **Sector:** N1 · lentes UX / estados no manejados (lente 15+6)
- **Síntoma:** El vecino está en el mostrador, pone su email, le dicen 'ya tenés cuenta, te mandamos un código'. El código tarda o vence (dura 5 minutos). Prueba, falla, y la app le dice literalmente 'Ese código no es correcto o ya venció. Pedí uno nuevo.' — y en la pantalla no hay ningún lugar donde pedirlo. La única salida es '← Usar otro email' y volver a mandar el formulario entero.
- **Evidencia:** `apps/web/src/pages/RegistroPage.tsx:130`, `apps/web/src/pages/RegistroPage.tsx:319`, `apps/api/src/routes/user-auth.ts:37`
- **Fix:** Agregar en la fase 'codigo' un botón 'Reenviar código' que llame a `userApi.requestOtp(email)` (con un contador de 30-60s para evitar el machaque), y que muestre el 429 con un mensaje humano ('esperá unos minutos'). Mismo botón en la fase 'codigo' de PerfilPage. Y revisar el cupo: 5 pedidos/hora es muy poco para una pantalla de mostrador donde el reintento es la reacción natural.

### [C1-02] Cada click en "Pagar" crea una Subscription y un preapproval nuevos: doble cobro posible y la suscripción vieja queda incancelable desde el panel
- **Sector:** C1 · lentes 9 carreras/idempotencia + 8 plata
- **Síntoma:** El comercio toca "Pagar", se arrepiente, vuelve y toca de nuevo. Quedan dos suscripciones vivas en MercadoPago. Si autoriza las dos (dos pestañas, o el link viejo del mail), le debitan el plan DOS veces por mes. En el panel ve una sola ("Activa") y el botón "Cancelar" sólo da de baja la última: la otra sigue cobrando y no hay ninguna pantalla donde verla.
- **Evidencia:** `apps/api/src/routes/billing.ts:157`, `apps/api/src/routes/billing.ts:204`, `apps/api/src/routes/billing.ts:226`, `apps/api/src/models/Subscription.ts:9`, `apps/web/src/pages/admin/AdminComercioPage.tsx:1345`
- **Fix:** Antes del create, reusar: `findOneAndUpdate({appId, merchantId, status:{$in:['pending','authorized']}}, {...}, {upsert:true})` o devolver el initPoint de la sub pending existente. Agregar índice único parcial `{appId:1, merchantId:1}` con `partialFilterExpression:{status:{$in:['pending','authorized']}}`. Y que /billing/me y /cancel operen sobre TODAS las subs vivas del comercio, no sobre la última.

### [C1-04] El vecino cancela y pisa un canje ya confirmado: el comercio cobró, el stock se consumió, pero la activación queda 'cancelado' y el canje desaparece del historial del vecino
- **Sector:** C1 · lentes 9 carreras/idempotencia + 12 integridad referencial + 8 los totales no cierran entre pantallas
- **Síntoma:** El cajero confirma el canje (ve "Canje confirmado", el descuento se aplicó, el Redemption quedó registrado y el stock bajó). El vecino, que en ese mismo instante tocó "Cancelar", ve "Cupón cancelado, lo podés volver a activar". Después el cupón NO le aparece en Canjeados, la pantalla del código nunca navega a Canjeados, y si intenta activarlo de nuevo le dice "Ya usaste este cupón el máximo de veces permitido". El comercio cuenta el canje y la plata; el vecino no ve nada.
- **Evidencia:** `apps/api/src/routes/activations.ts:248`, `apps/api/src/routes/activations.ts:253`, `apps/api/src/routes/redemptions.ts:143`, `apps/api/src/routes/redemptions.ts:266`, `apps/web/src/pages/CuponActivoPage.tsx:182`
- **Fix:** Cancel: `const res = await Activation.findOneAndUpdate({_id: id, appId, userId: auth.sub, status:'activo'}, {status:'cancelado'}, {new:true})`; si vuelve null, releer y devolver el estado REAL (409 'ya canjeado' si corresponde). Confirm: reemplazar el `save()` por `Activation.updateOne({_id, appId, status:'activo'}, {...})` y compensar si `modifiedCount===0`. En el front, no marcar 'cancelado' local sin mirar el `status` que devuelve el API (CuponActivoPage.tsx:177-183 hoy lo hace pase lo que pa

### [C1-06] Alta de comercio: el Merchant se crea ANTES de que la base garantice el email único → comercio fantasma publicado en el catálogo, sin dueño y sin forma de entrar
- **Sector:** C1 · lentes 9 carreras (doble alta simultánea) + 12 integridad referencial + 14 errores tragados
- **Síntoma:** En el directorio de la ciudad aparece un comercio duplicado (mismo nombre, slug "nombre-2") con teléfono y dirección reales, sin ningún descuento. Nadie puede loguearse a esa ficha: no tiene MerchantUser. El dueño, mientras tanto, vio un error 500 en el alta.
- **Evidencia:** `apps/api/src/routes/merchant-auth.ts:62`, `apps/api/src/routes/merchant-auth.ts:85`, `apps/api/src/routes/merchant-auth.ts:117`, `apps/api/src/models/MerchantUser.ts:21`, `apps/api/src/routes/merchants.ts:92`
- **Fix:** Envolver el `MerchantUser.create` en try/catch y, ante E11000, `await Merchant.deleteOne({_id: merchant._id})` + responder 409 'email ya registrado'. Mejor todavía: reservar el email con un insert previo en una colección de reservas con unique index ANTES de crear el Merchant. El mismo try/catch cubre el E11000 de slug (generateUniqueSlug también es exists-then-create).

### [C1-07] El guard del "último super-admin" cuenta y después escribe: dos bajas simultáneas dejan la plataforma sin ningún super habilitado
- **Sector:** C1 · lentes 9 carreras (countDocuments que después decide) + 11 límites N-1/N/N+1 + 5 RBAC
- **Síntoma:** Después de una limpieza de accesos, ningún owner puede invitar administradores, cambiar roles ni crear ciudades: todo /owner/admins y las rutas con requireOwnerRole('super') devuelven 403. No hay forma de recuperarlo desde el producto — hace falta entrar a Mongo a mano.
- **Evidencia:** `apps/api/src/routes/owner.ts:351`, `apps/api/src/routes/owner.ts:360`, `apps/api/src/routes/owner.ts:393`, `apps/api/src/middleware/auth.ts:99`
- **Fix:** Verificar DESPUÉS de escribir y revertir: `await target.save(); if ((await Owner.countDocuments({rol:'super', enabled:true})) === 0) { revertir y 409 }`. Mejor: mover el invariante a un doc de plataforma con `$inc` atómico condicionado a `$gt: 1`, o exigir un flujo de dos pasos (nombrar al reemplazo antes de degradar). Y agregar al arranque una alerta si `countDocuments({rol:'super', enabled:true}) === 0`.

### [C1-08] /redemptions/confirm no es idempotente: si se pierde la respuesta, el reintento del cajero dice "No se pudo confirmar" aunque el canje YA se registró
- **Sector:** C1 · lentes 9 carreras/idempotencia (doble submit) + 13 degradación (red del mostrador) + 14 errores tragados
- **Síntoma:** En el mostrador, con señal mala, el cajero toca "Confirmar canje", ve el error rojo "No se pudo confirmar — Sin conexión" y toca de nuevo. Ahora ve "No se pudo confirmar — ya canjeado". El cajero cree que el canje falló y le cobra el precio completo al vecino, o llama a soporte. En la base el Redemption está creado, el stock consumido y el mail al vecino salió.
- **Evidencia:** `apps/api/src/routes/redemptions.ts:146`, `apps/api/src/routes/redemptions.ts:235`, `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:121`, `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:124`
- **Fix:** Idempotencia por reintento: si el activationId ya tiene un Redemption y su `merchantUserId` (o al menos su merchantId) coincide con el que reintenta, devolver 200 con ESE redemption en vez de 409. Y en el front, tratar el 409 con `error:'ya canjeado'` como éxito ("Este canje ya estaba confirmado"), navegar a /admin igual y limpiar la validación cacheada.

### [C1-09] Un cupón 'agotado' vuelve a 'activo' al editarlo (o con cualquier PATCH parcial): aparece en el catálogo pero el vecino no lo puede activar
- **Sector:** C1 · lentes 11 límites/stock N-1/N/N+1 + 6 estados no manejados + 3 contrato FE/BE
- **Síntoma:** El vecino ve el descuento en la app, toca "Canjear descuento" y le salta "No se pudo activar el cupón — cupón agotado". El comercio lo ve publicado en su panel. Nadie entiende por qué está y no está.
- **Evidencia:** `apps/api/src/routes/coupons.ts:244`, `apps/api/src/routes/coupons.ts:229`, `apps/api/src/routes/activations.ts:103`, `apps/api/src/routes/coupons.ts:74`, `apps/web/src/pages/admin/AdminCuponEditPage.tsx:518`
- **Fix:** Que `estado` deje de ser editable salvo por la transición explícita activo↔pausado, y que el backend ignore (o rechace) un estado entrante cuando el cupón está 'agotado'/'vencido' y las condiciones derivadas siguen dándose. Si el comercio sube el stockMaximo, ahí sí re-evaluar: `if (nuevoStockMaximo > stockUsado && estado === 'agotado') estado = 'activo'`. Y unificar el criterio de disponibilidad en un solo helper usado por el catálogo, por /activations y por /validate.

### [R1-08] Pedir/reenviar el código OTP borra el código anterior ANTES de saber si el mail sale: con el transporte caído, el usuario pierde el código que SÍ tenía y quema el cupo de 5 intentos por hora
- **Sector:** C2 · lentes 13 Degradación · 9 Carreras/idempotencia · 6 Estados no manejados
- **Síntoma:** Al comercio le llegó el código pero se le venció la pantalla; toca 'No me llegó, reenviar código'. Justo el SMTP está caído. Ahora el código viejo (que estaba en su casilla y era válido) tampoco anda, el nuevo nunca llega, y después de cinco intentos el sistema lo bloquea una hora. Como el login es OTP-only, el comercio queda literalmente afuera de su panel.
- **Evidencia:** `apps/api/src/routes/merchant-auth.ts:231`, `apps/api/src/routes/merchant-auth.ts:273`, `apps/api/src/routes/user-auth.ts:94`, `apps/api/src/middleware/security.ts:116`, `apps/web/src/pages/admin/AdminLoginPage.tsx:142`
- **Fix:** Invertir el orden: generar y enviar primero, y recién con `sent.ok === true` invalidar los OTP anteriores y persistir el nuevo (o persistirlo con un flag `delivered` y que verify-otp acepte cualquier OTP no consumido y no vencido de ese email, no sólo el último). En el camino de fallo, devolver el token al bucket del rate limiter (o no consumirlo cuando el status es 5xx). Y en el front, cuando llega el 503, decirlo con todas las letras: 'No pudimos mandarte el código ahora. Si ya te había llegad

### [R1-09] Canje con red intermitente: el toast dice 'No se pudo confirmar' incluso cuando el backend responde 409 'ya canjeado' — el cajero no sabe si dar el descuento
- **Sector:** C2 · lentes 13 Degradación · 9 Carreras/idempotencia · 15 UX
- **Síntoma:** El cajero aprieta 'Confirmar canje' con el wifi del local haciendo agua. Aparece 'No se pudo confirmar'. Vuelve a apretar y ahora dice 'No se pudo confirmar: ya canjeado'. ¿Se registró o no? ¿Le hago el descuento o no? Nadie en el mostrador puede responder eso, con el cliente esperando.
- **Evidencia:** `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:119`, `apps/api/src/routes/redemptions.ts:235`, `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:111`
- **Fix:** Tratar el 409 'ya canjeado' como éxito idempotente: mismo toast de confirmación (con una nota 'ya estaba registrado') + clearCachedValidation + navigate('/admin'). Para el error de red genuino, ofrecer un botón 'Verificar si se registró' que consulte el estado de la activación antes de que el cajero decida. A nivel transversal: envolver el fetch de api.ts en try/catch y tirar un `ApiError(0, {error:'network'})` para que el resto de la app pueda distinguir red de negocio sin adivinar por el texto

### [R1-10] El resumen de campaña afirma 'Entregados' sobre un número que sólo significa 'el cliente de WhatsApp aceptó el envío'
- **Sector:** C2 · lentes 13 Degradación · 3 Contrato FE/BE
- **Síntoma:** Termina la campaña y el comercio ve tres casilleros: Enviados 120, Entregados 120, Leídos —. Cree que 120 vecinos recibieron el mensaje. En realidad sabemos que 120 llamadas a sendMessage no tiraron excepción; cuántos llegaron de verdad no lo sabemos.
- **Evidencia:** `apps/web/src/pages/admin/AdminWhatsappPage.tsx:524`, `apps/web/src/pages/admin/AdminWhatsappPage.tsx:978`, `apps/api/src/services/whatsapp.service.ts:336`
- **Fix:** Mostrar '—' en Entregados con el mismo tooltip que Leídos ('Disponible cuando se integre la WhatsApp Business API oficial'), o colapsar a dos columnas: Enviados / Fallaron. Si se quiere conservar el layout de tres, renombrar la primera a 'Salieron' y dejar Entregados y Leídos ambas en '—'.

### [R1-02] El estado 'pending' de una ciudad no bloquea nada: el panel del owner promete 'no se va a servir' y la ciudad sigue sirviendo la app completa
- **Sector:** T1 · lentes 6 estados no manejados / 2 multi-tenant
- **Síntoma:** El owner crea o pausa una ciudad dejándola en 'pending' porque todavía no la quiere lanzar. El panel le dice textualmente que no se va a servir. Sin embargo el subdominio sigue vivo: vecinos se registran, comercios se dan de alta y arrancan su trial de 3 meses, se emiten cupones y salen campañas de WhatsApp — todo en una ciudad que el dueño cree apagada.
- **Evidencia:** `apps/api/src/middleware/tenant.ts:98`, `apps/api/src/routes/tenant.ts:57`, `apps/owner/src/components/DnsSetupCard.tsx:48`, `apps/api/src/models/App.ts:69`
- **Fix:** Invertir el guard a allowlist en tenantContext y en /tenant/:slug/config: `if (tenant.status !== 'active') return 4xx` con un mensaje por estado (410 archived, 403 suspended, 404/409 'ciudad todavía no lanzada' para pending). Agregar un test de integración por cada estado del enum, para que sumar un estado nuevo obligue a decidir explícitamente si opera.

### [R1-03] La pantalla 'Dónde tenés la sesión abierta' traga el error del fetch y afirma 'Sólo este dispositivo' — falso negativo de seguridad justo en la pantalla de perder el celular
- **Sector:** T1 · lentes 14 errores tragados / 6 estados no manejados
- **Síntoma:** El vecino perdió el celular, entra a Perfil para ver dónde tiene la sesión abierta, y la app le dice 'Sólo este dispositivo'. Le está diciendo que nadie más tiene su cuenta. Pero eso es lo que muestra cuando la llamada FALLA (sin internet, 500, o el 403 'tenant mismatch' de tener el token de otra ciudad): la lista real puede tener el celular robado adentro y nunca se entera.
- **Evidencia:** `apps/web/src/pages/PerfilPage.tsx:92`, `apps/web/src/pages/PerfilPage.tsx:318`, `apps/web/src/pages/PerfilPage.tsx:328`, `apps/api/src/routes/user-auth.ts:332`
- **Fix:** Modelar tres estados en vez de uno: `sesiones: {status:'loading'|'error'|'ok', items:[]}`. En 'error' mostrar 'No pudimos leer tus dispositivos' + botón Reintentar (NUNCA una afirmación tranquilizadora); en 'ok' con 0 items mostrar 'Sólo este dispositivo'. Aplicar el mismo criterio en handleCerrarTodo: si logout-all falla, no navegar en silencio — avisar que no se pudo cerrar y ofrecer reintento.

### [R1-04] El fix multi-país de normalizeTelefono quedó a medias: App.phonePrefix no tiene default y el camino documentado para crear ciudades en prod (SEED_CITY_JSON) nunca lo setea → una ciudad no-argentina normaliza y arma WhatsApp con reglas de Argentina
- **Sector:** T1 · lentes 1 costura seed/real / 13 degradación
- **Síntoma:** En una ciudad colombiana creada por el camino de emergencia (variable de entorno), el celular del vecino se guarda mal y la campaña de WhatsApp del comercio sale hacia un número argentino inventado: el comercio ve 'enviados: 200' y no llega ninguno. El vecino tampoco puede ser contactado nunca.
- **Evidencia:** `apps/api/src/db/connection.ts:181`, `apps/api/src/models/App.ts:49`, `packages/shared/src/phone.ts:33`, `packages/shared/src/schemas.ts:105`, `apps/api/src/routes/user-auth.ts:180`
- **Fix:** Derivar el prefijo en el BACKEND, no en el formulario: un `prefijoPorPais(pais)` en packages/shared que use App.pais cuando phonePrefix está vacío, aplicado tanto en seedCityFromEnv como en POST /owner/apps (más un backfill idempotente de las Apps ya creadas). Complementario: que `toWhatsappDigits`/`normalizeTelefono` no caigan a AR en silencio cuando no reciben prefijo — devolver null, para que el bug se manifieste como 'no se pudo enviar' en vez de 'enviado a un número inventado'.

### [R1-05] seedCityFromEnv guarda el subdomain sin punycode ni validación de formato → una ciudad con acento/IDN queda inalcanzable por su subdominio (404 permanente)
- **Sector:** T1 · lentes 1 costura seed/real / 2 multi-tenant
- **Síntoma:** Se crea 'Mi Nariño' con subdomain 'minariño'. El DNS comodín resuelve, el server responde, pero minariño.micuidad.com devuelve 404 'tenant not found' para siempre. La ciudad existe en la base y no hay forma de entrar por su URL.
- **Evidencia:** `apps/api/src/db/connection.ts:189`, `apps/api/src/routes/owner.ts:670`, `apps/api/src/middleware/tenant.ts:148`, `apps/api/src/models/App.ts:52`
- **Fix:** Mover la normalización al modelo: `subdomain: { type: String, required: true, unique: true, lowercase: true, set: toAsciiLabel, match: /^[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?$/ }` en App.ts. Así los dos caminos de creación (y cualquiera futuro) quedan cubiertos por construcción y el handler de owner.ts puede seguir devolviendo el 400 lindo. Sumar un backfill idempotente que re-normalice los subdomains existentes.

### [R1-06] POST /auth/logout-all revoca los refresh pero NO los access: la pantalla promete 'con esto lo dejás afuera' y el celular perdido conserva hasta 1h de acceso total (incluye exportar y borrar la cuenta)
- **Sector:** T1 · lentes 4 auth cruzada / 3 contrato FE-BE
- **Síntoma:** El vecino pierde el celular, entra desde otro y aprieta 'Cerrar sesión en todos lados'. La app le confirma que lo dejó afuera. Durante hasta una hora, quien tenga el celular perdido sigue pudiendo abrir la app, ver su historial, descargar el export de habeas data con todos sus datos personales y hasta borrarle la cuenta.
- **Evidencia:** `apps/api/src/routes/user-auth.ts:322`, `apps/api/src/middleware/auth.ts:49`, `apps/api/src/services/jwt.service.ts:14`, `apps/web/src/pages/PerfilPage.tsx:328`, `apps/api/src/routes/user-auth.ts:427`
- **Fix:** Meter un `tokenVersion` (entero) en User/MerchantUser/Owner, incluirlo como claim en el access, e incrementarlo en logout-all / disable / revoke-support. La verificación no necesita pegarle a la DB en cada request si se cachea la versión unos segundos, y corta la ventana de 1h a segundos. Mínimo viable si eso es mucho: bajar ACCESS_TTL a 5-10 min para el sujeto 'user' y ajustar el copy de la UI para que no prometa un corte instantáneo.

### [R1-02] Cupón agotado: el front le dice al cajero "ya fue canjeado — el cliente ya lo usó", acusando al vecino de algo que no hizo
- **Sector:** F1 · lentes 3 contrato FE/BE · 11 límites/stock · 15 UX
- **Síntoma:** El cupón llegó a su cupo máximo. El vecino que lo había activado antes llega al local con su código válido. El cajero lo tipea y la pantalla le dice, en rojo: "Este descuento ya fue canjeado · El cliente ya lo usó en una visita anterior. Cada descuento vale una sola vez." El vecino no lo usó nunca. Discusión en el mostrador.
- **Evidencia:** `apps/web/src/pages/admin/AdminValidarPage.tsx:337`, `apps/api/src/routes/redemptions.ts:108`, `apps/web/src/pages/admin/AdminValidarPage.tsx:371`, `apps/web/src/pages/admin/AdminValidarPage.tsx:422`, `apps/web/src/lib/apiQueries.ts:186`
- **Fix:** Agregar case 'agotado': reason = 'sold-out' con copy propia ('Este descuento se agotó · Llegó al cupo que pusiste. El cliente no hizo nada mal: reactivá el cupo desde Descuentos si querés aceptarlo.'), y cambiar el default a un reason neutro tipo 'unknown' que muestre el v.message del backend en vez de afirmar que el cliente ya lo usó. Bonus: que errorCopy acepte el mensaje del backend como hint de fallback en lugar de descartarlo.

### [R1-05] useAsync sin cancelación: una respuesta vieja pisa a la nueva y el comercio ve la plata de otro período
- **Sector:** F1 · lentes 9 carreras/idempotencia · 8 plata · 6 estados
- **Síntoma:** El comercio toca "Este mes", "Últimos 7 días", "Mes pasado" en Estadísticas. El chip queda marcado en el que eligió, pero los números que ve son de otro período. No hay ningún indicio: no está cargando, no hay error, los importes son plausibles.
- **Evidencia:** `apps/web/src/lib/apiQueries.ts:40`, `apps/web/src/pages/admin/AdminEstadisticasPage.tsx:51`, `apps/web/src/lib/apiQueries.ts:240`, `apps/web/src/lib/apiQueries.ts:147`
- **Fix:** Copiar dentro de useAsync el patrón que ya usa useApiValidateByCode en este mismo archivo: let cancelled = false en el efecto, if (cancelled) return en .then/.catch/.finally, y return () => { cancelled = true }. Alternativa equivalente: un reqIdRef incremental y descartar respuestas cuyo id no sea el último. Con eso desaparecen de una todos los ítems del blast radius.

### [R1-06] La vigencia del cupón se corre un día en cada edición y el comercio y el vecino ven fechas distintas del mismo cupón
- **Sector:** F1 · lentes 7 fecha/timezone · 3 contrato FE/BE · 8 totales entre pantallas
- **Síntoma:** El comercio pone 'vigente hasta el 30 de agosto'. Vuelve a abrir el cupón para corregir el título y el campo de fecha ahora dice 31 de agosto. Guarda; la próxima vez dice 1 de septiembre. Mientras tanto, en la app del vecino ese mismo cupón dice 'Vigente hasta el 30 de ago'. Nadie sabe cuál es la fecha buena.
- **Evidencia:** `apps/web/src/pages/admin/AdminCuponEditPage.tsx:118`, `apps/web/src/pages/admin/AdminCuponEditPage.tsx:377`, `apps/web/src/pages/admin/AdminCuponEditPage.tsx:502`, `apps/web/src/lib/format.ts:16`, `apps/web/src/lib/format.ts:145`
- **Fix:** Reemplazar isoToDate por la inversa exacta de dateToIso: localISODate(new Date(iso)) (getters locales), función que ya existe en format.ts para exactamente este problema. Con eso el round-trip guardar→reabrir→guardar es idempotente y el editor coincide con lo que ve el vecino en formatVigencia. Agregar un test de round-trip con TZ=America/Argentina/Buenos_Aires (existe AdminCuponEditPage.derivar.test.ts como lugar natural).

### [R1-08] Owner: /equipo y /auditoria no tienen guard de rol — la ruta abre igual y le miente al usuario ("Todavía no hay administradores")
- **Sector:** F1 · lentes 5 RBAC sólo en el front · 6 estados no manejados · 14 errores tragados
- **Síntoma:** Un owner con rol viewer/soporte/finanzas/admin (todo menos super) no ve 'Equipo' en el sidebar, pero si escribe /equipo en la barra (o le pasan el link, o lo tiene en el historial) entra: ve el botón 'Invitar administrador' habilitado, un cartel rojo que dice 'forbidden' en inglés, y debajo el mensaje 'Todavía no hay administradores — Invitá a alguien para que gestione el negocio con vos'. La plataforma SÍ tiene administradores.
- **Evidencia:** `apps/owner/src/App.tsx:38`, `apps/owner/src/layouts/ShellLayout.tsx:38`, `apps/owner/src/layouts/ShellLayout.tsx:42`, `apps/owner/src/pages/TeamPage.tsx:100`, `apps/owner/src/lib/api.ts:58`
- **Fix:** Envolver las rutas gateadas en un <RequireRole recurso="equipo"> que use el can() que ya existe y redirija (o muestre 'No tenés permiso para ver esto') en vez de montar la página. Y en TeamPage/AuditPage separar los estados: if (error) → bloque de error/permiso ANTES de evaluar admins.length === 0, para no afirmar 'no hay administradores' cuando en realidad no se pudieron leer.

### [R1-09] Owner: suspender un comercio con el filtro de estado puesto saltea un registro de la paginación y deja el total desactualizado
- **Sector:** F1 · lentes 10 paginación · 6 estados
- **Síntoma:** El owner filtra 'Comercios · activos', suspende uno desde la lista, sigue bajando con 'Cargar más' y hay un comercio que simplemente nunca aparece. Además el contador del botón ('Cargar más (50 de 213)') sigue diciendo 213 aunque ya no haya 213 activos.
- **Evidencia:** `apps/owner/src/pages/MerchantsPage.tsx:69`, `apps/owner/src/pages/MerchantsPage.tsx:88`, `apps/owner/src/pages/MerchantsPage.tsx:38`
- **Fix:** Después de una mutación que puede sacar la fila del filtro activo, recargar la primera página (reset del listado) en vez de mutar in-place — o, si se quiere conservar la mutación optimista, mantener un contador loadedFromServer separado de merchants.length para el offset y decrementar total cuando la fila deja de matchear el filtro. Lo más robusto: paginar por cursor (createdAt+_id).

### [R1-10] Los totales del panel del comercio no cierran entre Inicio y Clientes cuando un vecino ejerce su derecho de borrado
- **Sector:** F1 · lentes 8 plata/totales entre pantallas · 12 integridad referencial
- **Síntoma:** El comercio ve en Inicio 'Clientes nuevos: 12' y 'Ahorro a clientes: $84.300'. Entra a Mis clientes y ve '11 clientes únicos' y 'Ahorro generado a clientes: $79.100'. Los dos números salen del mismo panel, del mismo período (todo), y no coinciden. Nadie puede explicar la diferencia.
- **Evidencia:** `apps/api/src/routes/user-auth.ts:435`, `apps/api/src/routes/merchants.ts:174`, `apps/api/src/routes/redemptions.ts:405`, `apps/web/src/pages/admin/AdminDashboardPage.tsx:84`, `apps/web/src/pages/admin/AdminClientesPage.tsx:104`
- **Fix:** Definir una política única para canjes anonimizados y aplicarla en los tres agregados: lo más honesto es contar clientesUnicos sólo sobre userId presentes (redemptions.filter(r => r.userId)) y exponer aparte los canjes anonimizados, o bien incluirlos en ambos lados. En el front, hacer que 'Clientes nuevos' del Dashboard y 'Clientes únicos' de la pantalla Clientes salgan del MISMO endpoint, para que sea imposible que discrepen.

### [R1-11] Los KPIs "hoy / esta semana / este mes" del panel salen de una ventana truncada a 200 canjes y contradicen a "Ventas con la app" en la misma pantalla
- **Sector:** F1 · lentes 8 plata/totales · 10 paginación · 6 estados
- **Síntoma:** Un comercio con volumen ve, en la misma pantalla: 'Este mes: 200' arriba y 'Ventas con la app: 640' abajo. Y si tuvo un mes fuerte, el 'Ahorro a clientes' del mes deja de crecer aunque los canjes sigan entrando.
- **Evidencia:** `apps/web/src/pages/admin/AdminDashboardPage.tsx:37`, `apps/web/src/pages/admin/AdminDashboardPage.tsx:60`, `apps/api/src/routes/redemptions.ts:359`, `apps/web/src/pages/admin/AdminDashboardPage.tsx:97`
- **Fix:** Servir los KPIs por período desde el backend (ya existe /merchants/me/stats/asesor?periodo=… con ventas/clientes/nuevos) en vez de derivarlos de /redemptions/recent. Mientras tanto, si un rango satura la ventana, no mostrar un número falso: marcarlo como '200+'.


# P3 (41)

### [R1-14] El fallback local del canje inventa un ticket de $4.000 y calcula el ahorro con el % aunque el cupón sea de precio fijo
- **Sector:** R1 · lentes 1 Costura demo/seed (número mágico) + 8 Plata
- **Síntoma:** Costura demo/producción todavía viva en el bundle: si el canje cae por la rama local (comercio sin sesión de API), se registra un ahorro calculado sobre un ticket ficticio de $4.000, con una fórmula distinta a la del backend, y sin guardar el monto. El cajero ve en la MISMA pantalla "Ahorro estimado: $X" (calculado con `calcAhorroCanje`, la fórmula buena) y se persiste $Y.
- **Evidencia:** `apps/web/src/lib/merchantQueries.ts:177`, `apps/web/src/lib/cuponValor.ts:31`, `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:124`, `apps/web/src/lib/stores.ts:243`
- **Fix:** Borrar `confirmRedemption` y toda la rama `source === 'local'` de AdminConfirmarCanjePage (o gatearla con `import.meta.env.PROD === false`, como ya se hace en DescuentosPage.tsx:88). Si se conserva, que llame a `calcAhorroCanje` y pase el `montoTicket` como tercer argumento a `markRedeemed`.

### [R1-15] Un vecino que borra su cuenta deja canjes sin userId: el dashboard del comercio cuenta a todos los borrados como UN cliente único fantasma y no coincide con la pantalla de estadísticas
- **Sector:** R1 · lentes 12 Contadores desincronizados + 8 Totales que no cierran
- **Síntoma:** El comercio ve en el dashboard "Clientes únicos: 41" y en Estadísticas "Total clientes: 40". La diferencia es un cliente inexistente que aparece en cuanto CUALQUIER vecino ejerce su derecho de supresión: todos los canjes anonimizados se agrupan bajo la misma clave vacía.
- **Evidencia:** `apps/api/src/routes/user-auth.ts:435`, `apps/api/src/models/Redemption.ts:17`, `apps/api/src/routes/merchants.ts:174`, `apps/api/src/services/merchantStats.ts:142`, `apps/api/src/services/merchantStats.ts:156`
- **Fix:** Filtrar los anonimizados en merchants.ts:174 (`.filter(Boolean)` como ya hace merchantStats), y mejor aún: en vez de `$unset`, marcar `userAnonimizado: true` dejando un identificador sintético estable por usuario borrado, para que "clientes únicos" y "primera visita" sigan siendo correctos sin exponer identidad. Además, sacar `required: true` de `Redemption.userId` o dejar de romperlo con $unset.

### [R2-12] Campaña con más de 500 clientes: el front deja apretar "Enviar" y el backend responde 400 'invalid input' — el comercio con base grande no puede mandar NUNCA una campaña
- **Sector:** R2 · lentes 11 límites N-1/N/N+1 + 14 errores opacos
- **Síntoma:** El comercio con 520 clientes ve "Montar campaña ahora · 520 clientes", confirma "¿Enviar a 520 clientes ahora?" y recibe "No pudimos enviar la campaña · invalid input". No hay ninguna indicación de que el tope sean 500 ni forma de segmentar para quedar por debajo (los buckets son sólo 'todos', 'recurrentes', 'nuevos').
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:183`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:192`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminWhatsappPage.tsx:407`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminWhatsappPage.tsx:484`
- **Fix:** Espejar el tope en el front (constante `MAX_RECIPIENTS_PER_CAMPAIGN = 500`, idealmente en packages/shared junto con MAX_CAMPAIGNS_PER_MONTH) y, cuando la audiencia lo supere, mostrar un aviso claro con la opción de enviar 'los primeros 500 por orden de último canje' o de partir la campaña. Y devolver `issues: parsed.error.format()` en el 400 de /wa/campaign para que el mensaje deje de ser 'invalid input'.

### [R2-13] El premio del programa de referidos está medido en "semanas de freeTrialUntil", un campo que el propio código declara que no corta nada — y la pantalla de Referidos muestra las dos promesas contradictorias juntas
- **Sector:** R2 · lentes 8 plata/producto + 3 contrato entre el modelo de precio y la UI
- **Síntoma:** En la misma pantalla de "Recomendá y ganá", el comercio lee "Tu colega entra gratis, igual que vos, hasta que se completen los primeros 50 comercios" y, tres tarjetas más abajo, "Tu período gratis llega hasta: 26 de octubre de 2026" (alta + 90 días). Dos reglas distintas sobre lo mismo. Y las "semanas ganadas" que acumula corren una fecha que, según el propio backend, no habilita ni corta nada.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/merchant-auth.ts:78`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Merchant.ts:119`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminReferidosPage.tsx:157`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminReferidosPage.tsx:200`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/coupons.ts:178`
- **Fix:** Decidir el modelo y hacer que el premio lo exprese. Si el modelo es por hito, el premio debería ser algo que exista (p.ej. 'X meses bonificados una vez que arranque el cobro', persistido en un campo propio tipo `mesesBonificados`) y la tarjeta 'Tu período gratis llega hasta' debe salir de la pantalla mientras el hito no se cumpla. Si se mantiene freeTrialUntil como unidad del premio, hay que implementar el corte (o al menos el aviso) al vencer, si no la métrica de progreso del referidor es humo.

### [R3-10] `OWNER_APP_URL` no está declarada en el esquema de entorno ni en el .env.example: el mail de invitación al equipo sale SIN link al panel
- **Sector:** R3 · lentes 1 Costura demo/prod (fallback silencioso) / 13 Degradación
- **Síntoma:** Sumás a alguien al equipo con un rol. Le llega un mail que dice 'Entrás con tu email y un código' y... ningún botón, ninguna URL. La persona no sabe a qué dirección entrar. Lo mismo con el mail del código: se pierde el magic-link de 'un toque' que el código construye.
- **Evidencia:** `apps/api/src/routes/owner.ts:126`, `apps/api/src/routes/owner.ts:326`, `apps/api/src/env.ts:101`, `apps/api/src/services/email.service.ts:262`
- **Fix:** Declarar `OWNER_APP_URL: z.string().url().optional()` en `env.ts`, agregarla a `.env.example` con el valor real (`https://administracion.micuidad.com/login`) y usar `env.OWNER_APP_URL`. Si está vacía, loguear un warning al boot en vez de degradar mudo. Y en el mail de invitación, si no hay loginUrl, al menos escribir el host de la plataforma en texto. Bonus: que `POST /admins` devuelva `{ok:true, emailEnviado:boolean}` para que TeamPage pueda avisar 'admin creado, pero no pudimos mandarle el mai

### [R3-11] Paginación por offset en la Auditoría (y en Comercios/Pagos/Vecinos): al "Cargar más" se duplican filas y se SALTEAN registros
- **Sector:** R3 · lentes 10 Paginación / auditoría
- **Síntoma:** En Auditoría tocás 'Cargar más' y aparecen filas repetidas — y, sin que se note, otras se perdieron para siempre en esa vista. En la superficie que existe justamente para reconstruir qué pasó, no podés confiar en que estás viendo todo. React además tira warning de keys duplicadas.
- **Evidencia:** `apps/owner/src/pages/AuditPage.tsx:70`, `apps/api/src/routes/owner.ts:436`, `apps/owner/src/pages/AuditPage.tsx:125`, `apps/owner/src/pages/AuditPage.tsx:73`
- **Fix:** Paginar por cursor: `?before=<at|_id>` con `find({ ...q, at: { $lt: cursor } }).sort({at:-1}).limit(n)` — estable frente a inserciones. Si se quiere el mínimo esfuerzo, al menos anclar la primera página con un `snapshotAt` (`at: { $lte: snapshotAt }`) que se mantenga en los 'Cargar más' de esa sesión, y deduplicar por `id` al concatenar. Y sanear `limit/offset` con `Number.isFinite`.

### [R3-12] Las agregaciones de negocio del owner cortan los meses y el snapshot diario en UTC, con la operación en -03
- **Sector:** R3 · lentes 7 Fecha/timezone
- **Síntoma:** Los canjes y las altas que ocurren después de las 21:00 hora argentina se contabilizan en el mes/día siguiente. En el cierre de mes los números del panel no coinciden con lo que el comercio ve en su propio panel, y la 'foto diaria' de MRR se corre según a qué hora reinicie el servicio.
- **Evidencia:** `apps/api/src/services/ownerStats.service.ts:72`, `apps/api/src/services/ownerStats.service.ts:83`, `apps/api/src/services/ownerSnapshot.service.ts:13`, `apps/api/src/services/ownerSnapshot.service.ts:9`
- **Fix:** Definir una zona de reporte (por ejemplo la de la ciudad principal, o UTC pero declarándolo en la UI) y pasarla explícita: `$dateToString: { format:'%Y-%m', date:'$redeemedAt', timezone: REPORT_TZ }`. Para el snapshot, usar la misma zona en `todayUtc()` (renombrándola). Y en StatsPage aclarar en el subtítulo 'meses en hora de Argentina (-03)' para que el número sea defendible frente a un comercio.

### [R3-13] El embudo trial->pago puede superar el 100%: numerador y denominador miden universos distintos
- **Sector:** R3 · lentes 8 Plata / los totales tienen que cerrar
- **Síntoma:** La tarjeta 'Embudo trial → pago' puede mostrar una conversión imposible (>100%) o inflada, porque cuenta como 'pagando' comercios que nunca entraron a trial y comercios ya cancelados o suspendidos.
- **Evidencia:** `apps/api/src/services/ownerStats.service.ts:141`, `apps/owner/src/pages/StatsPage.tsx:93`, `apps/api/src/services/expiry.service.ts:56`
- **Fix:** Calcular el embudo sobre una cohorte: `const idsTrial = await Merchant.distinct('_id', {freeTrialUntil: {$ne:null}})` y `pagando = (await Subscription.distinct('merchantId', {status:'authorized', merchantId:{$in:idsTrial}})).length`. Además excluir comercios en estado cancelado/suspendido del numerador. Y en el front, capear a 100 y mostrar el crudo (`12 de 40`) además del porcentaje.

### [R3-14] Crear una ciudad con un subdominio ya usado tira 500 'internal error' en vez de 409, y dos envíos simultáneos del wizard pueden chocar
- **Sector:** R3 · lentes 9 Carreras/idempotencia / 14 Errores tragados
- **Síntoma:** Terminás los 4 pasos del wizard de nueva ciudad, tocás 'Crear app' y aparece 'internal error' sin decirte qué pasó. El wizard no te dice que el subdominio ya lo usa otra ciudad; te quedás mirando un error genérico con todo el formulario cargado.
- **Evidencia:** `apps/api/src/routes/owner.ts:664`, `apps/api/src/routes/owner.ts:676`, `apps/api/src/routes/owner.ts:780`, `apps/api/src/index.ts:376`, `apps/owner/src/pages/NewAppPage.tsx:103`
- **Fix:** Envolver `App.create` en el mismo try/catch que el PATCH y devolver 409 con un mensaje que diga cuál campo chocó (leer `err.keyPattern`). Agregar un `GET /owner/apps/check?slug=&subdomain=` para validar en vivo en el paso 2 del wizard, y espejar RESERVED_SLUGS en el front. Validar `Types.ObjectId.isValid(id)` en `GET /apps/:id` y `/apps/:id/metrics` (ya se hace en /merchants/:id y /admins/:id) y descartar fechas inválidas en el parseo de `from`/`to`.

### [R3-15] `POST /owner/auth/refresh` revoca el refresh token ANTES de comprobar que sea de un owner: mandarle el token de un comercio lo desloguea
- **Sector:** R3 · lentes 4 Auth cruzada entre sujetos
- **Síntoma:** Un refresh token que no es del owner, si llega al endpoint de refresh del owner, queda revocado. El dueño de esa sesión (un comercio, cuya sesión está diseñada para no vencer nunca) se encuentra deslogueado sin haber hecho nada.
- **Evidencia:** `apps/api/src/routes/owner.ts:222`, `apps/api/src/services/jwt.service.ts:169`, `apps/api/src/services/jwt.service.ts:176`, `apps/api/src/routes/owner.ts:970`
- **Fix:** Pasarle a `rotateRefreshToken` el sujeto esperado (`rotateRefreshToken(token, {expect:'owner', ...})`) y devolver null SIN revocar si no coincide. Y propagar `impersonatedBy: doc.impersonatedBy` en el `issueRefreshToken` interno, para que la rotación no borre el rastro de impersonación aunque hoy nadie la dispare.

### [R3-16] La auditoría del owner es best-effort: si falla la escritura, la acción se completa igual y nadie se entera
- **Sector:** R3 · lentes 13 Degradación / 14 Errores tragados / auditoría
- **Síntoma:** Suspendés un comercio, cambiás un rol o cancelás una suscripción; la operación sale bien y la auditoría queda vacía si Mongo tuvo un hipo. La pantalla que existe para responder 'quién hizo qué' puede tener agujeros sin ninguna señal.
- **Evidencia:** `apps/api/src/routes/owner.ts:59`, `apps/owner/src/pages/AuditPage.tsx:82`, `apps/api/src/middleware/auditImpersonation.ts:27`, `apps/api/src/routes/owner.ts:873`
- **Fix:** Definir la política y hacerla explícita: para las acciones de alto impacto (`admin.*`, `merchant.*`, `subscription.*`, `app.*`, `support.session.start`), escribir el OwnerAuditLog ANTES de responder y devolver 500 si falla; dejar best-effort sólo para `auth.login`. Escribir `ownerEmail` y `recursoId` en todas las llamadas. Y si se mantiene el modo best-effort, marcar en la UI que la auditoría puede tener huecos.

### [R3-17] Doble pedido de código de acceso puede dejar dos OTP vivos y validar contra el viejo
- **Sector:** R3 · lentes 9 Carreras / 6 Estados
- **Síntoma:** Pedís el código dos veces (doble click, o volviste atrás y reenviaste). Te llegan dos mails. Usás el del último y te dice 'código inválido'. El que sirve es el primero, que es el que uno descarta por instinto.
- **Evidencia:** `apps/api/src/routes/owner.ts:110`, `apps/api/src/routes/owner.ts:160`, `apps/api/src/models/Otp.ts:23`
- **Fix:** Reemplazar delete+create por un `Otp.findOneAndUpdate({email, purpose:'owner'}, {$set:{codeHash, expiresAt, attempts:0, consumedAt:null}}, {upsert:true, new:true})` y agregar índice único parcial sobre `{email, purpose}` para purpose:'owner'. En `verify-otp`, buscar con `.sort({ createdAt: -1 })` y con `consumedAt: null, expiresAt: {$gt: new Date()}` dentro de la query. Y sumar un botón 'Reenviar código' con cooldown en LoginPage.

### [R3-18] El mail de invitación al equipo interpola el nombre sin escapar HTML, mientras el resto de los mails del owner sí escapa
- **Sector:** R3 · lentes seguridad / consistencia
- **Síntoma:** Un super que invita a alguien puede inyectar HTML arbitrario (links, imágenes remotas, texto falso) en el cuerpo del mail que la plataforma manda desde su propia dirección, a través del campo Nombre.
- **Evidencia:** `apps/api/src/services/email.service.ts:269`, `apps/api/src/services/email.service.ts:681`, `apps/api/src/routes/owner.ts:275`
- **Fix:** Envolver `input.nombre`, `input.rol` e `input.to` con `escapeHtml()` en `sendOwnerTeamInvite`. Y tipar el helper para forzar la decisión: `wrap(title: string, body: Html)` con un branded type `Html`, o exponer un tag template `html` que escape las interpolaciones por default.

### [R3-19] Settings muestra 'super' cuando falta el rol y anuncia como 'Próximamente' una sección que ya existe
- **Sector:** R3 · lentes 15 UX / contrato FE-BE
- **Síntoma:** En Settings, el campo Rol cae por default en 'super' si el perfil guardado no trae rol — le decís a alguien que tiene control total cuando puede ser un viewer. Y abajo, en 'Próximamente', figura 'Equipo (sumar otros owners con roles diferenciados)', que es exactamente la sección Equipo que ya está en el menú y funcionando.
- **Evidencia:** `apps/owner/src/pages/SettingsPage.tsx:53`, `apps/owner/src/pages/SettingsPage.tsx:106`, `apps/api/src/services/jwt.service.ts:29`
- **Fix:** Usar `rolLabel(auth.owner?.rol)` (que ya cae en '-') en vez del literal 'super'. Sacar 'Equipo' de la lista de Próximamente. Y hacer que `/me/audit` incluya también las entradas de OwnerAuditLog del propio owner (incluidas las de soporte) para que el historial personal sea real.

### [D1-14] Las activaciones no reservan stock: N vecinos activan la última unidad, uno la canjea y los demás se comen el "agotado" recién en el mostrador
- **Sector:** D1 · lentes 11 Límites (N-1 / N / N+1) + 9 Carreras
- **Síntoma:** Un cupón con 1 unidad de stock puede ser activado por cuantos vecinos quieran: todos ven su código listo para usar. Sólo el primero que llega al local lo canjea; el resto se entera ahí, delante del cajero, de que ya no había.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/activations.ts:102`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:183`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/Activation.ts:13`
- **Fix:** Antes de emitir el código, comparar el stock restante contra las activaciones 'activo' del cupón y, si ya hay tantas pendientes como unidades, avisar honestamente ("quedan pocas y hay N personas con el código: no te garantizamos la unidad") o bloquear. Alternativa más simple y honesta con el usuario: mostrar el stock restante en la pantalla del código.

### [D1-15] El historial de campañas de WhatsApp está truncado a 500 registros: campañas viejas desaparecen y las que quedan a mitad muestran conteos incompletos como si fueran totales
- **Sector:** D1 · lentes 10 Paginación/truncado + 9 Contadores que mienten
- **Síntoma:** El comercio abre el historial de campañas y ve que una campaña de 500 destinatarios dice "400 enviados". No falló nada: el backend leyó sólo las últimas 500 filas de envíos y agrupó lo que le tocó. Campañas anteriores directamente no aparecen.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:300`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/whatsapp.ts:178`
- **Fix:** Agregar por campaignId en la base ($group sobre WaSend con $match por appId+merchantId) y paginar por CAMPAÑA, no por mensaje. Si se prefiere no tocarlo ahora, al menos devolver una bandera `truncado: true` cuando se alcanzó el límite para que el front no presente un número parcial como total.

### [D1-16] PushSubscription.userId nunca se escribe: el vecino que borra su cuenta sigue recibiendo notificaciones y su suscripción push queda guardada sin forma de vincularla ni de borrarla
- **Sector:** D1 · lentes 3 Contrato del modelo vs. lo que escribe el código + 12 Integridad referencial
- **Síntoma:** El vecino borra la cuenta, la app le dice que sus datos personales fueron eliminados, y su celular sigue recibiendo los avisos de descuentos nuevos de la ciudad. Su suscripción push (endpoint del navegador + user agent) queda en la base indefinidamente, sin dueño y sin manera de encontrarla para borrarla.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/models/PushSubscription.ts:19`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/push.ts:38`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/user-auth.ts:428`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/push.service.ts:36`
- **Fix:** O se llena userId cuando la request trae sesión (leer el bearer de forma opcional en /push/subscribe y setearlo), y entonces DELETE /auth/me borra las suscripciones del vecino; o se saca el campo del modelo y se documenta que la suscripción push es anónima por dispositivo, ajustando el texto de la respuesta del borrado para no prometer más de lo que se hace.

### [D1-17] Cuando un canje se compensa por fallar a mitad, la activación vuelve a 'activo' pero se queda con los datos del comercio y el cupón pegados encima
- **Sector:** D1 · lentes 9 Compensación incompleta + 12 Integridad de estado
- **Síntoma:** Si algo se cae justo después de marcar el canje (un hipo de Mongo), el sistema deshace el canje para que el reintento salga limpio — pero la activación queda con el título del cupón y el nombre del comercio grabados como si ya se hubiera canjeado. Si más adelante ese cupón se borra, la app le muestra al vecino una tarjeta con datos de un canje que nunca ocurrió y con el id del comercio vacío.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:287`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/redemptions.ts:257`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/activations.ts:52`
- **Fix:** Agregar los cuatro campos de snapshot al $unset de la compensación. Mejor aún: extraer un par de funciones marcarCanjeado(activation, ...) / revertirCanje(activation) en el mismo módulo, para que la lista de campos exista una sola vez, y cubrir el camino de compensación con un test que inyecte el fallo.

### [D1-18] /api/v1/admin es un segundo camino para cambiar el estado de cualquier comercio de cualquier ciudad, sin RBAC, sin auditoría y con estados que el panel del owner no permite
- **Sector:** D1 · lentes 5 Dos caminos con reglas distintas para el mismo dato + 12 Trazabilidad
- **Síntoma:** Todo el trabajo de RBAC y de auditoría del panel del owner (quién suspendió a quién y cuándo) se puede saltear con un header estático: hay un endpoint paralelo que cambia el estado de cualquier comercio, admite estados que el panel no ofrece, y no deja rastro en ningún log de auditoría.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/admin.ts:112`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/admin.ts:15`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:865`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/index.ts:134`
- **Fix:** Dar de baja adminRoutes del router (o dejarlo detrás de una feature flag apagada por default) y migrar lo que falte al panel del owner con RBAC + OwnerAuditLog. Si tiene que quedar como escotilla de emergencia, que al menos escriba OwnerAuditLog con un ownerId sintético y que su enum de estados no sea más amplio que el del camino auditado.

### [D2-13] El "Ahorro total" del vecino deja de crecer a partir del canje 101 (GET /activations/me tiene limit(100) fijo)
- **Sector:** D2 · lentes 6 Listas grandes / 8 Plata
- **Síntoma:** La billetera de ahorro del vecino ("Ahorrado en tu ciudad", el número gigante de la home) y el total de Canjeados se calculan sumando las activaciones que baja la app, y la app sólo puede bajar 100. Al vecino más fiel —el que más nos importa— el contador se le congela y encima puede BAJAR cuando activa cupones nuevos, porque el orden es por fecha de activación descendente.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/activations.ts:208`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/components/features/SavingsWallet.tsx:109`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/lib/club.ts:61`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/lib/club.ts:45`
- **Fix:** Devolver los agregados desde el backend (GET /activations/me/resumen con ahorroTotal, canjesTotales, canjesEsteMes, racha, calculados con $group) y usar el listado paginado sólo para el historial visible. Si se mantiene el limit, agregar `total` y `hasMore` a la respuesta y ordenar por redeemedAt para el filtro 'canjeado'.

### [D2-15] El mapa de timezones cubre 6 de los 10 países que el owner puede elegir: las stats de Ecuador, EE.UU., Paraguay y Bolivia se calculan en hora argentina
- **Sector:** D2 · lentes 7 Fecha/timezone / 3 Contrato FE/BE
- **Síntoma:** Si se abre una ciudad en Ecuador o Estados Unidos, el Asesor de estadísticas del comercio calcula "este mes", "últimos 7 días" y el histograma "¿Cuándo viene la gente?" con el huso de Buenos Aires. Con EE.UU. son hasta 8 horas de corrimiento: los canjes de la tarde-noche del último día del mes caen en el mes siguiente y el gráfico por día de semana queda desfasado.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:25`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:35`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/lib/paises.ts:31`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:617`
- **Fix:** Mover el catálogo de países a packages/shared con {nombre, moneda, locale, prefijo, timeZone} y consumirlo desde el owner (selector), el backend (validación enum de createAppSchema/updateAppSchema) y merchantStats (tzForPais). Guardar `timeZone` directamente en App para no depender del string del país.

### [D2-18] /admin/metrics devuelve una CUARTA fórmula de MRR: suscripciones activas × precio del env, ignorando el precio real de cada ciudad
- **Sector:** D2 · lentes 8 Plata (dos fórmulas para el mismo número)
- **Síntoma:** Si alguien consulta el endpoint de super-admin con el token, obtiene un MRR distinto al del panel del owner: multiplica la cantidad de suscripciones por el precio global del .env, así que una ciudad con precioMensual propio (o en otra moneda) queda valuada al precio de San Pedro.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/admin.ts:40`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/routes/owner.ts:491`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/ownerStats.service.ts:25`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/index.ts:134`
- **Fix:** Borrar /api/v1/admin (nadie lo consume) o hacerlo delegar en computeMrr/computeOwnerStats para que exista una sola fórmula de MRR en todo el repo.

### [D2-19] Los umbrales de la billetera de ahorro no cubren dos de las monedas que el owner puede elegir (PYG y BOB)
- **Sector:** D2 · lentes 8 Plata (multi-país) / 11 Límites
- **Síntoma:** En una ciudad paraguaya el vecino llega a "Leyenda del barrio" con ₲50.000 (unos 7 dólares), y en una boliviana el mismo nivel exige Bs 50.000 (unos 7.200 dólares) y no lo alcanza nadie. La gamificación del ahorro queda rota en las dos puntas.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/components/features/SavingsWallet.tsx:12`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/components/features/SavingsWallet.tsx:24`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/owner/src/lib/paises.ts:31`
- **Fix:** Mover los umbrales al tenant (un campo en App, o derivarlos del precioMensual de la ciudad) en vez de una tabla de conversión hardcodeada. Como mínimo, completar PYG/BOB y cambiar el fallback: si la moneda no está en la tabla, mostrar el ahorro sin niveles en vez de asumir paridad con ARS.

### [D2-20] El crecimiento de ventas muestra "+100%" cuando el período anterior fue cero
- **Sector:** D2 · lentes 8 Plata / 6 Estados no manejados
- **Síntoma:** Un comercio que no tuvo ninguna venta el mes pasado y este mes facturó $400.000 ve "+100% vs el mes pasado", como si hubiera duplicado. El número correcto no es 100%: es indefinido. Con base cero, +100% subestima brutalmente y confunde la lectura del Asesor.
- **Evidencia:** `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:125`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/api/src/services/merchantStats.ts:167`, `/Users/alannaimtapia/dev/misanpedro-cazabug/apps/web/src/pages/admin/AdminEstadisticasPage.tsx:128`
- **Fix:** Devolver un discriminante (`{ tipo: 'nuevo' | 'pct' | 'sin_datos', valor?: number }`) y que el front muestre 'nuevo' / 'primer mes con ventas' en vez de un porcentaje inventado.

### [R1-12] Cuando el reconciliador nuevo suspende a un comercio, su panel le dice 'sesión expirada' en vez de 'tu cuenta está suspendida': el 403 del refresh se traga
- **Sector:** N1 · lentes Estados no manejados + errores tragados (lente 6+14)
- **Síntoma:** Al comercio cuya suscripción venció le corta el acceso de golpe, y lo que ve es un error genérico y después la pantalla de login con 'tu sesión expiró'. No se entera de que lo suspendieron ni de por qué, así que no sabe que tiene que ir a pagar — llama a soporte diciendo 'no me deja entrar'.
- **Evidencia:** `apps/web/src/lib/api.ts:92`, `apps/api/src/routes/merchant-auth.ts:391`, `apps/web/src/lib/merchantStore.ts:125`, `apps/api/src/services/expiry.service.ts:56`
- **Fix:** En `doRefresh`, tratar el 403 como definitivo: leer el payload, `tokens.clear(subject)` y despachar `msp:session-expired` con `{subject, reason: payload.estado}`. En ApiSync, si `reason` es suspendido/cancelado, navegar a `/admin/login?reason=<estado>` y que AdminLoginPage muestre el mensaje que ya está escrito en merchantStore.ts:128-133 en vez de 'tu sesión expiró'. Test: refresh que devuelve 403 con estado suspendido → el usuario termina en login con el mensaje de suspensión.

### [R1-13] El cupo mensual de campañas de WhatsApp se corta en la medianoche del servidor (UTC), no en la del comercio: 3 horas de cupo regalado o robado
- **Sector:** N1 · lentes Fecha/timezone (lente 7)
- **Síntoma:** El último día de cada mes, entre las 21:00 y las 23:59 de Argentina, al comercio le aparecen 4 campañas disponibles aunque ya usó las 4 del mes; y las campañas que mande en esa ventana se le descuentan del mes siguiente. En el borde de mes el número de 'campañas restantes' que ve no coincide con el mes calendario.
- **Evidencia:** `apps/api/src/routes/whatsapp.ts:32`, `apps/api/src/routes/whatsapp.ts:38`, `apps/api/src/routes/whatsapp.ts:17`
- **Fix:** Agregar `timezone` al modelo App (default 'America/Argentina/Buenos_Aires') y calcular los bordes de mes/semana/día con esa zona (Intl.DateTimeFormat con timeZone, o date-fns-tz). Pasar el tenant a `startOfMonth`. Test con TZ=UTC en el runner y un tenant en -03 verificando que un envío del 31/07 21:30 ART cuenta para julio.

### [R1-14] isLocalDb matchea 'localhost' en cualquier parte del connection string (usuario, password, appName): una URI de producción puede quedar clasificada como desarrollo y reabrir el _debugCode
- **Sector:** N1 · lentes Costura entorno/real (lente 1)
- **Síntoma:** El archivo existe para que ninguna configuración sola pueda hacer que el API entregue el código OTP por HTTP. Pero alcanza con que el connection string de producción contenga la cadena 'localhost' en cualquier lugar (un `?appName=localhost-migracion`, un usuario `localhost_admin`, una password generada que la contenga) para que `otpDisclosureAllowed()` vuelva a devolver true — y ahí cualquiera pide un OTP de cualquier email y lee el código en la respuesta.
- **Evidencia:** `apps/api/src/lib/envSafety.ts:20`, `apps/api/src/lib/envSafety.ts:31`, `apps/api/src/routes/user-auth.ts:151`
- **Fix:** Parsear la URI (con un parser de connection string de mongodb, o normalizando el esquema y usando `new URL`) y evaluar SÓLO la lista de hosts: es local si TODOS los hosts son loopback. Ante cualquier error de parseo, devolver false (fail-safe). Agregar tests con 'localhost' en appName, en el usuario y en la password. Adicional: loguear al arranque la decisión ('otp disclosure: ON/OFF, motivo') para que sea auditable de un vistazo.

### [R1-15] Dos pedidos de código simultáneos dejan DOS OTP vivos y verify-otp valida siempre contra el primero: el código que le llegó al vecino no funciona
- **Sector:** N1 · lentes Carreras (lente 9)
- **Síntoma:** El vecino toca 'Mandarme el código' dos veces (o se le va el pulgar), le llegan dos mails, prueba el ÚLTIMO —el que tiene más a mano— y la app le dice 'ese código no es correcto o ya venció'. Repite y sigue fallando, porque el único código que funciona es el del primer mail.
- **Evidencia:** `apps/api/src/routes/user-auth.ts:94`, `apps/api/src/routes/user-auth.ts:246`, `apps/api/src/models/Otp.ts:23`
- **Fix:** Dos cosas, independientes: (a) en los TRES verify, ordenar por el más reciente: `Otp.findOne({...}).sort({ createdAt: -1 })`; (b) poner un índice único parcial sobre {appId, email, purpose} y reemplazar delete+create por un `findOneAndUpdate(..., {upsert:true})` que pise codeHash/expiresAt y resetee attempts en una sola operación atómica. Test: dos request-otp en paralelo → un solo documento, y el código del segundo mail valida.

### [R1-16] El contador de intentos del OTP se resetea con cada nuevo pedido de código: el tope de 5 adivinanzas no tiene techo global
- **Sector:** N1 · lentes Auth + carreras (lente 4+9)
- **Síntoma:** El gate atómico impide adivinar más de 5 veces UN código, pero no impide adivinar indefinidamente: cada /request-otp borra el OTP y crea otro con attempts en 0. El único freno real al total de adivinanzas es el rate limit de 5 pedidos por hora — que es exactamente el que se puede saltear con R1-01.
- **Evidencia:** `apps/api/src/lib/otpGate.ts:24`, `apps/api/src/routes/user-auth.ts:94`, `apps/api/src/models/Otp.ts:16`, `apps/api/src/routes/user-auth.ts:38`
- **Fix:** Llevar el contador al sujeto, no al código: además del gate por documento, contar intentos fallidos por (appId, email, purpose) en una ventana de N minutos (campo en User/MerchantUser/Owner, o una colección `AuthThrottle`), y bloquear el email por 15 min tras, por ejemplo, 10 fallos acumulados sin importar cuántos códigos se pidieron. Es la defensa que no depende de la IP. Test: 3 ciclos de (pedir código + 5 fallos) → el 3º debe quedar bloqueado por email.

### [R1-17] El webhook de Mercado Pago puede mandar dos comprobantes de pago por la misma activación (lectura y escritura no atómicas)
- **Sector:** N1 · lentes Carreras / idempotencia de webhooks (lente 9)
- **Síntoma:** El comercio recibe dos veces el mismo mail de comprobante por un solo pago. Menor en impacto, pero es ruido en el canal donde después se le van a mandar avisos de cobro y suspensión.
- **Evidencia:** `apps/api/src/routes/billing.ts:107`, `apps/api/src/routes/billing.ts:116`
- **Fix:** Reemplazar exists+updateOne por un único `const res = await Merchant.updateOne({_id: sub.merchantId, estado:'pending_payment'}, {estado:'activo'}); const wasActivated = res.modifiedCount === 1`. Complementario: guardar el `dataId`/`x-request-id` de MP procesados en una colección con TTL e ignorar reentregas, que también protege contra reprocesar el mismo evento minutos después.

### [C1-05] El contador de semanas del referidor se incrementa con leer-modificar-escribir: se pierden semanas gratis y el panel deja de cerrar con la realidad
- **Sector:** C1 · lentes 9 carreras/idempotencia (contador no atómico) + 8 plata + 12 contadores desincronizados
- **Síntoma:** Un comercio que refirió a varios y los ve confirmados en "Mis referidos" (2 x "1 semana") tiene sólo 1 semana sumada a su freeTrialUntil y `weeksEarned=1` en el resumen. La suma de la lista NO coincide con el contador de arriba. En el borde del tope (7 de 8) puede pasar lo contrario: dos confirmaciones simultáneas pasan el `< 8` y se otorgan 9 semanas con el contador en 8.
- **Evidencia:** `apps/api/src/routes/coupons.ts:174`, `apps/api/src/routes/coupons.ts:175`, `apps/api/src/routes/coupons.ts:179`, `apps/api/src/routes/referrals.ts:50`, `apps/api/src/routes/referrals.ts:80`
- **Fix:** Hacer el crédito atómico y condicionado al cap en un solo update con pipeline: `Merchant.findOneAndUpdate({_id: referrerId, appId, $expr:{$lt:[{$ifNull:['$referralWeeksEarned',0]}, CAP_WEEKS]}}, [{$set:{ referralWeeksEarned:{$add:[{$ifNull:['$referralWeeksEarned',0]},1]}, freeTrialUntil:{$add:[{$cond:[{$gt:['$freeTrialUntil','$$NOW']},'$freeTrialUntil','$$NOW']}, 7*DAY]} }}], {new:true})`. Si vuelve null → cap alcanzado → marcar el Referral con rejectedReason:'cap_reached'. Escribir el Referral 

### [C1-10] Quitar el tope de stock mientras hay un canje en vuelo mata el cupón: el claim atómico no matchea contra un `stockMaximo` ausente y lo marca 'agotado' sin filtro de estado
- **Sector:** C1 · lentes 9 carreras + 11 límites/stock
- **Síntoma:** El comercio saca el tope de un descuento ("que lo use todo el mundo") y el cupón queda muerto: 'agotado', fuera del catálogo, con stockUsado por debajo de cualquier límite. Además, un cupón 'pausado' o 'vencido' puede terminar marcado 'agotado' por un intento de canje fallido.
- **Evidencia:** `apps/api/src/routes/redemptions.ts:184`, `apps/api/src/routes/redemptions.ts:189`, `apps/api/src/routes/coupons.ts:246`, `apps/api/src/routes/redemptions.ts:272`
- **Fix:** Blindar el predicado del claim: `$expr: { $lt: [{$ifNull:['$stockUsado',0]}, {$ifNull:['$stockMaximo', Number.MAX_SAFE_INTEGER]}] }`. Y condicionar el update de fallback igual que el de éxito: `Coupon.updateOne({_id, appId, estado:'activo', $expr:{$gte:[{$ifNull:['$stockUsado',0]},'$stockMaximo']}}, {estado:'agotado'})`, para no pisar 'pausado'/'vencido' ni marcar agotado un cupón que no lo está.

### [C1-12] El código de referido se puede regenerar en carrera y el link ya compartido deja de existir (el referido se pierde en silencio)
- **Sector:** C1 · lentes 9 carreras + 12 integridad referencial
- **Síntoma:** Un comercio viejo comparte su link de referidos y los comercios que se dan de alta con ese código no le figuran nunca como referidos. Nadie ve un error: el alta funciona perfecto, simplemente no se crea el Referral.
- **Evidencia:** `apps/api/src/routes/referrals.ts:33`, `apps/api/src/routes/referrals.ts:15`, `apps/api/src/routes/merchant-auth.ts:131`
- **Fix:** Claim atómico y one-time: `Merchant.findOneAndUpdate({_id, appId, $or:[{referralCode:{$exists:false}},{referralCode:null}]}, {referralCode: code}, {new:true})` y, si vuelve null, releer el doc y usar el código que ya está. Idealmente, un script de backfill que le ponga referralCode a todos los comercios viejos y sacar la generación lazy del GET.

### [C1-13] Todos los guards atómicos del canje dependen de índices únicos que se construyen en silencio: si alguno falla, el doble canje vuelve y nadie se entera
- **Sector:** C1 · lentes 9 carreras (el guard mismo) + 13 degradación + 1 costura test/prod
- **Síntoma:** Los tests de "no doble canje" y "no oversell" pasan en verde (crean los índices a mano con `createIndexes()`), pero en producción puede no existir alguno de esos índices y el sistema comportarse distinto sin ninguna señal: mismo código, mismo test suite, resultado opuesto.
- **Evidencia:** `apps/api/src/db/connection.ts:52`, `apps/api/src/db/connection.ts:65`, `apps/api/src/models/Activation.ts:52`, `apps/api/src/models/Redemption.ts:7`, `apps/api/src/routes/redemptions.integration.test.ts:124`
- **Fix:** Al arrancar (dentro de bootMutationsAllowed) verificar explícitamente que los índices críticos existen: listar `collection.indexes()` de Activation, Redemption, MerchantUser y Coupon y comparar contra la lista esperada; si falta alguno, `captureException` con severidad alta y exponerlo en /health. Agregar `Model.on('index', err => err && captureException(err))` para los modelos del canje. Y extender el syncIndexes del boot a Activation/Redemption/MerchantUser con el mismo reporte a Sentry que ya

### [R1-07] Conectar WhatsApp muestra 'Generando QR…' para siempre cuando el backend ya reportó error: lastError viaja por SSE y ningún componente lo lee
- **Sector:** C2 · lentes 14 Errores tragados · 6 Estados no manejados · 3 Contrato FE/BE
- **Síntoma:** El comercio entra a WhatsApp, ve el spinner 'Generando QR…' y se queda ahí. Diez minutos después sigue igual. Llama a soporte: 'no me sale el QR'. El servidor hace rato que sabe exactamente qué falló y no se lo dice a nadie.
- **Evidencia:** `apps/web/src/pages/admin/AdminWhatsappPage.tsx:137`, `apps/web/src/pages/admin/AdminWhatsappPage.tsx:228`, `apps/web/src/lib/useWhatsappStream.ts:107`, `apps/api/src/services/whatsapp.service.ts:216`, `nixpacks.toml:41`
- **Fix:** En ConnectionScreen, renderizar explícitamente `wa.status === 'error'`: reemplazar el spinner por un bloque de error con `wa.lastError` (o un mensaje traducido: 'No pudimos abrir la sesión de WhatsApp en el servidor') y un botón 'Reintentar' que vuelva a llamar a /wa/start. Agregar también un timeout de cortesía: si a los ~45s no llegó ni qr ni ready ni error, mostrar 'esto está tardando más de lo normal' con el mismo botón. Y en el backend, responder 503 en POST /wa/start cuando el módulo está 

### [R1-07] withTenant() —el helper que la propia documentación del middleware recomienda para scopear queries— es código muerto y tiene el spread invertido: cualquier filtro que traiga appId pisa el del tenant
- **Sector:** T1 · lentes 2 fuga multi-tenant (latente)
- **Síntoma:** Todavía no rompe nada porque nadie lo usa. Pero es una mina: el día que alguien siga el ejemplo de la doc y le pase un filtro armado a partir del body o del query string (patrón que ya existe en los endpoints del owner, que aceptan ?appId=), la query sale con el appId del atacante y el aislamiento entre ciudades se cae en silencio, sin error ni log.
- **Evidencia:** `apps/api/src/middleware/tenant.ts:179`, `apps/api/src/middleware/tenant.ts:175`, `apps/api/src/middleware/tenant.ts:116`
- **Fix:** Invertir el orden (`return { ...filter, appId: getAppId(c) }`) para que el tenant SIEMPRE gane, agregarle un test que le pase `{ appId: otro }` y verifique que sale el del contexto, y decidir: o se adopta en las rutas tenant-facing (y se prohíbe el literal `appId` suelto con un lint) o se borra junto con optionalTenantContext. Un helper de seguridad sin usos es peor que no tenerlo.

### [R1-08] Rutas del owner que reciben ids/appId por URL sin validar el ObjectId → CastError = 500 (con requestId en el body) en vez de 404/400
- **Sector:** T1 · lentes 6 estados no manejados / 14 errores
- **Síntoma:** En el panel del owner, un link viejo o un id copiado mal no muestra 'no encontrado': revienta con un error interno. El panel muestra un 500 genérico y en Sentry entra ruido de CastError indistinguible de una falla real.
- **Evidencia:** `apps/api/src/routes/owner.ts:721`, `apps/api/src/routes/owner.ts:801`, `apps/api/src/routes/owner.ts:846`, `apps/api/src/routes/owner.ts:868`, `apps/api/src/index.ts:369`
- **Fix:** Un middleware chico `objectIdParam('id')` (y un `objectIdQuery('appId')`) montado en ownerRoutes que devuelva 404/400 antes de tocar Mongoose, en vez de repetir el guard por handler. Alternativa mínima: agregar el `Types.ObjectId.isValid` en los 7 sitios listados y un test que recorra las rutas por-id del owner con un id basura esperando 404.

### [R1-09] Los refresh del vecino y del comercio se emiten con expiresAt en 2099, así que el índice TTL nunca limpia: la colección RefreshToken crece sin techo y los revocados quedan para siempre
- **Sector:** T1 · lentes 12 integridad / crecimiento
- **Síntoma:** No se ve nada roto hoy. Con el tiempo, cada login de cada vecino y cada comercio de cada ciudad deja un documento que no se borra nunca —ni cuando el vecino cierra sesión, ni cuando borra su cuenta— y la colección se vuelve el objeto más grande de la base sin que nada la pode.
- **Evidencia:** `apps/api/src/services/jwt.service.ts:18`, `apps/api/src/services/jwt.service.ts:109`, `apps/api/src/models/RefreshToken.ts:27`, `apps/api/src/routes/user-auth.ts:65`, `apps/api/src/routes/user-auth.ts:442`
- **Fix:** Separar los dos significados: dejar `expiresAt` como vigencia funcional y agregar `purgeAt` (por ejemplo revokedAt + 90 días, o createdAt + N años para los vivos) con el índice TTL apuntando a ese campo. Alternativa sin migración: al revocar, setear también expiresAt = now + 90 días, para que el TTL existente se lleve los revocados. Y devolver `total` en /auth/sessions para que la UI pueda decir 'mostrando 20 de N'.

### [R1-07] Perfil → "Ya tengo cuenta": la fase del código es un callejón sin salida (sin reenviar, sin volver, sin cambiar de email)
- **Sector:** F1 · lentes 6 estados no manejados · 15 UX · 13 degradación (el mail no sale)
- **Síntoma:** El vecino que se cambió de celular entra a Perfil → 'Ya tengo cuenta — entrar desde mi email', pone su mail y le aparece 'Revisá tu email'. El código no llega (spam, demora del SMTP, se equivocó de casilla). No hay botón reenviar, no hay volver, no hay usar otro email. La única salida es recargar la app.
- **Evidencia:** `apps/web/src/pages/PerfilPage.tsx:245`, `apps/web/src/pages/PerfilPage.tsx:80`, `apps/web/src/pages/admin/AdminLoginPage.tsx:314`, `apps/web/src/pages/RegistroPage.tsx:319`
- **Fix:** Extraer un componente <OtpStep email onResend onBack /> y usarlo en las tres pantallas; como mínimo, agregar en PerfilPage fase 'codigo' un 'No me llegó, reenviar código' con cooldown (portando handleResend/cooldown de AdminLoginPage.tsx:142-154) y un '← Usar otro email' que haga setFaseEntrar('email') (portando el de RegistroPage.tsx:319).

### [R1-12] Confirmar canje depende de sessionStorage: entrar por URL o con la pestaña reciclada rebota al cajero sin decir nada
- **Sector:** F1 · lentes 6 estados no manejados · 15 UX (navegación que pierde contexto)
- **Síntoma:** El cajero, con el cliente esperando, abre el link de confirmar canje (bookmark, pestaña nueva, o el celular recicló la pestaña en segundo plano) y la app lo tira a 'Validar descuento' en blanco, sin ningún mensaje. Parece que la app se reseteó sola.
- **Evidencia:** `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:22`, `apps/web/src/pages/admin/AdminConfirmarCanjePage.tsx:72`, `apps/web/src/lib/apiQueries.ts:211`, `apps/web/src/lib/apiQueries.ts:171`
- **Fix:** En vez de <Navigate replace> mudo, mostrar un estado explícito ('Necesitamos que valides el código de nuevo') con un botón a Validar; y guardar también el codigoNumerico en la URL o en el cache para poder re-validar automáticamente al montar. Como mínimo, scopear la key de sessionStorage por merchantId para que no sea legible entre sesiones distintas del mismo dispositivo.

### [R1-13] El CSV de clientes ignora el filtro de búsqueda: el comercio filtra un segmento y se lleva la base completa
- **Sector:** F1 · lentes 8 datos/alcance · 15 UX
- **Síntoma:** El comercio busca 'Marta' en Mis clientes, ve 1 fila, toca CSV y descarga un archivo con los 800 clientes (nombre, DNI, email y WhatsApp de todos). El diálogo de confirmación sí declara el número total, pero la expectativa de 'exporto lo que estoy viendo' no se cumple.
- **Evidencia:** `apps/web/src/pages/admin/AdminClientesPage.tsx:119`, `apps/web/src/pages/admin/AdminClientesPage.tsx:56`, `apps/web/src/pages/admin/AdminClientesPage.tsx:305`
- **Fix:** Exportar `filtered` (y decirlo en el diálogo: 'vas a exportar los N que estás viendo'), o bien ofrecer explícitamente las dos opciones ('exportar los N filtrados' / 'exportar los M totales'). Alinear también los totales del header con la lista visible, o etiquetarlos como 'totales del comercio'.


# Refutados (7)

- **[R1-10]** El código de recuperación se reporta como enviado aunque el mail no salga, si el deploy no tiene NODE_ENV=production — y la pantalla no ofrece reenviarlo — Las 6 citas son textuales y correctas (no hay evidencia inventada), pero el bug no se sostiene como P2 por dos razones.

1) La premisa del repro está tapada por el deploy config. `nixpacks.toml:57` se
- **[D2-16]** El comercio que cancela o queda suspendido sigue bajando la base de clientes con PII — el propio doc de auth.ts dice que ese endpoint debería estar gateado — Las CITAS del finding son todas exactas (archivo:línea coinciden, nada inventado) y la asimetría existe: /clientes, /clientes/notes y /recent no pasan por requireMerchantActive mientras /confirm, /cou
- **[R1-08]** bootMutationsAllowed confía únicamente en NODE_ENV — justo lo que envSafety.ts declara que no hay que hacer: sin esa variable el API arranca sin índice único de vecino y sin el reconciliador — Las 4 citas son textualmente exactas: no hay evidencia inventada. Lo que falla es la premisa de alcanzabilidad y las dos consecuencias titulares.

1) ALCANZABILIDAD. El repro arranca con "un deploy al
- **[C1-03]** Webhook duplicado de MercadoPago manda el recibo de pago dos veces (el claim de activación se lee antes de escribir y el resultado del update se tira) — La CITA es exacta: el patrón read-before-write existe tal cual (billing.ts:107-117), el `modifiedCount` del `updateOne` se descarta y no hay dedupe por `data.id`/`x-request-id` en ningún lado. Hasta a
- **[C1-11]** El cupo mensual de campañas de WhatsApp y el candado de "una campaña a la vez" viven en memoria del proceso y se deciden con un count previo — Las citas de código son textualmente correctas (salvo un desfasaje de ~7 líneas en la del cupo), pero el SÍNTOMA está refutado en sus dos mitades.

(a) "Un comercio puede mandar más de 4 campañas mens
- **[C1-14]** POST /push/subscribe: upsert concurrente sobre {appId,endpoint} puede devolver E11000 sin catch → 500 al vecino — Las dos citas de código son textualmente correctas (push.ts:38 upsert sin try/catch; PushSubscription.ts:28 unique compuesto) y el onError global de index.ts:369 efectivamente devuelve 500 + captureEx
- **[R1-03]** Mis cupones: skeleton que late para siempre cuando el cupón salió del catálogo (el fix del bug #3 se aplicó sólo en Canjeados) — La evidencia citada NO está inventada: los cuatro snippets coinciden carácter por carácter con el código actual de la rama cazabug/loop1-iso, y el mecanismo descripto (couponMap armado sólo con el cat
